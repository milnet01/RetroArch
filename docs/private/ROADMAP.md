# RetroArch Fork — Roadmap

Tracking file for this downstream fork of `libretro/RetroArch`. Items here are fork-local — they drive the audit / review / spec workflow on this side. Anything ready to send upstream is rebased and PR'd separately against `libretro/RetroArch`.

Status markers:
- 📋 pending
- 🚧 in progress
- ✅ done (kept for audit-recurrence detection)
- 🔄 deferred / waiting on upstream
- ❌ won't-fix / verified-FP / resolved-stale (kept for audit-recurrence detection — the static analyser will re-report these every run)

Severity: **CRITICAL** > **HIGH** > **MEDIUM** > **LOW**.

---

## 📊 Bundle progress (running summary)

Fixes land on `local/fixes-2026-04`. Each bundle is one logical theme; commits inside a bundle are the per-site changes.

| Bundle | Commit | Theme | Sites |
|---|---|---|---|
| 1 | `a9f5dcf7f4` `17cea42c5b` `9953abc994` | Wayland UAF defensive · BPS NULL deref · UPnP recursion | 3 |
| 2 | `403106af54` | Atomic save (tmp + rename) — task_save (3) + disk_index_file | 4 |
| 3 | `1f59d9d31d` | Bounds-check malicious save-state / replay file format | 3 |
| 4 | `8aedb937f1` | Local IPC + cheevos: localhost-bind, RAM bounds, chmod 0600, strcpy→memmove | 5 |
| 5 | `8eab27096d` `e54e1568ac` | Deref-before-NULL-check sweep (Linux build) + .claude/ gitignore | 8 |
| 6 | `6423760b01` | clang-analyzer security.ArrayBound: bind-order, scanline_even, mixer, options | 4 |
| 7 | `de0c6000c8` | task_http UAF cleanup at caller layer (core updater + pl thumbnail) | 3 |
| 8 | `9bf01aabdb` | Config triad cleanup: zombie settings, pool-overflow assert, default-mismatch | 3 |
| 9 | `088289d088` | Rewind compressor: bound `find_change` / `find_same` walks (incl. SSE2 path) | 1 |
| 10 | `bcb1ad483c` | Audio init buffer leaks · Menu init NULL-deref · BSV checkpoint malloc-fail | 3 |
| 11 | `9f7b59bcc1` | task_screenshot OOM NULL-deref · task_overlay calloc-OOM leak | 2 |
| 12 | `18b55ec71a` | Cloud sync manifest UB/traversal · core_option_manager OOM (×2) · webdav uninit-stack + OOM | 4 |
| 13 | `2386e898d4` | WebDAV digest-challenge: parse hardening (5 sites) + comparison typo + cnonce randomisation | 3 |
| 14 | `46855caa12` | libretro env callbacks: NULL-guard 8 SET_*/GET_* sites + GET_LANGUAGE HAVE_LANGEXTRA fallback | 9 |
| 15 | `149fdd5c1d` | Audio + gfx font init: free calloc'd struct on sub-init failure (6 sites) | 6 |
| 16 | `72b2a839f8` | gx_joypad pad bound (Wii) + webdav 404-body local-file write filter | 2 |
| 17 | `e3d7394951` | S3 SigV4 contract: path encoder + canonical query string sort | 2 |
| 18 | `349cc5644b` | Deref-before-check follow-up: command.c + video_driver.c + video_shader_parse.c | 3 |
| 19 | `5c99961ccd` | Realloc-leak class: runahead mylist_resize + bsv uint32s_bucket_expand (incl. adjacent calloc fix) | 3 |
| 20 | `d973d6cc0c` | Netplay password security: platform-CSPRNG salt + constant-time hash compare | 3 |
| 21 | `3d10605ee4` | BSV uint32s_index: double-free guard + RHMAP_PTR auto-create gate | 2 |
| 22 | `71fa88edb0` | Wayland NULL-deref + DnD leak (audit M `oi`, clang-tidy `os->output`, getline/buffer leak) | 3 |
| 23 | `89b8adf690` | Slang shader: explicit NULL-guard on four set_decoration sites | 4 |
| 24 | `92d8e97cd5` | Runahead temp-DLL: CSPRNG suffix + O_EXCL/O_NOFOLLOW atomic create | 1 |
| 25 | `e9681a0a4d` | ui_qt: NULL-guard the five SHDR_FLAG_MODIFIED writes | 5 |
| 26 | `c2818d5cfd` | Cloud sync read: verify Content-Length on response body (webdav + google_drive + s3) | 3 |
| 27 | `9ed554ae3b` | Resource caps: LAN host-discovery (256-host cap) + CUE serial-malloc (64 MiB ceiling, file_size clamp) | 2 |
| 28 | `856758e6b8` | clang-tidy MEDIUM memory-leak class: 7 sites (core_backup empty-strdup, core_info outer-struct ×2, gfx_animation delay==0, ozone node write-back, menu_setting double-strdup ×2, discord duplicate-key) | 7 |
| 29 | `ab7f4d0f6e` | clang-tidy MEDIUM branch-on-uninitialized-value: 3 stack-buffer reads on edge paths (task_screenshot, verbosity, menu_displaylist) | 3 |
| 30 | `e1d376da4c` | runahead: re-query savestate size mid-run (runahead_save_state + preempt_run) — heap-overflow guard on cores that grow state | 2 |
| 31 | `630c7294fd` | Audit S6 + S8: bind-index decomposition `_Static_assert` (menu_cbs_scan) + XInput XUSER_MAX_COUNT cap (`MAX_XINPUT_USERS = 4`, sweep xinput_joypad + xdk_joypad bound) | 3 |
| 32 | `c3ebdb11e4` | Cloud sync upload cap (Phase 1 of streaming spec): `cloud_sync_max_upload_mb` setting + cap check at `cloud_sync_update` dispatch (one site, serves s3 + webdav + google_drive) + UI under Cloud Sync (Advanced) | 1 |
| 33 | `e67532e244` | Audit S7: Wayland touch-slot index off-by-one (`<=` → `<`) at `input_wl_touch_state` + invariant comment naming `wl_touch_handle_down` slot-allocator as bound owner | 1 |
| 34 | `9ddc80cb84` `b27a516cf5` `db21ebd07e` `d6bbeeab55` `233568bfe0` `335bb31cf9` | Spec-needed cluster closure (S11 strip + S12 cppcheck suppress file + S3 wayland if/else + S1+S10 (1u<<N) UB sweep + S5 input_key_pressed bounds + S2 MENU_LIST_GET_SELECTION caller NULL-guard sweep) | 6 |
| 35 | `8c3d825be6` | Audit S4: per-line cppcheck suppressions for the three EGL-by-design context drivers (xegl_ctx + qnx_ctx + vivante_fbdev_ctx) — closes the cluster | 1 |
| 36 | `44e71ead14` | Audit S2 ext: materialui (3 sites: 3014, 10377, 10977) + rgui (4 sites: 5217, 7394, 7671, 8263) — same MENU_LIST_GET_SELECTION `->size` deref pattern Bundle 34 swept in xmb/ozone/cbs, applied to the indie-review-listed materialui/rgui sites | 2 |
| 37 | `e35479c146` | clang-tidy HIGH `vulkan_overlay_free`: gate destroy-loop on `vk->overlay.images` non-NULL (the existing `if (vk->overlay.images)` was *after* the loop's per-element read — wrong order) | 1 |
| 38 | `e1e0fd8e1d` | clang-tidy MEDIUM `bugprone-incorrect-roundings`: switch the cited cluster to `lroundf` (gfx_widgets.c:882/886/894 — same idiom three lines apart; materialui.c:8872; ozone.c:9575-9576 + 9750/9752/9753) | 9 |
| 39 | `9edf7ffa57` | clang-tidy MEDIUM `bugprone-suspicious-memory-comparison` at vulkan.c:5899 — narrow `memcmp` from the enclosing struct to the bare `float[16]` `.data` member; comment block documents the bit-exact change-detection intent | 1 |
| 40 | `2760d936c8` | clang-tidy MEDIUM `bugprone-sizeof-expression` at gfx_widgets.c:380/980/1934 — three FIFO write/read sites used `sizeof(msg_widget)` (pointer var); switched to `sizeof(disp_widget_msg_t*)` to match the init-site style at line 2126 (`MSG_QUEUE_PENDING_MAX * sizeof(disp_widget_msg_t*)`). Pointer size identical, intent now explicit at the call-site, FP no longer fires. Cluster too narrow (one file) for an AUDIT-POLICY entry. | 3 |
| 41 | `9498278d7e` | clang-tidy MEDIUM `clang-analyzer-security.FloatLoopCounter` cluster (13 sites): `menu/menu_displaylist.c:16166/16193/16243/16269/16317/16344` (DROPDOWN_LIST switch ST_INT/FLOAT/UINT, both subcases) + `menu/menu_displaylist.c:16509/16536/16585/16611/16658/16685` (DROPDOWN_LIST_SPECIAL switch, same shape) + `ui/drivers/ui_qt_widgets.cpp:518` (UIntComboBox::populate, missed by audit cite) + `ui/drivers/ui_qt_widgets.cpp:713` (UIntRadioButtons ctor). All `for (float i = min; i <= max [+ half_step]; i += step)` switched to integer index `for (int j = 0; j <= n_steps; j++)` with `n_steps = (int)((max - min) / step)` (or `+ half_step` for ST_FLOAT to match existing termination), and `i = min + (float)j * step` derived inside the loop body. Iteration count now deterministic; matches old code's intended count for non-pathological inputs. Cluster spans menu_displaylist + Qt only — too narrow for AUDIT-POLICY. | 13 |
| 42 | `e629aaaac2` | clang-tidy MEDIUM `bugprone-signed-char-misuse` cluster (5 sites). Each site fixed at the right level rather than uniformly: `menu/cbs/menu_cbs_ok.c:4151` — `int ret = false` was widening the `int8_t` return of `config_save_overrides`; declared `ret` as `int8_t` directly so the assignment matches the function signature (the switch on -1/0/1 still works). `libretro-db/rmsgpack.c:371` — `*s = (int8_t)tmp.u8` is the intentional msgpack 1-byte signed-int sign-extension; made the widening explicit `*s = (int64_t)(int8_t)tmp.u8` and added a one-line comment so the intent reads as deliberate. `menu/drivers/xmb.c:456/1921` — field `char entry_index_offset` only ever stores 0/1/2 (set at xmb.c:3502-3511); changed the field type to `unsigned char` so the read at line 1921 widens cleanly. `menu/menu_explore.c:303-305` — cast `p[0..2]` through `(unsigned char)` to match the existing pattern at line 287-288 in the same file (`return (unsigned char)a->path[0] - (unsigned char)b->path[0]`); avoids sign-extension UB if `char` is signed and the byte has the high bit set, since TOLOWER indexes a 256-entry table where a sign-extended negative would overflow. Cluster spans 4 unrelated subsystems — too narrow for AUDIT-POLICY, and the diagnostic itself already exists upstream. | 5 |
| 43 | `dbb3f030c3` | clang-tidy MEDIUM divide-by-zero cluster (4 cited / 2 fixed + 1 verified-FP, 1 covered-by-fix). Two source-level fixes rather than four per-site bandaids: `menu/drivers/materialui.c:8872` (init-time clamp) — added `if (font_data->glyph_width < 1) font_data->glyph_width = 1;` immediately after the `lroundf(font_size * 0.75f)` initial assignment in `materialui_init_font`. Reachable failure: font_size rounds to 0 AND the conditional `char_width > 0` overwrite at line 8919 doesn't run (font load failed or returned 0). The clamp covers the two cited divides (4727, 5566) plus six identical `width / glyph_width` patterns in the same file (4953, 5135, 5246, 5851, 6733, 6864) that the analyzer didn't flag but share the same denominator — fixing at the source kills the class. `menu/drivers/rgui.c:2411` (top-of-function guard) — added `if (max_width < 1 || max_height < 1) return false;` to `rgui_downscale_thumbnail`. The caller at line 2556 already gates `image_src->{width,height} >= 1` but leaves `thumbnail->max_{width,height}` unchecked, so the `display_aspect_ratio = max_width / max_height` divide at line 2422 and the SCALE_POINT-branch divides at lines 2477-2478 (cited as 2473) could both hit zero. Returning false matches the existing failure mode (caller frees `image_resampled.pixels` — initialized NULL, so `free(NULL)` no-op when we early-return without allocating). `menu/menu_setting.c:5773` — verified FP. The mod-by-types at line 5785 reads `(current_idx + types - 1) % types` where `types` comes from `libretro_device_get_size`, which unconditionally seeds `devices[0]=NONE` and `devices[1]=JOYPAD` before any conditional logic, guaranteeing `types >= 2` on return. clang-analyzer doesn't trace into the helper to learn the invariant. The right-side counterpart at line 8088 has the same guarantee. No code change. Cluster too narrow (2 unrelated files post-FP) for AUDIT-POLICY. | 2 |
| 44 | `5e7692e5ce` | clang-tidy MEDIUM `bugprone-suspicious-string-compare` cluster (10 cited sites across 5 files; roadmap "~14 sites" estimate was rough). All verified correct-intent ("if differ from constant"); no inverted-logic bugs in this batch. Mechanical close-out — appended explicit `!= 0` to each call: `core_info.c:1550` (suffix is NOT `_libretro` → strip it; the `+ 1` on the size already covers null-terminator for exact match), `menu/menu_displaylist.c:2587` (menu is NOT ozone), `menu/menu_displaylist.c:2695/2697` (entry->core_{name,path} != FILE_PATH_DETECT placeholder), `menu/menu_explore.c:534` (extension is NOT `.lpl` → skip), `menu/menu_explore.c:564` (entry->db_name differs from filename → use db_name as override), `network/netplay/netplay_frontend.c:3258` (resolved input differs from client → signal resim needed), `network/netplay/netplay_frontend.c:3299` (merged input differs from previous → signal resim needed), `network/netplay/netplay_frontend.c:6953` (MITM server returned non-zero session id → proceed with handler init; we sent zeroed unique, server should fill), `retroarch.c:799` (midi driver fell back to a different ident → write fallback name back to settings). Re-ran clang-tidy: cluster cleared to zero. Build verified clean. The `if (memcmp(...))` / `if (strcmp(...))` shorthand is house style throughout the codebase — these are the specific sites the linter calls out, and `!= 0` preserves semantics exactly while making "non-zero means differ" explicit at each call site. Cluster spans 5 unrelated files but with a uniform single-character mechanical fix — too narrow for an AUDIT-POLICY style entry. | 10 |
| 45 | `794a7a04bf` | cppcheck MEDIUM `nullPointerOutOfMemory` cluster in cloud-sync HTTP request builders (60 raw deref findings: 29 in `network/cloud_sync/google_drive.c`, 31 in `network/cloud_sync/webdav.c` — collapsing to 17 distinct allocation sites once the same alloc's downstream derefs are counted only once; roadmap "23 sites in webdav" estimate was based on the 2026-04-25 snapshot which didn't include the 12 `nullPointerArithmeticOutOfMemory` sub-findings against the digest header builder's `header + _len` strlcpy chain). Pattern: every public driver entry point (`*_sync_begin`, `*_read`, `*_update`, `*_delete`) and every internal context-builder (refresh-then-retry, folder-walk, begin-with-token, oauth-device-flow, digest-auth chain) had calloc/malloc returns dereferenced unconditionally on the next line — `task_http` hardening covered the network layer, but the per-driver state allocations were untouched. Fixes by category: **8 google_drive.c sites** — `gdrive_refresh_then_retry:516` (call cb_st->cb(false), free cb_st, return — caller has already passed cb_st ownership), `gdrive_resolve_parent:873` (same pattern), `gdrive_begin_with_token:1134` (call cb(NULL,false,NULL)), `gdrive_sync_begin:1175/1194` (return false — `task_cloudsync.c:1344` "Could not begin" path), `gdrive_read/update/delete:1335/1516/1653` (return false). **9 webdav.c sites** — `webdav_create_ha1_hash:194` / `_ha1:360` / `_ha2:383` (return NULL on malloc fail; existing caller at digest-parser line 244 already handles `if (!webdav_st->ha1hash) goto parse_fail`), `webdav_create_digest_response:406` (NULL-check own malloc + propagate up if `ha1`/`ha2` is NULL — frees the surviving sibling alloc), `webdav_create_digest_auth_header:476` (NULL-check own malloc + propagate up if `response` is NULL), `webdav_sync_begin:652` (free auth_header, return false), `webdav_read:765` / `_update:931` / `_delete:1058` (return false). The webdav digest chain is the load-bearing piece — `webdav_get_auth_header` already returns NULL when no creds are configured, and `task_push_http_*` handles a NULL `headers` arg cleanly (`task_http.c:542`), so the OOM-NULL propagation hits an established edge: a 401 retry path that the existing `webdav_needs_reauth` loop will retry once and then settle to a clean failure cb if OOM persists. Pattern mirrors the `webdav_ensure_dir` malloc NULL-check at `webdav.c:841` (Bundle 12, commit `18b55ec71a`) and the `task_overlay_handler` calloc-OOM cleanup pattern (Bundle 11). Re-ran cppcheck: zero `*OutOfMemory` findings remaining in either file. Build clean. Cluster too narrow (2 files in one subsystem) for an AUDIT-POLICY entry — but the pattern itself ("dereferencing a freshly-calloc'd pointer without a NULL check") is documented in [`docs/private/AUDIT-POLICY.md`](AUDIT-POLICY.md) as a class to fix at-source rather than suppress. | 17 |
| 46 | `6f59dc0558` | indie-review Tier-3 MENU dead-vtable-slots cleanup (5 slots, 8 files, net -59 lines). Removed `set_thumbnail_content` (dispatched nowhere — ozone+xmb populated the slot but only ever called the static fn directly), `update_thumbnail_path` (dispatched at `menu_cbs_scan.c:164-170` but every driver registered NULL), `list_prepend` (no driver fills, no caller dispatches), `navigation_increment` + `navigation_decrement` (dispatched 4× in `menu_driver.c` but every driver NULL — the noisy `xmb.c:10322-10325` comment block already documented "linking these just duplicates work"). Strip ordering: header struct first → cascade of `Wincompatible-function-pointer-types` from each driver flagged exactly which entries to drop next, removed in lockstep. Static functions `ozone_set_thumbnail_content` and `xmb_set_thumbnail_content` retained — they are direct call-sites inside their own files (10 + 8 calls respectively), only the vtable entries were dead. Trimmed 2 stale function-name lines from a doc-comment in `gfx/gfx_thumbnail.h` since the wrapper functions they named (`menu_driver_set_thumbnail_content`, `menu_driver_update_thumbnail_path`) had been removed long before. The 4 nav dispatch sites in `menu_driver.c` (UP/DOWN + SCROLL_UP/SCROLL_DOWN) collapsed cleanly — no surrounding `if`-block restructuring needed; only the trailing `if (foo) foo()` paragraph deleted. Build clean. | 5 |
| 47 | `54fc62b5c5` | audit LOW `ai/game_ai.c:153 + 182-184` — strcpy/strcat → strlcpy/strlcat (4 sites, 1 file, +4/-4). `game_ai_load` strcpy(g_game_name, name) bounded at sizeof(g_game_name) (1024 bytes); `game_ai_think`'s `data_path` build-up (strcpy of game_ai_lib_path + strcat "/data/" + strcat g_game_name) — three calls into a 1024-byte stack buffer from two 1024-byte global buffers, easily over-shootable — each bounded against sizeof(data_path). File already includes `<compat/strl.h>`; mechanical migration. Module is opt-in (`HAVE_GAME_AI=0` in default Linux config), so default build unchanged; standalone compile of game_ai.c with `-DHAVE_GAME_AI=1 -D_GNU_SOURCE -Wall -Wsign-compare` clean. Bundle small + scope-bounded; cluster too narrow for AUDIT-POLICY (one opt-in file, four sites). | 4 |
| 48 | `13689e17da` | audit MEDIUM `cores/libretro-video-processor/video_processor_v4l2.c:670/683/691` — calloc + strdup OOM NULL-checks in `v4l2_frame_times` (3 guards, 1 file, +6/-0). Three sites all feed an immediate `printf("%s", ...)` or `snprintf` body-deref of the result, so OOM here is a NULL-deref, not just a missed stat sample: lazy-init calloc (line 670) → `printf("%s", ft_info)` inside the once-per-second block at line 681; rotate-buffer calloc (line 683) → same downstream; `ft_info2 = strdup(ft_info)` (line 691) → `snprintf(..., "%s", ft_info2)` at 696. Three early-returns; function is `void`, called per dequeued v4l2 buffer, so an OOM-skipped frame just leaves `ft_info` NULL (next call's `if (ft_info == NULL)` retries the lazy init) and `ft_info2` NULL (cleanup site `close_devices` at 1559-1562 already gates `if (ft_info2)`). Module is opt-in (`HAVE_VIDEOPROCESSOR=0` per Makefile.common:2259-2261), so default Linux desktop build unchanged. Same opt-in / mechanical-fix shape as Bundle 47; cluster too narrow for AUDIT-POLICY (one opt-in file, three guards). | 3 |
| 49 | `0a6c471d46` | indie-review Tier-3 MENU `materialui` ident-invariant lockdown (1 comment block, 1 file, +6/-0) + roadmap reconciliation of two stale Tier-3 cross-references. **Code change:** 6-line invariant comment above `menu_ctx_mui`'s vtable-struct definition at `menu/drivers/materialui.c:12196` calling out that the `.ident` is intentionally `"glui"` (not `"materialui"`, despite the file/struct/UI-label naming everywhere else) for backward-compat with saved `retroarch.cfg` files, and naming `"glui"` as the only valid ident-string for any future `string_is_equal(menu_ident, ...)` check on this driver. Verified-no-live-mismatch first: 11 callsites in the codebase all use `"glui"`; renaming the ident is rejected as it would break user configs. **Roadmap reconciliations:** (1) `MENU — materialui ident "glui"` (audit S11 recurrence, latent-risk class) closed by the comment lockdown above. (2) `CHEEVOS — rcheevos_filter_url_param overlapping strcpy at cheevos_client.c:169` closed as a duplicate cross-reference of the audit-section ✅ entry — fix landed in `8aedb937f1` Bundle 4 (`strcpy → memmove(start, next+1, strlen(next+1)+1)`); the indie-review tier-3 entry was a stale re-listing. No new code change needed for the cheevos closure. Cluster too narrow for AUDIT-POLICY (one comment line in one file). | 1 |
| 50 | (doc-only) | indie-review cross-cutting Tier-1 `🔥 Path traversal via attacker-supplied cloud-sync manifest` (`tasks/task_cloudsync.c:640`) closed as a duplicate cross-reference of the audit-section ✅ High entry (line ~93). Fix landed in `18b55ec71a` Bundle 12 — `task_cloud_sync_fetch_server_file` now captures `slash = strchr(key, '/')` first and treats `path = NULL`/empty/leading-slash/`..`-containing as malformed, short-circuiting with `RARCH_WARN` + manifest update + `sync_state->failures = true`. Verified in current code at `task_cloudsync.c:644-658`. Same reconciliation shape as Bundle 49's two stale closures — three independent reviewers (audit, tasks-lane, cloud-sync) flagged the same root cause from different angles; the audit-section entry was the canonical record and the cross-cutting Tier-1 listing carried the same cite forward. No new code change. | 0 |
| 51 | `220b4e07be` | audit LOW `cores/libretro-video-processor/video_processor_v4l2.c:617, 1297` — `strcpy(splitresolution, video_capture_resolution)` → `strlcpy(..., sizeof(splitresolution))` (2 sites, 1 file, +2/-2). Both buffers are `ENVVAR_BUFLEN` (1024) statics; source `video_capture_resolution` is bounded by `strlcpy` at lines 611/1236 to its sizeof, so the original `strcpy` was safe-by-construction (NUL-terminated within destination's bounds). Mechanical migration brings the calls in line with the rest of the file — the same function uses `strlcpy` for `video_capture_resolution` / `video_device` / `audio_device` / `video_capture_mode` etc.; the two `strcpy` sites were the only outliers. File already includes `<compat/strl.h>`. Module is opt-in (`HAVE_VIDEOPROCESSOR=0` per Makefile.common:2259-2261), so default Linux desktop build unchanged. Same opt-in / mechanical-fix shape as Bundle 47 (`ai/game_ai.c`) and Bundle 48 (the same file's OOM null-checks); cluster too narrow for AUDIT-POLICY (one opt-in file, two sites). | 2 |
| 52 | `b510d9d654` | indie-review Tier-1 cross-cutting `🔥 No atomic write on disk-save paths` — third bullet (`network/cloud_sync/{google_drive,webdav,s3}.c` every downloaded synced file). Bundle-2 `tmp+rename` idiom applied to the three `*_read_cb` callbacks (3 sites, 3 files, +138/-17). Each site: open `<file>.tmp` in `WRITE` mode, write the body and check the return-byte count, close; on full-write success `filestream_delete(dest); filestream_rename(tmp, dest)`; reopen `dest` in `READ` mode for the caller's MD5-then-close cycle (`task_cloud_sync_fetch_cb`); on partial-write/open/rename/reopen failure `filestream_delete(tmp_path); success=false`. Behavior change in failure paths only: a previously partial write or failed local open silently returned `success=true, file=NULL` (the "remote-doesn't-have-file" contract per `cloud_sync_driver.h:25-29`), so the next sync skipped the file; now those paths report `success=false`, causing a refetch. Local `make -j$(nproc)` clean (webdav.c + google_drive.c rebuilt); s3.c is opt-in (`HAVE_S3=0` in default config) but the change is structurally identical to the other two and uses the same set of `filestream_*` calls already present elsewhere in s3.c. Combined with the Content-Length verification already in place (line 219), this closes the cross-cutting "downloaded synced file gets silently truncated/corrupted" failure mode. Pairs with the Bundle 12 path-traversal fix (closed as duplicate in Bundle 50) — both protect the local filesystem from a hostile sync server. | 3 |
| 53 | `f980df5d63` | indie-review Tier-3 DRIVER PATTERN `*_driver_find_driver` dedup. Survey at fix-time found 9 sites with the same canonical "Couldn't find any X / available are: ... / default to first" RARCH_ERR/RARCH_LOG_OUTPUT/RARCH_WARN block (audio, microphone, input, video, menu, location, camera, bluetooth, wifi, record), not 4 — indie-review only listed the largest. Hoisted into `driver_log_unknown(label, prefix, drv, verbosity_enabled)` in `retroarch.c` (decl in `driver.h`); reuses existing `find_driver_nonempty()` registry to enumerate idents so call sites don't need to know each subsystem's concrete `driver_t` type. Cloud-sync keeps its own variant — fallback target is `"null"` not first-driver, plus extra "only log if user named one" guard. Format normalized at the same time: `menu_driver_find_driver`'s "couldn't find" went WARN→ERR to match the other 9 subsystems; `menu` and `microphone` lost a stray trailing period; `microphone` lost a redundant inner null-check inside a loop already guarded by the same expression. No control-flow change at any call site. Net +70/-124 across 10 files (driver.h + 9 .c files). Build clean, binary smoke-tests OK. | 9 |
| 55 | `3537f96a38` | Two clusters cppcheck surfaced post-Bundle 54. **S2 follow-up sweep — 5 missed sites the Bundle 34/36 sweep didn't catch:** `ozone.c:4767` `ozone_change_tab` `menu_stack->size` after `MENU_LIST_GET(menu_list, 0)` — added `if (!menu_stack) return;`; `ozone.c:8009` `ozone_set_thumbnail_content` imageviewer branch — gated `selection_buf->list[selection].userdata` deref via ternary on `selection_buf`; `ozone.c:11770` `ozone_selection_changed` — added `if (!selection_buf) return;` before the deref-using node init; `xmb.c:1626` imageviewer branch — same shape as ozone:8009, gated via ternary; `materialui.c:10050` `materialui_preswitch_tabs` — added `if (!menu_stack) return false;` between `MENU_LIST_GET(menu_list, 0)` and the `->size` read. The Bundle 34 grep keyed on the inline `MENU_LIST_GET_SELECTION(...)->size` form and the Bundle 36 sweep extended the same key to materialui/rgui — both missed sites where the assign-then-deref were spread across more lines, and both keyed only on `selection_buf` rather than `menu_stack` (the other macro the S2 contract covers). **Bundle 54 dead-clause cleanup — 4 sites in `menu_cbs_ok.c`:** `sys_info = &runloop_state_get_ptr()->system` is the address of a static struct member (cannot be NULL); Bundle 54 conservatively kept `\|\| !sys_info` clauses to match surrounding style, cppcheck correctly identified them as dead. `:7637` dropped `\|\| !sys_info` from disk_index handler; `:7648` stripped now-redundant `if (sys_info)` from same function; `:8354` dropped `\|\| !sys_info` from disk_cycle_tray_status (the now-unused `sys_info` decl was also removed); `:8390` dropped `\|\| !sys_info` from disk_image_append (decl retained, still used at `:8425`). Net +27/-11 across 4 files. Build clean. cppcheck cluster cleared on all 4 files post-fix. | 9 |
| 54 | `6dae6b6377` | Deref-before-NULL-check follow-up sweep on the larger files Bundle 5 deferred (`menu/cbs/menu_cbs_ok.c`, `menu/drivers/{ozone,xmb,materialui}.c`, `network/netplay/netplay_frontend.c`). cppcheck flagged 21 `nullPointerRedundantCheck` raw findings; triaged into 13 distinct sites — all addressed, cluster cleared to zero post-fix. **Real deref-before-check (9 sites — moved deref after guard):** `menu_cbs_ok.c:7635/8350/8384` `settings->bools.*` preceded the `if (!settings)` check by 3 lines; `ozone.c:11901` `(uintptr_t)ozone->pending_message` preceded the `if (!ozone) return;` check by 22 lines; `xmb.c:9695` `xmb->show_playlist_tabs + xmb_list_get_size(xmb,...)` preceded `if (!xmb)`; `xmb.c:9715` `selection_buf->size` outside the existing `if (selection_buf && ...)` guard at line 9710 — gated the whole `xmb_calculate_visible_range` block on `selection_buf`; `materialui.c:5916` `node->entry_width` derefed unconditionally with the `if (node)` thumbnail-draw block 88 lines later being dead — added `if (!node) return;` before deref-using layout math; `materialui.c:9659` `settings->uints.menu_remember_selection` preceded the `if (!mui || !settings)` check; `materialui.c:12045` `strlen(path)` preceded the `(path && *path)` check 4 lines later — moved `strlen` inside the guarded block. **Dead defensive consistency-fixes (4 patterns):** `menu_cbs_ok.c:1413` `if (delim)` where `delim` was a stack-array address (the surrounding while-loop already derefs it) — removed dead check; `ozone.c:3646/3749` `if (ozone->theme)` for fields derefed unconditionally 8+ other places in the same function — `ozone->default_theme` is set at init (`ozone.c:9275`) and `ozone->theme` is set unconditionally by `ozone_set_color_theme`, never NULL post-init; folded conditional assignments into unconditional initializers; `ozone.c:5903` `if (node) y += node->height;` where outer `if (!node \|\| ...) goto border_iterate;` already excluded NULL — removed dead check; `ozone.c:8010` + `xmb.c:1620` bare `strcmp(s,"imageviewer")` with sibling branches NULL-checking `s` — added `s &&` for consistency (xmb has documented NULL-passing callers at `xmb.c:1952/1959/2149/2843`); `netplay_frontend.c:8656/8931` `((key && *key) && (value && *value))` where `key = buf_data` (malloc-NULL-checked at 8634/8909) and `value = delim + 1` (delim NULL-checked at 8650/8925) — both non-NULL when the check runs; simplified to `*key && *value`. Closes the "📋 Larger files (`ozone.c`, `materialui.c`, `xmb.c`, `netplay_frontend.c`, `menu_cbs_ok.c`) need per-site review" sub-bullet of the indie-review/audit Bundle 5 deref-before-check cluster (line 111). Net +48/-35 across 5 files. Build clean. | 13 |
| 56 | `78729e2ca6` | cppcheck post-55 warning-level sweep on `network/` + `tasks/` — five small unrelated findings each fixed at the right level. **Dead clause (1):** `network/cloud_sync/s3.c:380` `s3_parse_url` log line had `(!bucket || !*bucket) ? "(none)" : bucket`; `bucket` is NULL-checked at the function-entry guard (line 147) so the `!bucket` clause cannot fire — reduced to `(!*bucket) ? ...`. Same dead-clause shape as Bundle 54/55 cleanups. **Format-string `%d` -> `%u` for `unsigned` args (2):** `network/netplay/netplay_frontend.c:8854-8855` `player_count`/`spectator_count` in the netplay announce body (both declared `unsigned` at lines 8747-8748 and filled by `retrieve_client_count(netplay, unsigned*, unsigned*)`); `tasks/task_autodetect.c:243` `_alt%d` for `alternative` (`unsigned` parameter at line 218, with `if (alternative > 0)` guard). Wire-format unchanged in practice since the values are always non-negative; the format now matches the actual argument type. **NULL-guard on attacker-influenced data (1):** `tasks/task_cloudsync.c:644` Bundle 12 hardened the path-traversal cases against malformed `path` (NULL/empty/leading-slash/`..`-containing) but missed the case where `key` itself is NULL — `CS_FILE_KEY(server_file)` returns `server_file->alt` which is JSON-derived (`tcs_string_handler` at line 130) and may be NULL when `tcs_object_member_handler` saw a "path" member but `tcs_string_handler` never got the value-string. `strchr(NULL, '/')` is UB. Fix: `slash = key ? strchr(key, '/') : NULL` so the existing `!path` short-circuit fires on NULL key; plus `key ? key : "(null)"` in the warning printf. Caller `task_cloud_sync_add_to_updated_manifest` already handles NULL key via string_list_append rejecting NULL strings. **OOM-NULL deref guard (1):** `tasks/task_content_disc.c:364` `task_push_cdrom_dump` derefs both `task = task_init()` (returns NULL on OOM per `task_queue.c:1117`) and `state = calloc(...)` four lines later without any NULL check, both leading to NULL writes. Added one combined `if (!task \|\| !state)` guard at function entry: free whichever succeeded, log, return. Pattern matches Bundle 11's `task_screenshot` OOM cleanup. Build: 4 buildable files compile clean (s3.c has a pre-existing fork-local breakage at lines 1880-1881 — `settings_t.arrays` is missing `access_key_id`/`secret_access_key`/`s3_url` — unrelated to my one-line edit; HAVE_S3 is opt-in default-off anyway). cppcheck cluster cleared on all 5 fixed sites; remaining `uninitvar: *list` at `netplay_frontend.c:2305` is a confirmed FP since `simulated_input[]`/`real_input[]` are members of a calloc'd `struct delta_frame` (allocated at line 7337) and cppcheck cannot trace calloc through struct-member access across function boundaries. Cluster too narrow (5 unrelated subsystems) for an AUDIT-POLICY entry. | 5 |
| 57 | `c4b1b42bf2` | cppcheck post-56 sweep on `gfx/` + `input/` + `command.c` (narrow scope, `--max-configs=1` after default-configs run timed out past 20 min on `materialui.c` per the audit-config `known_issues` note). Eleven warning/error sites across seven Linux-desktop files, batched as five unrelated sub-clusters. **invalidPrintfArgType_sint `%d` -> `%u` (11 sites, 4 files):** `gfx/display_servers/dispserv_kms.c:104,109` (×6) — `resolution_list_qsort_func` builds `"%04ux%04u (%u Hz)"` over `video_display_config_t::{width,height,refreshrate}`, all `unsigned` per `video_display_server.h:36-39`; `gfx/video_shader_parse.c:255,263,271` (×3) — `VID-USER-ROT` / `VID-FINAL-ROT` / `SCREEN-ORIENT` preset-wildcard expansions formatting `unsigned * 90` derived from `settings->uints.video_rotation`, `retroarch_get_rotation()` (returns `unsigned int` per `retroarch.h:196`), or `settings->uints.screen_orientation`; `gfx/video_crt_switch.c:218` — `super_width` is `unsigned int` per `crt_sr2_init` signature at line 164; `command.c:1841` — `del_idx` is `unsigned` (`command.c:1674`), cast to `(int)` rather than swap to `%u` because the bounded 5-byte buffer made `-Wformat-truncation=` fire on `%u` under GCC's tighter unsigned range analysis — `(int)` cast satisfies cppcheck's arg-type match while preserving the prior int-range bound (behaviour identical for the realistic savestate-slot range, `del_idx <= savestate_max_keep`). **invalidLifetime (1 site, 1 file):** `gfx/gfx_thumbnail.c:1177` — `coords.color = (const float*)thumbnail_color` reassigned over a prior `coords.color = (const float*)shadow_color` set at line 1163 inside the inner shadow-drawing block; `shadow_color[16]` was declared at the top of that inner block (was line 1129) and went out of scope at block-end three lines before the outer reassign. Strictly UB in C99 6.2.4 even without a read of the dangling pointer (cppcheck `invalidLifetime` is the same family as the prior gfx/audio shadow-callback warnings). Fix: hoisted the `shadow_color[16]` declaration to the outer scope alongside `thumbnail_color[16]` at the same brace-initializer style — array now stays in scope across the reassign. `gfx_display_set_alpha(shadow_color, shadow_alpha)` in the inner block still mutates only the alpha channels as before. **uninitvar (1 site, 1 file):** `gfx/drivers_context/xegl_ctx.c:299` — `temp.visualid = vid` read `EGLint vid` which is only initialized by `egl_get_native_visual_id(&xegl->egl, &vid)` under `#ifdef HAVE_EGL` (line 294-297). XEGL is always built with HAVE_EGL on Linux desktop but the structure leaves the read unsafe-by-default. Fix: `EGLint vid = 0` at declaration (split from the comma-decl with `num_visuals` to keep the assignment readable). **nullPointerRedundantCheck (1 site, 1 file):** `input/input_driver.c:4523` — `input_config_get_bind_string` derefs `bind->mbutton` unconditionally, but the four feeder branches at lines 4478/4482/4486/4490 all guard `bind &&`/`auto_bind &&` and the `#ifndef RARCH_CONSOLE` block at 4499 explicitly NULL-checks `bind`. Callers may legitimately pass `bind = NULL` with `auto_bind != NULL` and reach line 4523 with a NULL deref. Fix: `if (bind && bind->mbutton != NO_BTN)`. Same pattern as the upstream callers' guards. **False positives confirmed and not fixed:** `input/input_driver.c:2134` `arrayIndexOutOfBoundsCond` (cppcheck reports `idx<2` either redundant or `input_remap_ids[16][37]` accessed at index 92 — the `if (idx<2 && id<2)` IS doing real bounds work: max offset = RARCH_FIRST_CUSTOM_BIND(16) + (1*4) + (1*2) = 22, well under RARCH_CUSTOM_BIND_LIST_END(37)); `gfx/drivers_shader/shader_gl_cg.c:917` `arrayIndexOutOfBoundsCond` (Cg shaders HAVE_CG-gated, deprecated by NVIDIA, deferred). Build: 7 buildable files compile clean under `make -j4` with `-Wall -Wsign-compare`; the `command.c %u` -> `(int)` swap was specifically to keep -Wformat-truncation= silent. cppcheck cluster cleared on all 14 fixed sites post-fix. Cluster too narrow (5 unrelated rule families across 7 files in 4 subsystems) for an AUDIT-POLICY entry. | 11 |
| 58 | `6a2b703b28` | cppcheck post-57 sweep on `audio/` + `cheevos/` + top-level files (`playlist.c`, `core_info.c`) + `gfx/common/wayland_common.c` + Emscripten audio driver. Three actionable warning sites across three files plus five FPs documented in `.cppcheck-suppress.txt`. **nullPointerRedundantCheck — deref-before-check (2 sites, 2 files):** `playlist.c:1315` — `playlist_push` assigned `const char *core_name = entry->core_name;` at declaration, before the `if (!playlist || !entry) goto error;` guard four lines below at 1318. Fix: defer the assignment past the guard (`core_name = NULL;` at decl, then `core_name = entry->core_name;` after the check). `audio/drivers/wasapi.c:1471,1476` — `wasapi_free` cast `(wasapi_t*)wh` then read `w->write_event` before the `if (w)` guard at line 1473, and again at the unguarded `if (w->client)` at 1476. Fix: hoist the NULL check to an early-return — `if (!w) return;` then capture `write_event` once `w` is known valid (the `WaitForSingleObject(write_event, ...)` after `free(w)` at line 1485 still works because write_event is captured before the free). Windows-only path (`HAVE_MMDEVAPI`) but the deref-before-check is genuine. **uselessAssignmentPtrArg (1 site, 1 file):** `core_info.c:2802` — `core_info_free_core_updater_info` assigns `info = NULL;` immediately after `free(info)`, where `info` is the function parameter. The caller's pointer is unchanged either way (C passes by value); the line is dead. Fix: drop the line. **FP suppressions documented in `.cppcheck-suppress.txt` (S13/S14/S15):** `cheevos/cheevos_rvz.c:1753` — `SHA1Digest(data, len, digest[20])`'s third arg is an output buffer per the signature in `libretro-common/include/lrc_hash.h`; cppcheck lacks an annotation for output-only params and reads it as uninitialised at the call site. RVZ encryption fills a per-block hash array block-by-block; each cell is written before any reader runs. `gfx/common/wayland_common.c:292,1066` — `wl_list_for_each(pos, head, link)` is a for-loop macro that assigns `pos` from the list head before the first iteration; cppcheck 2.20 doesn't trace through the macro and reports `od->output` as uninitvar inside the loop body. Both Wayland output-enumeration sites pattern-match the same FP. `audio/drivers/audioworklet.c:182,184` — Emscripten's `EM_ASM_INT({...}, ...)` embeds JavaScript inside a C macro; the `ac.state == "running"` comparison is a JS string literal that cppcheck parses as a C `char *` literal comparison and flags `literalWithCharPtrCompare`. **Out-of-scope FPs not pursued:** `audio/librsound.c:765` `#error` directive for platforms without timer (compile-time gate, not a code issue); `gfx/include/dxsdk/{d3dcommon,dxgi1_6}.h` `#error` requiring updated `<rpcndr.h>` (vendored DirectX SDK headers, MinGW-only); `ui/drivers/ui_qt.h:724` `QT_VERSION_CHECK` macro parse error (cppcheck preprocessor limitation); `libretro-common/lists/vector_list.c:56,57` (vendored, scope.txt excludes `libretro-common/`); `record/drivers/record_ffmpeg.c:317` `AV_VERSION_INT` macro parse error (FFmpeg headers); `frontend/drivers/platform_emscripten.c:1058` (Emscripten port, scope.txt excludes); `frontend/drivers/platform_orbis.c:159` (PS4 Orbis port, `TARGET=retroarch_orbis` only). Build: 3 buildable files compile clean under `make -j4` (wasapi.c is Windows-only). cppcheck cluster cleared on all 3 fixed sites post-fix; 5 FP classes now permanently suppressed at-line. Cluster too narrow (3 unrelated rule families across 3 files in 3 subsystems, with 5 documented FP classes) for an AUDIT-POLICY entry. | 3 |
| 59 | `f03558b22c` | cppcheck post-58 sweep on `network/` + `tasks/` (the two remaining never-Bundle-swept subdirs after 57's `gfx/`+`input/`+`command.c` and 58's `audio/`+`cheevos/`+top-level). Initial scan surfaced 61 raw findings; prior bundles (52 atomic-write, 56 OOM-NULL+dead-clause, plus the standalone 794a7a04bf cloud-sync OOM-NULL-check and b510d9d654 cloud-sync atomic-write commits — both pre-bundled but not folded into a numbered Bundle row) had already closed 47 of those, leaving 13 actionable sites across 10 files plus 2 FP classes. Re-running cppcheck on the fixes-branch state confirmed the post-prior-bundle delta. **uselessAssignmentPtrArg (9 sites, 9 files — same `param = NULL;` after `free(param)` shape as Bundle 58's `core_info.c:2802`):** `task_autodetect.c:93` `autoconfig_handle`, `task_core_backup.c:144` `backup_handle`, `task_core_updater.c:268/682/1180/1607/1941` `list_handle`/`download_handle`/`update_installed_handle`/`pfd_install_handle`/`pfd_switch_cores_handle` (5 sites in the same file, each in its own `free_X_handle` helper), `task_database.c:1762` `manual_scan`, `task_menu_explore.c:66` `menu_explore`, `task_pl_thumbnail_download.c:405` `pl_thumb`, `task_playlist_manager.c:89` `pl_manager`. Every site is the last statement of a `free_X(X *param)` helper whose sole caller is the task-state free-callback — the assignment touches a function-local copy of the pointer that goes out of scope at the closing brace. Fix: dropped the dead line in each. **Dead-clause / redundant-condition cleanups (3 sites, 3 files):** `tasks/task_content.c:573` `if (archive_delim && *archive_delim)` — `archive_delim` was already proven non-NULL at the surrounding `if ((archive_delim = path_get_archive_delim(path)))` at line 555, and `archive_delim++` at line 572 cannot null a non-NULL pointer. Dropped the redundant first conjunct — left `if (*archive_delim)`. `tasks/task_pl_thumbnail_download.c:830` — `if (pl_thumb)` guard inside the `case PL_THUMB_ITERATE_TYPE` arm, immediately after the same arm already dereferenced pl_thumb at line 825 in `task_set_progress(task, ((pl_thumb->type_idx - 1) * 100) / 3);`. pl_thumb is proven non-NULL at function entry (line 757-758: `if (!(pl_thumb = ...)) goto task_finished;`). Stripped the dead guard and the now-stale `/* fold into the guarded block */` comment it was trying to justify. `tasks/task_translation.c:548` `scaler->in_stride = -1 * width * 3;` where `width` is `unsigned` and `in_stride` is `int`. Without the cast `-1 * width` is unsigned-promoted to UINT_MAX and the negative-stride encoding (used to flip the image vertically in the scaler) relies on implementation-defined unsigned-to-signed truncation. Cast width to (int) so the multiplication happens in signed space — `-1 * (int)width * 3`. **FP suppressions documented in `.cppcheck-suppress.txt` (S16, S17):** `network/net_logger.c:33,37` — `#error "An IP address for the PC logging server was not set in the Makefile"` and the parallel UDP port `#error`. The file requires `PC_DEVELOPMENT_IP_ADDRESS` / `PC_DEVELOPMENT_UDP_PORT` to be defined in CFLAGS at build time. Without them the netlogger driver cannot dial the developer's logging host, so the `#error` is a deliberate hard-fail to prevent a silent no-op build. The .c file is only compiled when qb's `--enable-netlogger` picks it up, so the `#error` is a precondition gate, not a defect. `network/netplay/netplay_frontend.c:2305` — `netplay_input_state_for()` takes `netplay_input_state_t *list` where `netplay_input_state_t` is itself a pointer type, so `*list` is a pointer the function walks as a linked-list head. Every call site passes `&delta->resolved_input[i]` or similar — the head field on a delta struct that was zero-initialised at allocation, so `*list` is always defined (NULL or a node). cppcheck doesn't infer the caller-side initialisation. **Out-of-scope/already-fixed FPs not in delta:** every `nullPointerOutOfMemory` finding from the initial network/cloud_sync/{google_drive,webdav}.c scan (29 + 15 sites) was already addressed by `794a7a04bf` (pre-Bundle 51); the s3.c:376 `nullPointerRedundantCheck` was already addressed by `78729e2ca6` (Bundle 56); netplay_frontend.c %d/%u and key/buf_data redundant-checks were already addressed by Bundle 57; task_autodetect.c %u, task_cloudsync.c NULL key, task_content_disc.c OOM were already addressed by Bundle 56. Build: 9 buildable files compile clean under `make -j4` (only pre-existing `-Wstringop-overread` warnings in `slang_cache.cpp` and `video_shader_parse.c`, unrelated to this bundle). cppcheck cluster cleared on all 11 fixed files post-fix (0 error/warning over the 11-file rerun). Cluster too narrow (2 unrelated rule families across 10 files in 2 subsystems, with 2 documented FP classes) for an AUDIT-POLICY entry. | 13 |
| 60 | `3545d70cdd` | cppcheck post-59 sweep on the next batch of never-Bundle-swept Linux-buildable surface — `runahead.c`, `libretro-db/`, `frontend/`, `record/`, `midi/`, `camera/`, `location/`, `bluetooth/`, and `disk_index_file.c` + `msg_hash.c`. Initial scan surfaced 28 warning-or-higher findings; 17 sorted as out-of-scope (`platform_orbis.c:159` PS4-port-only, `libretro-common/lists/vector_list.c` vendored, `libretro-db/c_converter.c` + `libretro-db/lua/lua_common.c` build-time tools not in default OBJ — 13 OOM-NULL hits between them) plus 2 confirmed FP classes (S18 emscripten EM_ASM_INT JS optional-chaining + S19 FFmpeg AV_VERSION_INT macro inside `#if`). Leaves **8 actionable sites across 3 files** plus 2 documented FP classes. **nullPointerOutOfMemory (5 sites, 1 file):** `runahead.c:829` — `mylist_create` didn't NULL-check `malloc(sizeof(my_list))` before writing five fields on the next five lines (cppcheck cited 831-835 each independently). Fixed mylist_create with a single NULL-check + early-return; `*list_p` is already cleared so callers see NULL. Two callers had to be hardened because the consumer-side derefs don't tolerate a NULL list: `runahead_input_state_set_last:931` now rechecks `input_state_list` after the create (the loop at 937 would NULL-deref `->size`); `runahead_create:1216` adds `|| !runloop_st->runahead_save_state_list` to the existing size-zero bail (the unguarded `runahead_load_state` at line ~1244 derefs `data[0]` without checking the list). **varFuncNullUB (2 sites, 1 file):** `libretro-db/libretrodb.c:221, 251` — bare `NULL` terminator passed to the variadic `rmsgpack_dom_read_into(...)` is UB on platforms where `NULL` is an integer constant (`sizeof(int) != sizeof(void*)`). The reader at `rmsgpack_dom.c:515` does `va_arg(ap, const char *)`, so the terminator's intended type is `(const char *)`; both call sites now use `(const char *)NULL`. **allocaCalled (1 site, 1 file):** `libretro-db/rmsgpack_dom.c:485` — cppcheck flags alloca() as obsolete and recommends a VLA, which CLAUDE.md explicitly forbids (no-VLA C89 rule). Replaced with a fixed-size C89 array `struct rmsgpack_dom_value *stack_buf[MAX_DEPTH]` — same stack lifetime as the prior alloca, fully C89-portable, and `rmsgpack_dom_reader_state` never marks this path `growable` so realloc against the array address never runs. **FP suppressions documented in `.cppcheck-suppress.txt` (S18, S19):** `frontend/drivers/platform_emscripten.c:1058` — `Atomics?.waitAsync?.toString().includes("[native code]")` JavaScript inside `EM_ASM_INT({...}, ...)`; cppcheck parses the JS body as C and reports the optional-chaining `?.` as a syntax error. Sibling of S15 (audioworklet.c). `HAVE_EMSCRIPTEN`-only, out of Linux desktop build. `record/drivers/record_ffmpeg.c:317` — `#if HAVE_CH_LAYOUT` where `HAVE_CH_LAYOUT` is defined via `AV_VERSION_INT(...)` at line 85; cppcheck has no FFmpeg headers so `AV_VERSION_INT` is unresolvable as a function-like macro. File compiles cleanly under gcc/clang with the real ffmpeg headers installed. **Out-of-scope FPs not pursued:** `platform_orbis.c:159` (`TARGET=retroarch_orbis` only); `libretro-common/lists/vector_list.c:56,57` (vendored, scope.txt excludes `libretro-common/`); `libretro-db/c_converter.c:115-120/416-434` and `libretro-db/lua/lua_common.c:40/58/84` — build-time tools (`c_converter` is the playlist-DB-to-C-array generator; `lua_*` are the optional Lua bindings tool), not in default Linux OBJ per `Makefile.common:563-567`. Build: 3 buildable files compile clean under `make -j4`, full retroarch link OK. cppcheck postfix re-run on the three changed files: all 8 prior actionable findings cleared (only style/information remaining). Cluster too narrow (3 unrelated rule families across 3 files in 2 subsystems, with 2 documented FP classes) for an AUDIT-POLICY entry. | 8 |
| 61 | `bd9fc145d8` | clang-tidy Bundle 61 — clang-analyzer null-deref + dead-defensive sweep. The "Pending audit re-runs" item 161 unblocked once `bear` was installed locally + `compile_commands.json` regenerated; ran clang-tidy `clang-analyzer-{core.NullDereference, core.uninitialized.*, core.DivideZero, security.ArrayBound, unix.Malloc, unix.cstring.*}` against the fixes-branch source. 11 raw findings across 6 files. **Real bugs (8 sites, 6 files):** `runahead.c::mylist_create` inner-calloc NULL-check + rollback — Bundle 60 hardened the outer malloc; the inner `calloc(initial_capacity, sizeof(void*))` can still fail independently and would leave `list->data = NULL` with `list->capacity = initial_capacity > 0`, an inconsistent state that NULL-derefs in subsequent `mylist_resize` (lines 759, 781) and free-of-already-freed at the post-realloc walk (line 811). On inner OOM, free the outer struct and clear `*list_p` so callers' Bundle-60 post-create rechecks tolerate it. `runahead.c::mylist_resize` defensive `if (list->capacity < 0) return;` at function entry to close a theoretical signed-overflow path through `list->capacity * 2` that traces via mylist_destroy → mylist_resize(0) → realloc(data, 0). Practical bounds (MAX_RUNAHEAD_FRAMES=12 * MAX_USERS) keep capacity well under INT_MAX, so the guard exists to close the analyzer's path-sensitive trace, not patch a reachable bug. `gfx/video_driver.h::VIDEO_HAS_FOCUS` macro NULL-safe on `(video_st)->current_video` — real bug at `runloop.c:5735` where the immediately-preceding ternary (5732-5734) guards `video_st->current_video` while the macro derefs `current_video->focus` unconditionally on the next line; same macro called at `video_driver.c:3175` without a surrounding guard. Default "focused" matches the prior behaviour when the driver omits a focus probe. `menu/menu_driver.h::MENU_LIST_GET_STACK_SIZE` macro NULL-safe sibling of `MENU_LIST_GET_SELECTION` (audit S2 contract) — returns 0 when list or menu_stack[idx] is NULL, matching callers' `<= 1` / `> 1` "already at root" semantics. Closes menu_driver.c:1325 + an in-bounds call at :4305. `menu/menu_setting.c::setting_get_string_representation_password (:3273)` — `*setting->value.target.string = '\0'` ran after the `if (setting->value.target.string && ...)` guard at :3268; gate the fallback write on the same predicate. `menu/menu_driver.c::menu_st_iterate_info (:7339)` — S2 follow-up. `cbs = selection_buf ? ... : NULL` makes cbs NULL whenever selection_buf is NULL, but the `else (!cbs || ...)` branch then deref'd `selection_buf->list[selection].type`. Bundle 55 missed this shape because the grep keyed on inline `MENU_LIST_GET_SELECTION(...)->size` and on `menu_stack` — this is a `selection_buf->list[i].type` shape derived via the cbs ternary chain. Gate the else on `selection_buf`. `menu/menu_driver.c::retroarch_menu_running_finished (:6642)` — `!settings->bools.menu_pause_libretro` deref'd unconditionally after the siblings at :6616 and :6626 already gated the same field defensively. settings (from config_get_ptr) genuinely can be NULL early in lifecycle (config_st starts at NULL until config_load_state runs). Gate. `menu/menu_setting.c::general_write_handler (:8504)` — added `if (!settings) return` after the existing `if (!setting) return` (function dispatches user-driven setting changes; settings is non-NULL by reachability in practice, but the analyzer flagged the core_info_cache_enable write at ~:9300 as NULL-derefable). `menu/menu_setting.c` populate helpers — three functions (`setting_append_list_input_{player,libretro_device,remap_port}_options`) got function-entry `if (!list || !*list || !list_info) return false;` defensive guards. `SETTINGS_LIST_APPEND` short-circuits on `!*list` but the per-user `(*list)[index-1].xxx` write block then derefs `*list` unconditionally — NULL-deref if `*list` was NULL on entry. The caller (`menu_setting_initialize`) populates `*list` before invoking these, so the guards are defensive but they close the analyzer's path at three sites the practical control flow never hits. **Dead-defensive (2 sites, Bundle-54 class):** `command.c::command_event_reinit (:2529)` — drop dead `video_st &&` from the snapshot-conjunction; `video_state_get_ptr()` returns `&video_driver_st` (static struct address, never NULL). Closes flagged reads at :2584/:2589. `retroarch.c::driver_init_internal (:1590)` — drop dead `if (menu_st)` guard around `MENU_ST_FLAG_DATA_OWN`; `menu_state_get_ptr()` returns `&menu_driver_state` (static struct). Closes flagged read at :1736. **FP suppressions (NOLINTNEXTLINE inline; sibling .cppcheck-suppress.txt entries named in the comment):** `retroarch.c:1177` (find_driver_nonempty) + `:2299` (STRING_LIST_LOCATION_DRIVERS) — NULL-terminated `location_drivers[]` table walks; `security.ArrayBound` doesn't model the NULL sentinel. Same FP class as audit S4 / S11 driver-table FPs in `.cppcheck-suppress.txt` (different tool, hence NOLINT inline rather than a suppress file). Other 8 driver tables in `find_driver_nonempty` follow the same pattern but only the 2-element location_drivers (Linux default: location_null + NULL) tripped the checker. `menu/menu_setting.c:5799` `[(current_idx + types - 1) % types]` divide — collapsed the multi-line statement onto one line so `NOLINTNEXTLINE(clang-analyzer-core.DivideZero)` applies to the `%` token. Bundle 43 verified `types` (libretro_device_get_size's return) is always >= 2; analyzer can't trace into the helper. **Carry-overs / out-of-scope:** the four `(*list)[list_info->index - 1].xxx` patterns in `menu_setting.c` populate helpers are the recurring class — closed for the three flagged populate functions, but ~100+ similar sites exist file-wide and would need either a macro-infrastructure refactor (SETTINGS_LIST_APPEND fail-flag through to the writer block) or per-function defensive-guard sweep. Not pursued in Bundle 61. Build: 7 changed files compile clean; full `make -j4 retroarch` links ok with the binary running. Re-ran clang-tidy on the six files post-fix: 0 clang-analyzer-{core.NullDereference, core.DivideZero, security.ArrayBound, unix.Malloc} findings remaining. Cluster spans 4 unrelated subsystems (top-level state, video macro, menu driver, menu settings) with two structural macro fixes — too narrow for an AUDIT-POLICY entry per the 3+ unrelated-files threshold. | 8 |
| 62 | `9ed75a3d51` | cppcheck post-Bundle-55 sweep on `menu/drivers/materialui.c` — closing the long-pending audit re-run "let materialui.c finish (expect +10-15 items in the S2 pattern)". Re-ran cppcheck with `--max-configs=1 --check-level=exhaustive` so the 12 231-LoC file completes in ~10s instead of timing out at default branch-budget (the audit-config `known_issues` note documents the workaround). The predicted +10-15 S2-pattern (nullPointer*) findings turned out to be ALREADY-FIXED by Bundles 54 + 55 — the only `nullPointer*` raw findings on the audit-branch snapshot at lines 5916/9659/10037/12045 were stale cite-points whose underlying derefs are already guarded on the fixes branch. Only 3 actionable sites on the fixes-branch snapshot, all unrelated to S2. **knownConditionTrueFalse — dead clause (2):** `materialui.c:2778` `if (msg && *msg)` inside `materialui_get_message`, immediately after the early-return at line 2773 already gates `!mui || !msg || !*msg`. The preceding unconditional `mui->msgbox[0] = '\0'` zeroisation was also dead since the strlcpy below runs unconditionally and writes the NUL terminator itself; collapsed both into a single strlcpy call. `materialui.c:10409` `if (new_selection != selection)` after the IS_PLAYLISTS_TAB random-selection while-loop at line 10403 — the loop's exit condition is `new_selection != selection && entry_new.type == PLAYLIST_COLLECTION` so the guard is guaranteed true at the deref. Stripped the dead `if` block plus its `#ifdef HAVE_AUDIOMIXER` sibling at line 10419. Added a one-line invariant comment naming the while-loop as the bound owner. The sibling branch at 10429 (menu_playlist_random_selection) left intact — that path *can* return the same selection so the guard is load-bearing there. **invalidPrintfArgType_sint (1):** `materialui.c:12047` `%d` formatting `unsigned user_value` inside the per-user `input_binds_list` label builder. Same fix shape as Bundle 56 (task_autodetect.c:243) and Bundle 57 (dispserv_kms/video_shader_parse/video_crt_switch/command.c) `%d` → `%u`. Value range 1..MAX_USERS (16) so no wire-format change in practice. **Verified FP / won't-fix:** `materialui.c:5443` (knownConditionTrueFalse — `0 && mui->flags & MUI_FLAG_USE_SMOOTH_TICKER`) is an author-marked commented-out branch introduced by upstream commit `73abaddd9c3` "GLUI: Add save state thumbnails (#17592)". The `0 &&` prefix is the standard "keep-the-code-disabled-for-now" idiom; stripping it would either delete code the upstream author wants to keep or re-enable a branch whose original disable rationale we don't know. Will surface in an upstream cleanup PR if pursued. **Pre-existing tidy noise NOT fixed in this bundle but roadmapped separately under "📋 cppcheck materialui.c style noise" + "📋 clang-tidy materialui.c integer-division" + "📋 clangd unused-includes audit" (see below):** 31× constVariablePointer, 13× shadowVariable, 13× constParameterCallback, 9× constParameterPointer, 3× variableScope (`label_x` :5102, `sys_bar_clock_width` :6496, `color_white` :8158), 1× unreadVariable (`sys_bar_clock_width` :6496 — its init-to-0 is dead since both readers sit inside the same if-guarded block); 5× `bugprone-integer-division` (materialui.c:5924/5943/7339/7340/7351 — `(int_a - int_b) / 2` feeding a float layout context; ≤1 px centring drift, not a crash); 1× `readability-misleading-indentation` (materialui.c:12086 — the `break;` after an unbraced `else stmt;` chain; likely FP since the codebase's CODING-GUIDELINES explicitly mandates no-brace single-statement blocks); 4× clangd `unused-includes` (config.h, frontend_driver.h, core_info.h, input_osk.h). Build: materialui.o compiles clean, full `make -j4 retroarch` links ok. cppcheck cluster cleared on materialui.c post-fix (0 warning/error remaining; only style noise + the one author-disabled branch + 1 unmatchedSuppression info + 1 checkersReport info). Cluster too narrow (1 file, 3 rule families) for an AUDIT-POLICY entry per the 3+ unrelated-files threshold. | 3 |
| 63 | `ff483a6362` | BUILD wire-up — closes the long-pending "📋 BUILD — `network/cloud_sync/s3.c` is half-landed" item (line 256) that had been outstanding since `218200040c`.  Five prior fork-side bundles (17 SigV4 contract, 26 Content-Length, 32 upload cap, 52 atomic write, 56 dead-clause) had touched `s3.c` without any build path defining `HAVE_S3` — none of those fixes had been compile-tested.  **qb wire-up:** `qb/config.params.sh` adds `HAVE_S3=no  # S3 cloud sync backend (experimental, requires networking)` between `HAVE_BUILTINSMBCLIENT` and `HAVE_COCOA`; defaults off, opt in with `./configure --enable-s3`.  `qb/config.libs.sh` adds `check_enabled NETWORKING S3 's3' 'Networking is' false` after the existing `NETPLAYDISCOVERY` line — gates on `HAVE_NETWORKING` and dies if the user passes `--enable-s3` with `--disable-networking`.  **Makefile gate:** inside the existing `HAVE_CLOUDSYNC` block at `Makefile.common:2390`, between the `HAVE_SSL` google_drive block and the SMB block: `ifeq ($(HAVE_S3), 1) DEFINES += -DHAVE_S3; OBJ += network/cloud_sync/s3.o endif`.  `griffin/griffin.c:1734-1736` already had the unity-build include guarded on `HAVE_S3`, so the console-target unity build is covered as a side-effect of the new `#define`.  **Compile-fix surfaced by the first real Linux build:** `s3.c:829` `char credential[256]` was undersized for `snprintf("%s/%s/%s/%s/aws4_request", access_key_id (≤127), date (8), region (NAME_MAX_LENGTH-1 = 255 on desktop), S3_SERVICE ("s3"))` whose worst-case is 409 bytes — `-Wformat-truncation=` flagged "may be truncated writing up to 255 bytes into a region of size between 112 and 254".  Bumped to `credential[512]` with a comment naming the four field widths.  Bug was latent for the full lifetime of s3.c (since `c7bf80fb72 S3 Implementation`); surfaced now because `HAVE_S3` was finally defined.  **Verification:** `./configure --enable-s3` -> `config.mk: HAVE_S3 = 1`, `config.h: #define HAVE_S3 1`.  `make obj-unix/release/network/cloud_sync/s3.o` -> CC clean (no warnings post-bump).  `make -j4 retroarch` -> LD clean, 20.8 MiB binary.  `nm retroarch | grep s3` shows `cloud_sync_s3`, `s3_sync_begin`, `s3_read`, `s3_parse_url`, `action_bind_sublabel_cloud_sync_s3_url` all linked.  `./retroarch --version` prints normally.  `./configure` (no flag) -> `HAVE_S3 = 0`, `/* #undef HAVE_S3 */` — default path untouched (s3.o not built, no `-DHAVE_S3`).  **Out of scope, follow-ups newly enabled:** the option (a) "SigV4 round-trip smoke test against a localhost minio container" is not done — none of the prior fork-side bundles can be runtime-verified without it.  The four Bundle-62 Tier-3 tidy items (style noise / integer-division / misleading-indent / unused-includes) are unaffected; the new `clangd unused-includes` hit on `s3.c:23` (`retroarch.h` not directly used) folds into the existing Tier-3 unused-includes sweep entry at line 260.  Cluster too narrow (1 file + build system) for an AUDIT-POLICY entry. | 1 build + 1 compile-fix |
| 64 | `2137199165` | clang-tidy `bugprone-integer-division` tree-wide sweep — closes the long-pending "📋 TIDY — clang-tidy `bugprone-integer-division` in `menu/drivers/materialui.c` (5 sites)" roadmap item by widening to the full menu+gfx layout subtree as the entry recommended. Total scope: 51 sites across 8 files. **Sites:** `materialui.c` (5: 5924 thumbnail vertical, 5943 portrait thumbnail horizontal, 7339+7340 fullscreen missing-icon, 7351 no-thumbnail caption); `ozone.c` (26: sidebar entry/icon centring 3630/3631/3687/3779/3780/3832, fullscreen-thumbnail no-icon/no-caption 5510/5511/5528/5529, scroll-anchor middle 5601/5607, "no entries" overlay 6085×2/6086, entry-row icon+label centring 6280/6281/6393, message-box slice-margin 7423/7426/7460, header logo 10817/10818, header time+title vertical 10850/10913/10981); `xmb.c` (10: msg-box x-centring 1196, title-margin font math 6340, fullscreen-thumbnail 7705/7706/7724/7725, left thumb-pair 8780, power/clock margin 9047/9107, timedate 9143); `gfx_widgets.c` (2: msg-queue fade 610, msg-queue icon size 1478); `gfx_display.c` (4: osk virtual-keyboard grid 1041×2/1042/1043); `gfx/widgets/gfx_widget_leaderboard_display.c` (1: progress text 380); `gfx/widgets/gfx_widget_screenshot.c` (2: flash duration 94/131); `gfx/widgets/gfx_widget_volume.c` (1: muted caption 224). **Fix shape:** `/ 2` → `/ 2.0f` (also `/ 4`, `/ 6`, `/ 8`, `/ 12`). Integer-literal divisor → float literal so the division happens in float space. Behaviour-preserving in any int-returning context (float result rounds back to the same int for positive operands < 2^24, every pixel-coord value on real hardware), corrects the ½-px drift wherever the result feeds a float coord. **Verification:** `clang-tidy --checks='-*,bugprone-integer-division'` on the 8 files: 0 warnings (was 51 pre-Bundle). `make -j4 retroarch` -> CC clean across all 8 .o files, LD clean. `./retroarch --version` -> normal output (1.22.2). Cluster too narrow for AUDIT-POLICY (8 files, 1 rule). The roadmap line-258 style noise + line-261 unused-includes + line-260 readability FP cluster remain open under separate tidy entries. | 51 |
| 65 | `de24958242` | cppcheck post-Bundle-62 sweep on `menu/drivers/{ozone,xmb,rgui}.c` — the three Linux-buildable menu drivers Bundle 62 deferred when it closed `materialui.c`. Initial scan surfaced 222 raw findings; the four pure-style classes (constVariablePointer, constParameterPointer/Callback, shadowVariable, variableScope) account for 160 of those and are tracked separately under the ROADMAP TIDY entries — this bundle targets only the real-bug + dead-code class, same shape as Bundles 54/55/57. **Real bugs (3 sites):** `xmb.c::xmb_draw_icon` (invalidLifetime + redundantAssignment) — same fix shape as Bundle 57's gfx_thumbnail.c:1177 hoist; `shadow_color[16]` was declared inside the `if (shadows_enable)` block, the assign `coords.color = shadow_color` escaped that scope, and the outer reassign `coords.color = (const float*)color` was preceded by a brief gap during which coords.color pointed into already-out-of-scope memory. Hoisted `shadow_color[16]` to function scope (4th local alongside `draw`/`coords`/`mymat_tmp`). Inline `cppcheck-suppress redundantAssignment` added at the outer reassign since cppcheck cannot trace `draw.coords->color` reads back to `coords.color`. `xmb.c::xmb_draw_bg` (redundantAssignment) — `draw.color = &coord_black[0]` init was dead because both branches of the wallpaper / gradient split unconditionally reassign `draw.color` before any read. `ozone.c::ozone_messagebox` fade-out branch (oppositeInnerCondition) — the outer condition pinned `!(ozone->flags & OZONE_FLAG_MSGBOX_STATE)`, so the inline `if (ozone->flags & OZONE_FLAG_MSGBOX_STATE) ... else ...` that bracketed the `&= ~OZONE_FLAG_MSGBOX_STATE_OLD` clear had a dead if-side and an else-side that did exactly the same work; collapsed to the unconditional clear. **Dead-write / dead-control cleanup (5 sites):** `xmb.c:2584` `break;` after `goto end;` in the same case body (duplicateBreak — unreachable). `ozone.c:5903` `y += node->height;` inside else-if-then-break "all remaining entries below boundary"; `y` is unconditionally reassigned at the next outer block before any further read (redundantAssignment). `ozone.c:3745` same shape inside the tab-below-visible-area break block; `y` is not read after the for loop closes (unreadVariable). `ozone.c:6428` `int left_thumbnail_y_position = 0;` default-init was dead because both branches of the inner `if (show_right_thumbnail)` inside the `if (show_left_thumbnail)` block unconditionally assign it before the first read (unreadVariable). The sibling `right_thumbnail_y_position = 0` init at the line above stays — it is load-bearing for the `!show_right_thumbnail` path that copies it. `rgui.c:2138` `bool on_screen = false;` default-init was dead because rgui_draw_particle assigns on_screen at the top of every loop body before the `if (!on_screen)` check (unreadVariable). **Shadow rename (1 site):** `xmb.c::xmb_pointer_up` (shadowArgument) — inner `entry` declared inside the tab-switch sub-handler shadowed the outer function-parameter `entry` (used by 6 sibling case bodies). Renamed inner to `tab_entry` so the dual purpose is explicit. **Deferred:** 3 autoVariables in `rgui.c:4877/4879/4885` (RGUI ticker pattern; need to verify gfx_animation_ticker* fields aren't cached past the synchronous call before fixing); 3 duplicateExpressionTernary in `xmb.c:5098/5099/8981` (upstream author-marked intent placeholders `? 37 : 37` / `? 1.25f : 1.25f`, same call as Bundle 62's `0 && X`); 5 duplicateCondition (sequential if-blocks separated by per-action comments); 1 unusedStructMember (`ozone_theme::text[16]` — 16-theme initialiser-position cascade is high-risk); 21 knownConditionTrueFalse (case-by-case judgement work, ~50/50 real-vs-FP per Bundle 62 ratio); 160 pure-style findings (the "📋 TIDY — materialui.c style noise" Bundle 62 entry expanded tree-wide). **Verification:** `make -j4 retroarch` compiles the 3 changed .o files clean, full link OK (20.6 MiB binary), `./retroarch --version` boots normally. cppcheck postfix re-run on the 3 files: every Bundle 65 target finding cleared (0 invalidLifetime, 0 oppositeInnerCondition, 0 duplicateBreak, 0 unreadVariable, 0 shadowArgument; the 1 remaining redundantAssignment is the inline-suppressed FP at xmb.c:1053). Cluster too narrow (2 rule families across 3 files in 1 subsystem) for an AUDIT-POLICY entry per the 3+ unrelated-files threshold. | 9 |
| 66 | `c8996e2b58` | cppcheck FP-suppression follow-up to Bundle 65 — closes the 6 deferred sites Bundle 65 documented but couldn't action without verifying the underlying control flow.  **rgui.c autoVariables (3 sites, post-Bundle-66 lines 4870/4876/4884; cppcheck-reported lines pre-edit 4866/4868/4874):** all in `rgui_render_osk`'s input-label if-else.  Caller `rgui_render` declares `ticker` + `ticker_smooth` as function locals and passes them by address; inside `rgui_render_osk` we set `ticker_smooth->dst_str = input_label_buf` (local), `ticker_smooth->x_offset = &ticker_x_offset` (local), and (else branch) `ticker->s = input_label_buf`.  The very next statement in each branch is the synchronous `gfx_animation_ticker_smooth(ticker_smooth)` / `gfx_animation_ticker(ticker)` call, which consumes the fields and returns; neither callee (`gfx/gfx_animation.c:1073` and `:1312`) stores the struct pointer or memcpy's the struct anywhere persistent — they only `utf8cpy(ticker->s, ...)` within the same synchronous call and set `p_anim->flags |= GFX_ANIM_FLAG_TICKER_IS_ACTIVE`.  After rgui_render_osk returns, `input_label_buf` / `ticker_x_offset` go out of scope, but no one reads ticker(_smooth)->* again until the caller overwrites them at the next callsite (rgui.c has 8+ callsites that all assign fresh locals before calling).  Textbook cppcheck FP — the auto-variable addresses are valid for the entire window they're read from.  **xmb.c duplicateExpressionTernary (3 sites, post-Bundle-66 lines 5100/5104/8988; cppcheck-reported lines pre-edit 5096/5097/8979):** all author-marked intent placeholders.  `((xmb->use_ps3_layout) ? 37 : 37) * xmb->scale_mod[0]` and the sibling `? 58 : 58` keep the ps3_layout / non-ps3_layout split visible so the two arms can diverge later without restructuring; `xmb->icon_size * (icon_thumbnails ? 1.25f : 1.25f)` inside the height_offset computation preserves the icon_thumbnails-vs-no-icon-thumbnails split for future divergence.  Same intent-placeholder shape as the `0 && X` placeholder Bundle 62 documented at materialui.c:5443.  **Suppression format follows the Bundle 65 precedent** (`/* cppcheck-suppress <id> ; <explanation> */` on its own line above the target), with one mid-statement wrinkle for the 8988 site: the cppcheck inline-suppress comment must sit on the line immediately above the warning's reported line — and the height_offset assignment is a 2-line continuation, so the suppression comment had to be inserted between the two continuation lines (anchored to the ternary's actual line, not the assignment's start line).  **Verification:** cppcheck postfix (`--enable=style --inline-suppr` on xmb.c + rgui.c) — 0 autoVariables, 0 duplicateExpressionTernary (down from 3 + 3); other style findings in the two files unchanged (143 remaining, all in the per-file tidy backlog tracked under the existing TIDY entries at lines 260/262/263).  `make -j4 obj-unix/release/menu/drivers/{rgui,xmb}.o` clean.  `make -j4 retroarch` -> LD clean, 20.8 MiB binary.  `./retroarch --version` boots normally.  Cluster too narrow for AUDIT-POLICY (2 rule families across 2 files in 1 subsystem, FP suppressions only). | 6 |
| 67 | `b68aa9b59b` | cppcheck/clang-tidy port-forward — closes 5 cppcheck findings (1 error + 4 warnings) + 10 clang-tidy `bugprone-integer-division` warnings still flagged on `menu/drivers/xmb.c` against the `local/audit-2026-04` branch state.  Same shape as Bundle 64+65 — this is the port-forward sibling that brings the audit branch's xmb.c into the same cppcheck/clang-tidy state the fixes branch already has from Bundles 64 (`2137199165`) + 65 (`de24958242`).  Audit/fixes branch drift surfaced: Bundles 60/61/62/64/65 fix commits live ONLY on `local/fixes-2026-04`, while the doc fold-in commits referencing them live on `local/audit-2026-04` — the cppcheck cache `.audit_cache/cppcheck-b66-postfix2.xml` was generated on the audit branch and showed the 5 sites as still flagged because the audit branch is missing the underlying Bundle 65 code.  Bundle 67 applies the xmb.c-only portion to close the audit-branch findings.  **cppcheck closes (5 sites):** xmb.c:1046 `invalidLifetime` (hoist `shadow_color[16]` from inner `if (shadows_enable)` block to function scope, 4th local after draw/coords/mymat_tmp — same fix shape as Bundle 57 gfx_thumbnail.c + Bundle 65 xmb.c; inline `cppcheck-suppress redundantAssignment` at the outer `coords.color = (const float*)color` reassign since cppcheck cannot trace `draw.coords->color` back through the dispctx->draw indirection); xmb.c:1620 `nullPointerRedundantCheck` on `s` (`else if (memcmp(s, "imageviewer", …))` → `else if (s && memcmp(…))`, downstream `else if (s && *s)` branch was already correctly written); xmb.c:1626 `nullPointer` on `selection_buf` (`xmb_node_t *node = (xmb_node_t*)selection_buf->list[selection].userdata` → `selection_buf ? (xmb_node_t*)... : NULL` — MENU_LIST_GET_SELECTION returns NULL when menu_list is NULL per the audit S2 contract); xmb.c:9706 `nullPointerRedundantCheck` on `!xmb` (`unsigned horizontal_list_size = (xmb->show_playlist_tabs) ? ... : 0` read xmb->show_playlist_tabs before the `if (!xmb) return;` guard 4 lines below — split declaration from assignment); xmb.c:9726 `nullPointerRedundantCheck` on `selection_buf` (`xmb_calculate_visible_range(xmb, height, selection_buf->size, ...)` derefs selection_buf without a guard while the preceding line already had `if (selection_buf && selection >= selection_buf->size)` — wrap the visible-range call + 3 follow-up arithmetic lines in `if (selection_buf) { ... }`).  **clang-tidy closes (10 sites):** `bugprone-integer-division` across `xmb_render_messagebox_internal` (line 1196 pre-edit), `xmb_layout_common` (6340), `xmb_draw_no_thumbnail_available` (7705/7706/7724/7725), `xmb_frame` (8780, 9047, 9107, 9143).  Fix shape: `/ 2` → `/ 2.0f`, also `/ 4`, `/ 6`, `/ 8` — same mechanical shape as Bundle 64's tree-wide sweep.  **Deferred / out-of-scope:** the remaining xmb.c drift from fixes-branch (Bundle 42 `entry_index_offset` type change, Bundle 46 dead vtable slots, Bundle 65 `xmb_pointer_up` shadowArgument rename, Bundle 65 `xmb.c:2584` duplicateBreak, Bundle 65 `xmb_draw_bg` coord_black redundant init) are not flagged in the b66-postfix2 cppcheck scan so left for separate audit-branch sync bundles.  The same fixes-branch drift exists for `ozone.c` (12 pre-existing warnings) and `materialui.c` (5 pre-existing warnings) but those belong to per-file follow-up bundles.  **Verification:** `make obj-unix/release/menu/drivers/xmb.o` CC clean.  `make -j4 retroarch` → LD clean, 20.8 MiB binary.  `./retroarch --version` → 1.22.2 boots normally.  cppcheck postfix on `xmb.c` (`--enable=warning,style,performance,portability --max-configs=1 --check-level=exhaustive`): 0 error/warning level findings (was 5).  clang-tidy `bugprone-integer-division` on xmb.c: 0 warnings (was 10).  Cluster too narrow for AUDIT-POLICY (1 file, 2 rule families, port-forward fix shape). | 15 |
| 68 | `2804c81347` | cppcheck/clang-tidy port-forward — sibling of Bundle 67 (`b68aa9b59b`) for the remaining two Linux-buildable menu drivers `ozone.c` + `materialui.c`.  Closes 12 + 5 cppcheck warning-level findings + 26 + 5 clang-tidy `bugprone-integer-division` warnings still flagged on `local/audit-2026-04` (the audit-branch drift Bundle 67's verification step documented as deferred).  Same scope shape as Bundle 67 — only cppcheck warning+ and clang-tidy `bugprone-integer-division` sites; pure-style + vtable-strip + signed-char-misuse drifts left to separate sync bundles.  **ozone.c cppcheck closes (12 sites across 9 hunks):** 3612×2/3722 `nullPointerRedundantCheck` on `ozone->theme` (the inner `if (ozone->theme)` checks at the sidebar text-color init and horizontal_list text-color init are redundant because the surrounding code already derefs `ozone->theme->entries_border` / `ozone->theme->text_selected` unguarded — drop the inner check and merge init into declaration, Bundle 65 shape); 4772/4774 `nullPointer` on `menu_stack` (add `if (!menu_stack) return;` guard above `menu_stack->size`, Bundle 55 S2 shape); 5898/5900 `nullPointerRedundantCheck` on `node` (drop dead `if (node) y += node->height;` after break inside the "below boundary" path — y is unconditionally reassigned at the next outer block before any further read, Bundle 65 dead-code shape); 8010 `nullPointerRedundantCheck` on `s` + 8015 `nullPointer` on `selection_buf` (`!strcmp(s, "imageviewer")` → `s && !strcmp(...)`; `selection_buf->list[selection].userdata` deref wrapped in `selection_buf ? ... : NULL` ternary per the audit S2 contract); 11776 `nullPointer` on `selection_buf` (add `if (!selection_buf) return;` guard, Bundle 55 S2 shape); 11901 `nullPointerRedundantCheck` on `!ozone` (move `messagebox_tag = (uintptr_t)ozone->pending_message;` past the `if (!ozone) return;` guard four lines below — same shape as xmb_list_cache Bundle 67 port-forward); 12256 `oppositeInnerCondition` (Bundle 65: outer condition pins `OZONE_FLAG_MSGBOX_STATE` off, so the inline `if (ozone->flags & OZONE_FLAG_MSGBOX_STATE) ... else ...` that formerly bracketed the `&= ~MSGBOX_STATE_OLD` clear had a dead if-side and identical else-side — collapsed to the unconditional clear).  **ozone.c clang-tidy closes (26 sites):** `bugprone-integer-division` `/ 2` → `/ 2.0f` (also `/ 4`, `/ 12`) across sidebar drawing (3630/3631/3687/3780/3781/3833), fullscreen-thumbnail no-icon/no-caption placement (5511/5512/5529/5530), scroll-anchor math (5602/5608), "no entries" overlay (6087×2/6088), entry-row icon + label centring (6282/6283/6395), message-box slice + line x-centring (7425/7428/7462), header logo placement (10819/10820), header time/timedate/title y-centring (10852/10915/10983) — Bundle 64 mechanical shape, ½-px centring drift gone wherever the result feeds a float coord.  **materialui.c cppcheck closes (5 sites):** 5916 `nullPointerRedundantCheck` on `node` in `materialui_render_selected_entry_aux_savestate_list` (add `if (!node) return;` guard before `node->entry_width` deref; defer `background_x` + `thumbnail_x` init to after the guard; inner `if (node) { Draw thumbnails }` block at function bottom loses its now-redundant guard, Bundle 65 reorder); 9659 `nullPointerRedundantCheck` on `!settings` in `materialui_populate_entries` (defer `remember_selection = settings->uints.menu_remember_selection;` past the `if (!mui || !settings) return;` guard, Bundle 54 deref-after-check); 10037 `nullPointer` on `menu_stack` in `materialui_switch_tabs` (add `if (!menu_stack) return false;` guard above `menu_stack->size`, Bundle 55 S2); 12030 `invalidPrintfArgType_sint` on `snprintf("%d", user_value)` in input-binds-list filter (`%d` → `%u`, user_value is `unsigned`, Bundle 62 shape); 12045 `nullPointerRedundantCheck` on `path` in playlist-manager icons block (move `size_t path_size = strlen(path);` inside the `if (path && *path)` block — path was previously dereffed unguarded, Bundle 65 variableScope).  **materialui.c clang-tidy closes (5 sites):** `bugprone-integer-division` at entry-thumbnail vertical centring (5928), portrait-mode horizontal centring (5940), fullscreen-thumbnail missing-icon placement (7342/7343), fullscreen-thumbnail no-thumbnail caption x-centring (7354).  **Deferred / out-of-scope (Bundle 67-style exclusion):** Bundle 43 glyph_width div-by-zero clamp (not flagged by warning+ scan, separate clang-tidy div-by-zero check); Bundle 46 dead vtable slots in `menu_ctx_ozone` / `menu_ctx_mui` (needs coordinated `menu_driver.h` edits, separate cleanup); Bundle 49 glui invariant comment (style/doc only, not flagged); Bundle 54 ozone msgbox redundant-clause cleanup + materialui `if (new_selection != selection)` dead-cleanup in switch_tabs (not flagged); 160 menu-driver pure-style findings (constVariablePointer, constParameterPointer/Callback, shadowVariable, variableScope) — tracked under existing TIDY entries.  rgui.c port-forward not in this bundle — Bundle 67's verification step recorded "0 actionable warnings" on rgui.c for the audit branch, so no port-forward needed unless future clang-tidy passes find new sites.  **Verification:** `make obj-unix/release/menu/drivers/{ozone,materialui}.o` CC clean.  `make -j4 retroarch` → LD clean, 20 MiB binary.  `./retroarch --version` → 1.22.2 boots normally.  cppcheck postfix on both files (`--enable=warning,style,performance,portability --max-configs=1 --check-level=exhaustive --inline-suppr`): 0 error/warning level findings (was 12 + 5).  clang-tidy `bugprone-integer-division` on both files: 0 warnings (was 26 + 5).  Cluster too narrow for AUDIT-POLICY (2 files, 2 rule families, port-forward fix shape). | 48 |
| 69 | `150ace80fd` | cppcheck post-Bundle-65 sweep on the non-driver `menu/` tree — the `menu/drivers/` cluster was closed in Bundles 64+65+66+67+68; this bundle picks up `menu_displaylist.c`, `menu_setting.c`, `menu_driver.c`, `menu_explore.c`, `menu_contentless_cores.c`, and `menu/cbs/menu_cbs_ok.c` (the six top-level menu UI files cppcheck had never been run against narrowly). 29 warning-level findings across 6 files, three rule families. **invalidPrintfArgType_sint — `%d` → `%u` (15 sites; Bundle 56/57/62 shape):** `menu_displaylist.c` × 10 (1469 option_index unsigned, 5672 devices[i] unsigned[128], 8118 p over `unsigned max_users`, 8658 ×2 video_list[i].width/.height — `video_display_config_t` fields all unsigned, 8665 ×3 .width/.height/.refreshrate, 8670 i over `unsigned i, size`, 12361 i over `unsigned i = 0`); `menu_setting.c` × 4 (3258/5170/7149/7231 — all `*setting->value.target.unsigned_integer`); `menu/cbs/menu_cbs_ok.c` × 1 (4501 — `type` is the unsigned action_ok parameter; sibling %d at 4500 already has explicit `(int)` cast and stays as-is). **S2-sibling MENU_LIST_GET deref-without-NULL-check (7 sites in 3 functions; Bundle 55 fix shape — the macro at `menu/menu_driver.h:72` expands to `((list) ? ((list)->menu_stack[idx]) : NULL)`, so the return value must be NULL-checked before deref):** `menu_displaylist.c:3967-3974` "Remove 'Remove' from Explore lists" block (`menu_stack->list` / `->size` deref hoisted into a new `if (menu_stack)` body inside the existing `if (remove_entry_enabled)` block); `menu_explore.c::explore_action_ok` (910-915, NULL-guard with forward-jump to a new `explore_action_ok_end:` label that picks up the tail filebrowser_clear_type() + return — preserves the single-return shape); `menu_explore.c::explore_cancel` (956-960, early-return `action_cancel_pop_default(...)` on NULL menu_stack — same fallback the function's else-branch uses for non-EXPLORE close types); `menu_explore.c::explore_action_ok_deleteview` (1083-1086, short-circuit to `explore_cancel(...)` on NULL menu_stack before the explore_get_view_path call — also clears the cppcheck second-order finding at the inner-function line 1024 via call-path analysis); `menu_explore.c::menu_displaylist_explore` (1360-1366, early `return (unsigned)list->size;` on NULL menu_stack with local declarations split to honor C89 declaration-at-top-of-block); `menu_driver.c::menu_driver_displaylist_push` (2411-2422, `cbs` default-init to NULL above and hoisted into the existing `if (list && list->size)` block — Bundle 54 deref-before-check shape). **Deref-before-check / dead-control (6 sites; Bundle 58 shape):** `menu_cbs_ok.c::action_ok_push_dropdown_item_disk_index` (7632-7641), `menu_cbs_ok.c::action_ok_disk_cycle_tray_status` (8348-8357), `menu_cbs_ok.c::action_ok_disk_image_append` (8380-8392) — all three read `settings->bools.menu_insert_disk_resume` into a local bool *before* the `if (!settings || !sys_info) return -1;` guard.  Fixed by default-initing the local bool to `false` at declaration and moving the assignment after the guard.  `menu_cbs_ok.c` ACTION_OK_DL_SUBSYSTEM_ADD_LIST path-cleanup block (line ~1410) — `if (delim)` check after a while-loop that init'd delim from a stack-array address; delim is provably non-NULL at the check.  Stripped the dead guard and added a 2-line comment naming the invariant.  `menu_contentless_cores.c::menu_contentless_cores_get_info` (lines 209-214) — *missing return* after `*info = NULL` in the bad-input branch caused the immediately-following deref of `contentless_cores_state->info_entries` to fire on the very inputs the check was meant to reject.  Real null-deref crash on NULL contentless_cores_state / NULL info_entries.  Added the missing braces + `return` after `*info = NULL`.  `menu_driver.c::generic_menu_iterate` ITERATE_TYPE_INFO else-branch (line ~7350) — `selection_buf->list[selection].type` deref inside the `else` of `if (cbs && cbs->enum_idx != MSG_UNKNOWN)`; cbs NULL ⇒ selection_buf NULL (they're co-initialised at 7237-7241), so the else-branch could reach the deref with NULL.  Converted `else` to `else if (selection_buf)` so the deref is only reached when selection_buf is known-non-NULL. **NULL-label deref fix (1 site, distinct shape):** `menu_driver.c::action_iterate_type` (line 3332) — `strcmp(label, ...)` on a `label` that the call site at line 7177 may pass as NULL (the upstream `label = NULL` default at 7164 is only conditionally overwritten by the `if (list && list->size)` block at 7173-7174).  Added an early `if (!label) return ITERATE_TYPE_DEFAULT;` guard at the top of action_iterate_type; ITERATE_TYPE_DEFAULT is the function's natural fall-through return for "no special label match", so the new path is semantically identical to "label matched nothing". **Verification:** `make -j4 obj-unix/release/menu/{menu_displaylist,menu_setting,menu_driver,menu_explore,menu_contentless_cores,cbs/menu_cbs_ok}.o` → CC clean across all 6 .o files.  `make -j4 retroarch` → LD clean, 20.8 MiB binary.  `./retroarch --version` prints normally (1.22.2).  cppcheck `--enable=warning` re-run on the 6 files: 0 warning, 0 error (was 29).  cppcheck `--enable=warning,style`: 396 style findings remain, all in the pure-style backlog tracked under the existing Tier-3 TIDY entries; no new style findings introduced.  Cluster too narrow (3 rule families across 6 files in 1 subsystem) for an AUDIT-POLICY entry — the three families are themselves siblings of existing AUDIT-POLICY entries (printf-spec %d→%u under Bundle 57's class, MENU_LIST_GET under audit S2 sibling, deref-before-check under Bundle 5/54/58's class). | 29 |
| 70 | `3a6278b94f` | test-audit on the 5-file libretro-common libcheck suite (`test/{hash,lists,queues,string,utils}/test_*.c`).  Two parallel test-audit-chunk subagents (1300-line `test_linked_list.c` standalone + 974-line cluster of queues/string/utils/hash) reviewed against all 18 dimensions; 60 raw findings filtered to 1 build break + 6 missing END_TEST + ~35 weak-assertion conversions + 4 misc quality fixes after orchestrator dedup and FP triage. **Build break (CRITICAL):** `test_stdstring.c:141::test_string_replacesubstr` called `string_replace_substring("foobaarhellowooorldtest", "oo", "ooo")` (3 args), but the SUT signature gained explicit `*_len` parameters in commit `ca7e53e3ca` on 2024-12-23 (`string_replace_substring - move out implicit strlen`).  Test has failed to compile for ~17 months; the libcheck suite was structurally orphaned.  Restored by switching the call to the 6-arg form with `strlen()` for each length, matching the SUT's current signature.  **Missing `END_TEST` macros (HIGH — libcheck UB: free-standing function definitions between START_TEST blocks get nested into the previous test's static-storage scope; 3-of-3 chunk-A and orchestrator-syntax-pass surfaced 6 sites total):** `test_linked_list.c` x3 — `test_linked_list_get_first_matching_function_null` (after line 220), `test_linked_list_get_last_matching_function_null` (after 262), `test_linked_list_foreach_valid` (after 1190); `test_generic_queue.c` x3 (orchestrator-only via shim syntax-pass, missed by chunk B) — `test_generic_queue_remove_first` (after 328), `test_generic_queue_remove_middle` (after 344), `test_generic_queue_remove_last` (after 360).  **Assertion-quality sweep (HIGH — libcheck >=0.9.10 macros, already in use elsewhere in these files):** `ck_assert(!strcmp(a,b))` → `ck_assert_str_eq(a,b)` 26 sites across `test_stdstring.c` (test_string_filter/replace/case/tokenizer/replacesubstr/trim/word_wrap/strlcpy/strlcat) + `test_hash.c` (test_sha256/test_sha1); `ck_assert(ptr != NULL)` → `ck_assert_ptr_nonnull(ptr)` 5 sites (test_stdstring tokenizer + replacesubstr; test_utils crc32; test_hash sha1); `ck_assert(ptr == NULL)` → `ck_assert_ptr_null(ptr)` 1 site; `ck_assert(p == (char*)p)` pointer cmp → `ck_assert_ptr_eq` 4 sites (test_string_trim + test_strldup); `ck_assert(tv2[0]=='f' && tv2[1]==0)` compound → split into two `ck_assert_int_eq` calls in test_strldup; `ck_assert(!memcmp(...))` on binary digests → `ck_assert_msg(!memcmp(...), "<label>")` 6 sites (MD5 x3 in test_utils, strlcpy/strlcat byte buffers x4, utf16_conv UTF-8 byte output x1) — `memcmp` has no `ck_assert_mem_eq` equivalent before libcheck 0.11.0; labeled `_msg` form is the portable middle-ground.  **Coverage/quality fixes (MEDIUM):** `test_string_comparison` was exercising raw `memcmp()` — not any `stdstring.h` contract; replaced with `string_is_equal` / `_case_insensitive` / `string_is_empty` / `string_starts_with[_size]` / `_case_insensitive` / `string_ends_with[_size]` test data (the actual SUT exports).  `test_string_num_conv` tested both `string_to_unsigned` and `string_hex_to_unsigned` in one body so a first-half failure hid second-half coverage; split into `test_string_to_unsigned` + `test_string_hex_to_unsigned` with both registered in `create_suite()`.  `test_utf8_conv_utf32` indexed `output[0..7]` without guarding the returned count (count mismatch ⇒ reads uninitialised slots before any assertion fired); added an early-return guard.  `test_utf8_util` declared `char out[64]` but never used it — pre-existing `-Wunused -Werror` trip in the Makefile.test build, dropped the dead local + rewrote the `&test1[idx] == utf8skip(...)` pointer comparison via `ck_assert_ptr_eq` with `void*` casts.  `test_utils.c` had `#define SUITE_NAME "hash"` colliding with `test_hash.c`'s suite name, ambiguous libcheck output → corrected to `"utils"`.  `_free_value()` in `test_generic_queue.c` was an unannotated no-op named like a free callback → added a 2-line intent comment + `(void)value;` cast for `-Wunused-parameter`.  **Temp-file hygiene (MEDIUM — partial CWE-377 mitigation):** `test_crc32_file` (test_utils.c) and `test_sha1` (test_hash.c) both use `tmpnam()` + `fopen()` to make a CWD-relative temp file and never `unlink`'d it; repeated runs accreted stale files in `libretro-common/`.  Added `remove(tmpfile)` cleanup after each test body.  Full `mkstemp()` (POSIX) + `GetTempFileName()` (Windows) migration deferred to a roadmap follow-up — `tmpnam` is the only portable single-call form in C89 and the test suite still has to run cross-platform.  **False positives logged (no code change):** `test_linked_list.c:1004-1066` `ck_assert_int_eq(linked_list_set_at(...) == true/false, 0)` flagged as "tautology" by chunk A — actually mathematically equivalent to `ck_assert(returned != expected)`, awkward style but correct; left as-is to avoid drive-by reformat per CLAUDE.md rule 11.  Chunk A's "tests not registered in create_suite()" claim against `test_linked_list_get_{first,last}_matching_function_null` and `test_linked_list_foreach_valid` — they ARE registered at lines 1209/1210/1285 of the post-fix file; chunk A misread the suite registration block.  **Verification:** `gcc -Iinclude -I/tmp/check_shim -Wall -Werror -Wdeclaration-after-statement -fsyntax-only` on all 5 test files (+ SUT dependencies per Makefile.test): clean across the suite.  Pre-fix, the same command on `test_stdstring.c` failed with `string_replace_substring` arg-count mismatch and on `test_generic_queue.c` with `invalid storage class for function` errors at the post-line-330/346/362 START_TEST blocks (the missing END_TEST sites).  libcheck is not installed on this host so the end-to-end `make -f Makefile.test` is not exercised here; the shim mirrors libcheck's documented `START_TEST` / `END_TEST` macro shape (`static void name(int)` wrapper) so the test-body syntax + macro-pair balance is verified.  **Deferred to roadmap (separate section below):** ~12 coverage gaps (NULL-arg variants for `linked_list_add`/`_iterator_next`/`_value`/`remove_*_matching`; `SHA1Digest` direct API not tested — only `sha1_calculate` is; `file_crc32` boundary at `CRC32_BUFFER_SIZE` not exercised), refactor candidates (`tcase_add_checked_fixture` for the 3-element queue setup; loop-table `tcase_add_loop_test` for the `remove_*` family ~35 near-identical bodies), `tmpnam` → `mkstemp` migration, and one SUT-side observation (`linked_list.c::remove_*_matching` lack the NULL-callback guards `get_*_matching` have at lines 159/175 — a real defensive-gap, not just a coverage gap).  Cluster too narrow (1 subsystem: libretro-common tests) for an AUDIT-POLICY entry, but the build-break shape (test calls SUT helper with a stale arg count, suite has not been built in ~17 months) is worth one: see the new "TEST-AUDIT — orphan-test detection" entry below. | 38 |
| 71 | `0da519a267` | docs-only fold-in of 9 cold-eyes 2026-05-18 deferred items — 7 source-doc fixes (master `README.md`, `CONTRIBUTING.md`, three platform READMEs) + 1 audit-infra doc fix (`docs/private/audit/README.md`) + 1 ROADMAP fix (bundle-table monotonicity) — audit branch only, no `local/fixes-2026-04` commit.  **Cold-eyes README cluster:** master `README.md` — added "Platform-specific build notes" subsection linking the three repo-root platform READMEs (`README-exynos.md` / `README-mali_fbdev_r4p0.md` / `README-OMAP.md`) with the "historical reference" caveat; split the Platforms list into "Actively built" (Linux / Win7-11 / macOS / Apple iOS / tvOS / Switch / 3DS / Wii / Wii U / GameCube / PS2-4 / PSP / Vita / Android / DOS / FreeBSD / Haiku / etc.) vs "Historically ported" (Blackberry / ReactOS / Redox OS / RetroFW / SerenityOS / Win NT 3.5 / Win 95-98-Me / Win 2000-XP-Vista / OG Xbox / Xbox 360 / Nintendo Classic Edition / PPC + x86-32 macOS) so abandonware targets don't shadow current-CI ones; replaced vague "OSX requires latest Xcode" with explicit Xcode-12 floor (matches `-mmacosx-version-min`) + docs.libretro.com cross-link.  `CONTRIBUTING.md` — folded the C89/no-VLA/Allman/`-Wsign-compare`/`MISSING_DECLS=1`/struct-by-alignment/128KB-stack/`HAVE_*`-gating sub-rules into the existing "non-obvious things to be aware of" list (fork-only `CLAUDE.md` was the canonical home; cold-eyes "lighter touch is a cross-link, fold-in is the right home" note settled on fold-in since `CLAUDE.md` is fork-only — upstream `CONTRIBUTING.md` is the natural canonical home).  `README-exynos.md` — stripped dead `github.com/tobiasjakobi/{linux-odroid,libdrm}` URLs (404 since ~2022); kept the repo names as inline-code text with explicit "mainline kernel + libdrm carry Exynos DRM API today" + Wayback Machine breadcrumb; reworded the libdrm-from-source section to point at the mainline `--enable-exynos-experimental-api` (or meson `-Dlibdrm-exynos=true`) options.  `README-mali_fbdev_r4p0.md` — rewrote as proper markdown (real `#` H1, fenced ```sh``` code blocks for the cross-compile incantations, blockquote-decorated banner, sub-headings for Sources / Kernel build / Userland blobs / Configuring); kept the same dead-URL caveat for `forum.odroid.com` thread / `builder.mdrjr.net/tools/r4p0-mp400-fbdev.tar` + ARM Mali developer portal redirect.  `README-OMAP.md` — cross-checked `gfx/drivers/omap_gfx.c` (9 `settings->` reads total); documented the seven standard OSD-font settings the driver honors (`video_font_enable`, `path_font`, `video_font_size`, `video_msg_color_{r,g,b}`, `video_msg_pos_{x,y}`) alongside the two OMAP-specific knobs (`video_monitor_index`, `video_vsync`); added explicit "anything else in menu Video section is not honored" boundary statement with grep hint.  `docs/private/audit/README.md` — extended "Adding a new big-file boundary" walk-through with a paragraph naming `wc -l` as the practical mechanism for re-verifying existing `indie-review-partition.md` entries against drift (per cold-eyes "verify against current banner comments before each run" → `wc -l` was the missing mechanism cite).  **Bundle-table monotonicity (cold-eyes 2026-05-18 follow-up):** resorted bundle-progress rows 59-70 ascending (they had landed in commit-order, not bundle-number order — 60 → 59 → 61 → 64 → 63 → 62 → 69 → 68 → 67 → 66 → 70 → 65 — making "newest" hard to locate per cold-eyes finding); table now reads strictly ascending 1, 2, 3, …, 71.  **Verification:** all 6 doc files re-rendered locally, markdown structure intact, no broken anchor references, link syntax preserved; bundle-progress table left column reads strictly ascending after sort; docs-only — no source-code change, no `local/fixes-2026-04` commit, no build/test verification needed. | 9 |
| 72 | _docs-only_ | docs-only fold-in of 5 cold-eyes 2026-05-18 TLS-spec cluster items into `docs/private/specs/2026-04-27-tls-verification-opt-in-design.md` — audit branch only, no `local/fixes-2026-04` commit, no build verification needed.  **BearSSL backend coverage (MEDIUM):** added a parallel `### libretro-common/net/net_socket_ssl_bear.c (vendored — BearSSL backend)` subsection with the discovered REQUIRED-by-accident behaviour (`br_ssl_client_init_full` at line 245 + system CA bundle at line 227 ⇒ bad cert ⇒ `BR_SSL_CLOSED` ⇒ `ssl_socket_connect` returns -1 today); OPTIONAL/DISABLED implemented via `br_ssl_client_init` + `br_x509_minimal_init` + permissive end_chain vtable wrapper (~30 lines, no new file); `ssl_socket_set_verify_mode` parallel mirrors the mbed atomic + snapshot pattern.  Added a four-row build matrix table covering all `HAVE_BUILTINMBEDTLS` × `HAVE_BUILTINBEARSSL` combinations including the "no TLS at all" gate.  **Vendoring-clean log surface (MEDIUM):** replaced inline `RARCH_ERR`/`RARCH_WARN` from inside the vendored mbed file (which has zero `RARCH_*` references and no `#include "verbosity.h"`) with a weak-hook contract — default `__attribute__((weak)) ssl_socket_log_verify_fail(mode_required, domain, verify_info)` no-op in libretro-common, RA-side strong override at `network/tls_log.c` (new) calls the real `RARCH_ERR/WARN`.  Pinned three log-line regex contracts (`^\[TLS\] Cert verification failed for [^:]+: .*$`, `^\[TLS\] Cert verification soft-failed for [^:]+: .*$`, `^\[TLS\] Certificate verification disabled — connections vulnerable to MITM\.`) so the libcheck conformance tests can pattern-match.  Documented the MSVC / console-SDK `#pragma weak` fallback via the existing `libretro-common/include/retro_inline.h` compat-macro pattern.  **Failure-mode enumeration (MEDIUM — dim 10):** new `## Failure modes` section with an 8-row table — expired / self-signed / hostname-mismatch / chain-broken (the obvious four) plus revocation-unavailable (CRL/OCSP — v1 ignores; vendored mbedtls is built without `MBEDTLS_X509_CRL_PARSE_C`), time-skew (consoles without RTC — indistinguishable from genuine expiry in v1; release-note guidance routes user to check device clock first), captive-portal redirect (hostname-mismatch in practice; cloud-sync UI must hint at hotel/café portals), enterprise MITM proxy refusing on REQUIRED (Zscaler/Bluecoat — *Optional* mode is the documented workaround until v2 spec adds a custom CA-bundle UI).  Each row names the expected log line regex + user-visible behaviour per verify mode.  Plus two `### Operational note` subsections — REQUIRED failure UX (cloud-sync error message must mention *Settings → Network → Advanced → TLS Verification → Optional* path) and DISABLED toast (5-second linger, dismiss tap-target, once-per-session no re-appear).  **Thread-safety race (LOW):** new `### Thread safety` subsection — `static int ssl_authmode` is now `_Atomic unsigned` under `defined(HAVE_THREADS) && !defined(__STDC_NO_ATOMICS__)` (C11 `<stdatomic.h>`), with a `volatile unsigned` + snapshot-at-entry fallback on C89-only console SDKs.  All in-function reads go through `ssl_authmode_snapshot()` so the in-flight handshake sees one consistent value even on the fallback path; mid-session toggles take effect on the next connection (documented in D3 consent-dialog UX).  Same atomic+snapshot shape used in the BearSSL parallel so both backends are uniform.  **Performance budget + automated test wiring (LOW):** new `## Performance budget` section with five rows — handshake p99 ≤ 2.0 s desktop / ≤ 5.0 s console-class (REQUIRED, good cert; 2.5× console-budget margin covers PSP @ 333 MHz + 3DS @ 268 MHz + software-only modexp), handshake p99 ≤ 1.5 s / 3.5 s (REQUIRED, bad cert — fail-closed earlier), `ssl_socket_set_verify_mode` p99 < 100 ns (single atomic store), `ssl_authmode_snapshot` p99 < 50 ns (single atomic load × 2 per handshake — negligible vs ~50 ms mbedtls handshake), 0 bytes heap inflation per handshake (stack `vrfy_buf[512]` + module-scope BSS `ssl_authmode` + const-ref weak-hook log).  Empirical anchor: Bundle 41 cheevos/HTTP test fixture clean login ~800 ms p99 desktop; REQUIRED adds one chain-validation walk (~5 ms desktop / ~50 ms console) ⇒ ~2× budget margin.  CI assertion via libcheck `EXPECT_BENCH_LE` (style — `ck_assert_int_le` macro) using the existing `libretro-common/test/include/test_bench.h` helpers from the stdstring suite.  Expanded `### Automated` test wiring under `## Test plan` — three new test files (`test_socket_ssl_mbed.c` / `test_socket_ssl_bear.c` / `test_socket_ssl_log.c`) wired into a new `HAVE_NET_TEST` block in `libretro-common/Makefile.test` (matching the existing `HAVE_STDSTRING_TEST` / `HAVE_QUEUES_TEST` blocks); per-backend tests exercise all four mode/cert combinations + `ssl_socket_set_verify_mode` race (one thread toggling, one thread connecting, ASan/TSan clean); weak-hook contract test links the vendored file standalone (no RA override) and verifies the default is a true no-op (the libretro-core standalone build contract).  In-process minimal TLS server (200-line wrapper around vendored mbedtls' `ssl_server.c` example) bound to `127.0.0.1:0` — no network egress, CI-friendly.  **Spec consequence (also updated):** implementation effort estimate revised from 2–3 days to ~2 days (one extra phase for BearSSL parallel but the BearSSL test mirror is near-zero incremental cost); Phase list bumped from 4 phases to 5 (Phase 3 BearSSL fix added between mbed fix and rollout signals); all references to `libretro-common/include/mbedtls/ssl.h` (the wrong path) corrected to `deps/mbedtls/mbedtls/ssl.h`; the dangling "or new network/tls_config.h" header-placement option resolved to "new RA-side header at `network/tls_config.h`"; External references gained the BearSSL X.509-minimal validator API entry pinned to the vendored `deps/bearssl-0.6/inc/bearssl_x509.h` + `bearssl_ssl.h` paths.  **Banner update:** the spec's cold-eyes 2026-05-18 status update banner (lines 9-24) flipped from "REQUIRES REFRESH — apply before any implementation bundle opens" + 8 numbered open issues to "Bundle 72 fold-in" + 5 ✅ folded + 3 ✅ previously-applied (items 1/4/5 were Bundle 71 one-line text fixes); the 4 deferred low/info nits (dead get_verify_result block on line 162, unsourced 99.5%/0.5% figures in §Goals, made-up `settings/tls/verification` URL in line-168 error message, undefined CHANGES.md shape) are listed at the end of the banner for next docs pass.  **Verification:** spec re-rendered locally, markdown structure intact (32 `^##` and `^###` heading lines after edit — verified ascending document order: Summary → Current behaviour → Goals → Non-goals → Architecture → **Failure modes (new)** → Decision points (D1-D4) → Data model changes (configuration.h → ssl_mbed → **Vendoring-clean log surface (new)** → **Thread safety (new)** → **ssl_bear (new)** → cloud_sync+cheevos+task_http → menu_setting → Startup wire-up) → Test plan (Manual conformance → **Automated (expanded)** → Build verification) → **Performance budget (new)** → Risks → External references → Implementation phases → Spec status), 4 cited file paths verified against current source (`qb/config.libs.sh:481/485/493/497`, `libretro-common/net/net_socket_ssl_bear.c:227/245`, `libretro-common/net/net_socket_ssl_mbed.c:199` + grep-confirmed-0 for `RARCH_` + `#include "verbosity.h"`, `deps/mbedtls/mbedtls/ssl.h` + `deps/bearssl-0.6/inc/bearssl_x509.h` exists), 5 cold-eyes bullets in `docs/private/ROADMAP.md` lines 438-442 flipped 📋 → ✅ via single `sed -i '438,442 s/^- 📋 /- ✅ /' docs/private/ROADMAP.md` (per Bundle 71 feedback re. batch flip ergonomics).  Docs-only — no source-code change, no `local/fixes-2026-04` commit, no build/test verification needed. | 5 |
| 73 | _docs-only_ | docs-only fold-in of the 5 cold-eyes 2026-05-18 cloud-sync-spec cluster items into `docs/private/specs/2026-04-27-cloud-sync-streaming-upload-design.md` — audit branch only, no `local/fixes-2026-04` commit, no build verification needed.  **Phase-1-shipped rewrite (MEDIUM):** marked Goal 1, the Data-model cap blocks, and Phase 1 as ✅ SHIPPED (Bundle 32 `c3ebdb11e4`, dispatch layer `cloud_sync_update` in `network/cloud_sync_driver.c:129-164` — supersedes the per-backend cap snippets in s3.c/webdav.c/google_drive.c, which are now flagged SUPERSEDED-but-kept-for-history); re-anchored every `s3.c` cite to function name (`s3_update` / `s3_sha256_hash` / `s3_multipart_*`) with a "cite stability" callout (the file drifts ~130 lines/audit-cycle — old 1807/1838/1860 cites are now 1941/1972/1994; key by symbol, not line); corrected the streaming SHA-256 helper from the `_ret`-suffixed API (not present in this tree) to the vendored **unsuffixed legacy** API `mbedtls_sha256_starts/_update/_finish` (`deps/mbedtls/mbedtls/sha256.h:83/92/101`); reframed the new helper as an **incremental sibling** `s3_sha256_hash_file` that extends the existing one-shot `s3_sha256_hash` (`s3.c:386`) and shares its hex tail, rather than a parallel `s3_compute_payload_sha256_streaming` (rewrote the helper code block C89-clean — decls at block top, `for` index declared separately, `int64_t` for the filestream_read return).  **Multipart Phase-4 sizing bump (MEDIUM):** confirmed `s3_multipart_state_t::file_data` holds the **whole file** for the upload duration (each part hashes/streams via `mp_st->file_data + offset`, e.g. `s3_sha256_hash(mp_st->file_data + offset, content_len)` at the part path); promoted Phase 4 from a 0.5-day "audit + maybe fix" to a definite **~1-day refactor** with known shape (mp state → `RFILE *` + per-part offset, seek per part); updated Goal 3, the Summary, Risks ("multipart refactor is in-scope, not a surprise" naming the `s3_multipart_upload_next_part` / `s3_multipart_fallback_single_put` call sites that read `file_data + offset`), Test-5 (now the Phase-4 acceptance gate, fails-by-design pre-Phase-4), the Effort-estimate header (Phases 2–4 → ~2 days), and the closing phase paragraph.  **Failure modes + numeric SLOs (LOW):** new `## Failure modes` section — 5-row table (network drop mid-upload → abort+fail-state, no partial commit; S3 5xx → exp-backoff budget ≤4 retries / base 500 ms / ×2 / cap 8 s / total ≤~15 s; Google-Drive OAuth-token expiry mid-upload → refresh+retry-once, deferred v1.1; HTTP 507 / quotaExceeded → fail-closed no-retry with a distinct sync-state; stale-remote conflict → defer to the existing `cloud_sync_destructive` setting + keep-local/keep-server resolve commands already in `cloud_sync_driver`) each with expected log line + recovery; plus a `### Numeric SLOs` block (max heap inflation 256 KB desktop / 128 KB console-class; 5xx retry/backoff wall-clock bound; two-pass disk-read cost noted, no latency SLO pinned for v1).  **TLS cross-reference (LOW):** new `## Cross-spec dependency` note — TLS verification (sibling spec) MUST be `required` or `optional` before this work hardens any privacy claim; streaming changes the data-at-risk *shape* (incremental vs one-shot across many `mbedtls_ssl_write` calls) but **not** the auth surface (same secrets, same socket).  **UI placement reconciliation (LOW):** ground-truth check against `menu/menu_setting.c` + `menu/menu_displaylist.c` — the shipped cap lives on the dedicated **Cloud Sync** page (`SETTINGS_LIST_CLOUD_SYNC` / `MENU_ENUM_LABEL_CLOUD_SYNC_MAX_UPLOAD_MB`, `menu_setting.c:12239`), reached via **Settings → Saving → Cloud Sync** (`DISPLAYLIST_SAVING_SETTINGS_LIST` parents `MENU_ENUM_LABEL_CLOUD_SYNC_SETTINGS` at `menu_displaylist.c:11144`) — not "Cloud Sync → Advanced" (no such subpage) and not under Network; corrected the `menu_setting.c` data-model subsection to the real path and added a domain-scoped reconciliation rationale in `## Cross-spec dependency` (cloud-sync cap is cloud-sync-specific → Cloud Sync page; TLS verify-mode is network-wide → Network → Advanced; neither moves — a setting goes on the page that owns its domain).  **Bonus (banner item 8, not a roadmap item):** folded the third D3 option (`x-amz-content-sha256: UNSIGNED-PAYLOAD` single-PUT + `Content-MD5` integrity — one-pass, no SigV4-chunk machinery; tagged the strongest v1.1 candidate) into the D3 decision point + External references so all 9 banner items close cleanly.  **Banner + status update:** the spec's cold-eyes 2026-05-18 banner flipped from "corrections MUST be applied before any further implementation work" + 9 numbered open issues to "Bundle 73 fold-in" + 9 ✅; the `**Status:**` line and `## Spec status` section updated (D1 2 GB / D4 one-cap / Phase-split resolved by shipped Phase 1; D2 scratch-size + D3 hash-strategy still open for Phases 2–4).  **Verification:** spec re-rendered, markdown intact, no stale `_ret` / old-line-cite / "Cloud Sync → Advanced" references remain (grep-swept); 4 source claims verified against current source (`deps/mbedtls/mbedtls/sha256.h:83/92/101` unsuffixed API; `network/cloud_sync/s3.c:386` `s3_sha256_hash` + `:1941`/`:1972`/`:1994` `s3_update`/malloc/multipart-branch + `s3_multipart_state_t::file_data` whole-file pattern; `network/cloud_sync_driver.c:129-164` dispatch-layer cap; `menu_setting.c:12239` + `menu_displaylist.c:11144` Saving-parented Cloud Sync page); 5 cold-eyes bullets in `docs/private/ROADMAP.md` flipped 📋 → ✅.  Docs-only — no source-code change, no `local/fixes-2026-04` commit, no build/test verification needed. | 5 |
| 74 | _docs-only_ | docs-only fold-in of the 5 cold-eyes 2026-05-18 menu-driver-swap-spec cluster items (8 banner sub-issues) into `docs/private/specs/2026-04-27-menu-driver-swap-design.md` — audit branch only, no `local/fixes-2026-04` commit, no build verification needed.  **API rename + D2 deletion (MEDIUM):** corrected `menu_settings_list_{new,free}` (do not exist) to the real `menu_setting_new(void)` (def `menu/menu_setting.c:25955`, decl `menu/menu_setting.h:102`) + `menu_setting_free(rarch_setting_t *)` (def `:25743`, decl `:104`) throughout architecture box + data model; **deleted D2's A-vs-B framing** — the free function already exists and is already called outside shutdown by `menu_entries_settings_deinit` (`menu/menu_driver.c:2283`, also `:3665`/`:6770`), so the original Phase-1 "lift" (0.5 day) is removed and total drops to ~1.5 days; **re-anchored the gating-site table** from drift-prone line numbers to a grep recipe (`string_is_equal(...menu_driver..., "<drv>")` × 21 + `memcmp(..., "glui", 5)` × 4 = **25 sites**, was undercounted as "20+"); the three worked examples now key on setting label not line.  **D1 precedent re-evaluation (MEDIUM):** the spec claimed a `RUNLOOP_FLAG_*_DRIVER_RECONFIGURE` deferred-flag precedent — verified **zero `RECONFIGURE` hits** in `runloop.h`/`runloop.c`/`retroarch.c`; the real precedent is the synchronous `video_driver_reinit` (`gfx/video_driver.c:4827`) inside `command_event_reinit` (`command.c:2484`) dispatched via `command_event(CMD_EVENT_REINIT, …)`.  Re-framed D1(B) + the data-model entry around a new `CMD_EVENT_MENU_REINIT` (mirror of the real reinit idiom, runs at a safe point outside the draw call) instead of the fictional runloop flag; regression note updated accordingly.  **Failure-fallback ordering (LOW):** rewrote the architecture box + D3 to **build-new-first** — `menu_setting_new()` builds the new tree (reads only the `menu_driver` string, safe while old driver live) before any teardown, so the likeliest failure (tree alloc) needs no fallback; the single-slot driver swap's fallback is **bounded non-recursive** (prev → `rgui` → give up), fixing the original deinit-old-first ordering whose "re-run `menu_driver_init(old)`" could recurse unboundedly; old tree freed only on success (step 4).  **Edge cases (LOW):** new `## Edge cases` section — swap during a context-menu popup (clear popup/overlay flags before the stack reset so no dangling pointer into the freed old tree), swap during a task-progress overlay (task-owned, no setting-pointer cache; one-frame suppression if it draws against the old surface), swap with content loaded vs unloaded (identical path; reset must pick Quick-Menu root vs main-menu root via the same selector `menu_driver_init` uses).  **`glui` aliasing (LOW):** one-line note at first mention that `glui` is the legacy ident string for the `materialui` driver (registered `menu/drivers/materialui.c:12232`).  **Banner + status update:** spec banner flipped from "REQUIRES REFRESH — corrections MUST be applied" + 8 numbered open issues to "Bundle 74 fold-in" + 8 ✅; Status/effort lines updated (~1.5 days, Phase-1 lift dropped).  **Verification:** spec re-rendered, no stale `menu_settings_list_*` / `RUNLOOP_FLAG_*_RECONFIGURE` / "20+" / old-line-cite references remain outside the banner's own descriptions (grep-swept); all 8 source claims verified against current source (`menu_setting_{new,free}` decl/def lines; zero `menu_settings_list_*`; `menu_setting_free` call at `menu_driver.c:2283`; 21+4=25 gating sites; zero `RECONFIGURE`; `video_driver_reinit` `:4827` + `command.c:2555`/`command_event_reinit` `:2484`; `glui` at `materialui.c:12232`); 5 cold-eyes bullets in `docs/private/ROADMAP.md` flipped 📋 → ✅.  Docs-only — no source-code change, no `local/fixes-2026-04` commit, no build/test verification needed. | 5 |
| 75 | _docs-only_ | docs-only fold-in of the 2 cold-eyes 2026-05-18 audit-spec-needed-cluster items into `docs/private/specs/2026-04-27-audit-spec-needed-cluster.md` — audit branch only, no `local/fixes-2026-04` commit, no build verification needed.  **Per-S closure record (LOW):** added a top-level `## Closure summary` table (the alternative the cold-eyes item offered, cleaner than per-S footers) mapping each of S1–S12 to its **chosen option + bundle + commit**, sourced from the ROADMAP "Spec-needed" closure entries — S1 `d6bbeeab55`/B34, S2 `335bb31cf9`+`382ee9f83f`+`3537f96a38`/B34+55, S3 `db21ebd07e`/B34, S4 `8c3d825be6`/B35 (option 2 per-line suppression, **not** the body's "ask upstream"), S5 `233568bfe0`/B34, S6 `630c7294fd`/B31, S7 `e67532e244`/B33, S8 `630c7294fd`/B31, S9 policy (AUDIT-POLICY.md + aggregate.py KNOWN_FP_RULES)/B34, S10 `d6bbeeab55`/B34, S11 `9ddc80cb84`/B34, S12 `b27a516cf5`/B34 — so a future maintainer reads the outcome without cross-walking the ROADMAP, resolving the "S-section bodies read in the recommending tense" concern.  **Follow-on-spec path fix (LOW):** replaced the three `<date>-*.md` placeholders in the `## Follow-on spec docs` section with the real `2026-04-27-*.md` filenames (including the two renamed since drafting — `tls-verification-opt-in-design.md`, `cloud-sync-streaming-upload-design.md`), dropped the "(proposed)" framing since all three sibling specs now exist, and refreshed each one-liner to current state (menu-driver-swap 25 gating sites; TLS title corrected Bundle 72; cloud-sync Phase-1 cap shipped Bundle 32).  **Banner + status update:** banner items 1 + 5 flipped to ✅ (Bundle 75); items 2/3/4/6 explicitly retained as reading-notes (the per-S decision-rationale body is deliberately preserved); Status line points at the new closure table.  **Verification:** spec re-rendered, no stale `<date>` / `tls-opt-in-design` / `cloud-sync-streaming-design` / "(proposed)" references remain (grep-swept); all 12 commit hashes + chosen options sourced from the ROADMAP "Spec-needed" `roadmap_query` closure entries; 2 cold-eyes bullets in `docs/private/ROADMAP.md` flipped 📋 → ✅.  This closes the cold-eyes-2026-05-18 SPEC backlog — only the LOW DOC "ROADMAP.md table cell widths too wide" item remains (a larger table-restructure deferred by its own cold-eyes note).  Docs-only — no source-code change, no `local/fixes-2026-04` commit, no build/test verification needed. | 2 |
| 76 | `00e6d85d4e` | test-audit Tier-1 + Tier-2 coverage-gap fold-in for the libretro-common libcheck suite (`local/fixes-2026-04`).  **Prerequisite finding — the suite had never run to completion in this fork** (libcheck was not installed locally), so Bundle 70's `-fsyntax-only` verification missed every link-time / runtime / `-Werror` break.  Installed `check-devel` + `lcov`, ran `make -f Makefile.test`, and repaired four latent breaks so the suite now builds + runs green end-to-end (stdstring 17, utils 4, hash 4, linked_list 96, generic_queue 16; `make` exit 0): (1) `Makefile.test` now compiles `compat/compat_strldup.c` — Bundle 70 added `test_strldup` but never wired the source → undefined-reference link error; (2) `Makefile.test` now compiles `file/file_path_io.c` for the utils + hash tests — `file_stream.c` references `path_is_directory`/`path_mkdir` defined there (latent link break); (3) added `-Wno-error=discarded-qualifiers` — GCC 15 + newer glibc made `strchr`/`strrchr` const-generic, breaking vendored `file_path.c:find_last_slash` which returns `char*` by design (mainline build doesn't enable this `-Werror` class); (4) dropped two **upstream** NULL-arg calls in `test_stdstring.c` (`string_remove_all_chars`/`string_replace_all_chars` with NULL) — both SUT docstrings declare the input "must be non-NULL, otherwise UB" and `*src` on NULL is a real segfault (ASan SEGV), so the contract-respecting fix is to drop the UB calls, not add a leaf-function guard.  **Tier-1 (SUT fix + regression tests):** `lists/linked_list.c` `remove_{first,last,all}_matching` gained the `!matches` NULL-guard their `get_*_matching` siblings already had — `remove_*_matching(non_empty, NULL)` previously derefed NULL inside the iteration callback; added `remove_{first,last,all}_matching_function_null` tests on a **non-empty** list (the empty-list path never reaches the callback, so only a non-empty list can catch the bug — verified load-bearing by reverting the guard → ASan SEGV).  **Tier-2 (test-only, no SUT change):** linked_list `add_null` / `iterator_next_null` / `iterator_value_null` / `foreach_args` (asserts index 0..N-1 + value identity); generic_queue `_empty` extended with `length(NULL)==0` / `remove(missing)==NULL` / `iterator(empty)==NULL`; hash `test_sha1_digest` covers the in-memory `SHA1Digest` path (distinct from file-based `sha1_calculate`, diverges on Apple `CC_SHA1`); stdstring char-classify now exercises `ISUALPHA`/`ISUALNUM`/`IS_XDIGIT` (incl. `_` + non-hex `g`/`G`) + `TOUPPER`/`TOLOWER`; utils `test_crc32_file_large` drives `file_crc32`'s read loop past the first `CRC32_BUFFER_SIZE` chunk via a runtime-generated >1 MiB temp file (no committed binary fixture; `nread<0` stays platform-dependent/uncovered).  Net +201/-9 across 7 files.  Strongly validates the open "TEST-AUDIT — orphan-test detection" follow-up: the suite has no CI hook so these breaks accreted silently.  `linked_list.c` is vendored libretro-common — the NULL-guard is a candidate for upstreaming. | 7 |
| 77 | `31e000b6eb` | test-audit Tier-3 refactor/cleanup fold-in for the libretro-common libcheck suite (`local/fixes-2026-04`).  Closes the 3 Test-Audit 2026-05-18 Tier-3 items; **test-only, no system-under-test behaviour altered**, with exact check-count parity to Bundle 76's green baseline (stdstring 17, utils 4, hash 4, linked_list 96, generic_queue 16; `make -f Makefile.test` exit 0) plus a negative-control table mutation proving the new loop assertions are load-bearing (1 failure at `test_linked_list_remove_at_param:3`, iteration index reported).  **`tcase_add_loop_test` parametrisation (MEDIUM):** the 7 `remove_{at,first,last,all,first_matching,last_matching,all_matching}` families collapse from ~35 near-identical bodies into 7 per-family `static const` tables (3 row structs — index-keyed, value-keyed, predicate-keyed) driven by `tcase_add_loop_test`; rows carry the full per-row input (matching families keep their position-shuffled inputs), and `remove_at` rows now also assert the previously-unchecked return value.  Adds `_build_list_arr`/`_verify_list_arr` (varargs `_verify_list` delegates to the array form); `_value_{1,2,3}` switched `char*`→`char[]` so addresses are compile-time constants usable as static initialisers (pointer-identity unchanged).  **`tcase_add_checked_fixture` (MEDIUM):** the tests that all start from a fresh `[v1,v2,v3]` collection move to a dedicated "ThreeElement" tcase with a setup/teardown fixture — 12 in `test_linked_list.c`, 6 in `test_generic_queue.c` — dropping the inline `new` + 3× `add` boilerplate.  **`tmpnam`→`mkstemp` migration (LOW):** new shared `test/test_tmpfile.h` (`mkstemp`+`fdopen` POSIX / `GetTempFileName` Win32) routes all 3 call sites (`test_crc32_file`, `test_crc32_file_large`, `test_sha1`), removing the CWE-377 create-then-open TOCTOU race and the glibc "dangerous, better use mkstemp" link-time warning.  Net +439/-986 across 5 files (1 new header).  Both refactor mechanisms are idiomatic libcheck and could be upstreamed alongside the test files. | 3 |
| 78 | `a55c5986e5` (+ global config) | test-audit follow-ups (process/tooling) — closes the **last 2 active items** of the Test Audit 2026-05-18 section, so that whole `## Test Audit` section is now fully ✅.  **Orphan-test detection (MEDIUM, `a55c5986e5` on `local/fixes-2026-04`):** the libcheck suite had no build-only gate, so a SUT signature change that orphans a test (the 17-months-stale `string_replace_substring` 3-arg call Bundle 70 surfaced) or a renamed/removed SUT helper a test still links against (`strldup` / `path_is_directory`, Bundle 76) accreted silently — `Makefile.test` only had an `all:` target that compiles **and** runs **and** lcov-covers each suite in one pass.  Added a `build` target: compile + link all five suites via the existing `$(TEST_*_SRC)` vars but **don't** execute or run lcov.  Catches both the compile-error class (stale arg count) and the undefined-reference link class without needing the test run / coverage runtime.  Verified: green on the current tree (all 5 link, exit 0); EXIT 2 with `too few arguments to function 'linked_list_add'; expected 2, have 1` when a stale-signature call is injected, then file restored.  Reuses the source-list vars so it tracks future SUT-source changes automatically; added `.PHONY: all build clean`.  Documented as the test-suite orphan gate in `docs/private/audit/README.md` (new "Quick reference — auditing the test suite").  A hosted-CI hook stays out of scope (this fork audits by hand) — the by-hand `make -f libretro-common/Makefile.test build` gate now exists and is documented.  **Chunk-agent structural-pair audit (LOW, global Claude config, user-approved 2026-06-10, no repo commit):** the `/test-audit` chunk reviewer missed 3 of 6 missing-`END_TEST` sites (the build caught them) because it relied on reading-flow.  Added a grep-based **structural-pair audit** bullet to `~/.claude/skills/test-audit/references/dimensions.md` §4 (count `START_TEST` vs `END_TEST` per file, line-walk any mismatch; also flag defined-but-unregistered tests) + a matching explicit Procedure step #5 in `~/.claude/agents/test-audit-chunk.md`.  Framework-aware (names libcheck macros + pytest collection).  Lives in global config because the test-audit subagent is shared across all projects — RetroArch's ROADMAP records it for provenance but the artefact is not in this tree.  Cluster: 1 fixes-branch build-tooling commit + 1 doc + 2 global-config edits; too narrow + cross-cutting for an AUDIT-POLICY entry. | 2 |
| 79 | `14bf6e57e2` | **Branch-drift reconciliation — ports Bundle 69 onto `local/fixes-2026-04`.**  Bundle 69 (`150ace80fd`, the menu/ top-level cppcheck sweep) was committed to `local/audit-2026-04` **only** and never landed on the source-fix branch, so a fresh full-tree cppcheck on `local/fixes-2026-04` re-flagged all 29 of its already-✅ findings as if open — the cluster looked like new work but was a mis-branched bundle.  `git cherry-pick 150ace80fd` onto the fixes branch; menu_displaylist/menu_setting/menu_driver/menu_explore/menu_contentless_cores auto-merged, 4 conflicts in `menu/cbs/menu_cbs_ok.c` (the disk-index / cycle-tray / image-append local bools + the SUBSYSTEM_ADD_LIST `delim` invariant comment) resolved to Bundle 69's init-to-`false` side — the fixes branch had **independently** moved those assignments after the `!settings` guard (Bundle 54 shape), so B69's redundant-safe init-at-declaration layers cleanly with no double-guard.  No new distinct fixes: the same 28 sites are already counted under Bundle 69 (15 `%d`→`%u`, 7 MENU_LIST_GET deref-without-NULL-check, 6 deref-before-check incl. the real `menu_contentless_cores_get_info` missing-`return` crash, 1 `action_iterate_type` NULL-label guard).  **Verification:** cppcheck `--enable=warning` on the 6 files → 0 warning/error (was 29); `make -j retroarch` → LD clean, 20.8 MiB; `./retroarch --version` → 1.22.2 boots.  Surfaced the broader audit-only-source class while triaging: B63 (HAVE_S3 build wiring) + B66 (cppcheck FP suppressions) are **intentionally** audit-only; B69 was the lone mis-targeted *primary* source fix.  Cluster: 1 cherry-pick commit on `local/fixes-2026-04`; branch-hygiene reconciliation, not a new finding class. | 0 (port) |
| 88 | `1a238db72a` `b2906491e1` | **C89 linux-c89 CI regressions + `local-CI.sh` Linux CI mirror.**  Four C89 violations earlier audit bundles left on `local/fixes-2026-04` (never CI-validated — `local/*` branches don't trigger CI) that fail `make C89_BUILD=1` (retroarch.yml linux-c89): runloop.h:129 enum `(1u<<31)` exceeds INT_MAX → out of C89 int-range, fixed as `(int)(1u<<31)` (keeps d6bbeeab55's unsigned-shift UB avoidance); + 3 declaration-after-statement introduced by NULL-guard / buffer-init insertions — runloop.c SET_MESSAGE env-cb (46855caa12), verbosity.c `rarch_log_file_init` (ab7f4d0f6e), menu_setting.c `setting_append_input_bind_list` (bd9fc145d8) — each fixed by hoisting the inserted statement below the block's declarations.  Adds `local-CI.sh` reproducing the 5 runnable Linux CI jobs (native c89 + 2 sample suites, podman xenial-gcc9 for the 2 i686 jobs); documents host-lib divergence handling (native c89 fails only on repo-source errors, reporting newer-host-lib system-header pedantic noise — pipewire/libxkbcommon — as informational; `--disable-pipewire` aligns configure to CI's apt set) and a `--disable-qt` headless alignment: the reused `xenial-gcc9` i686 image bundles a Qt5 whose moc doesn't link RA's Qt frontend (master fails the same link in it), while CI's headless job runs Qt-off on `ubuntu-latest` — so `--disable-qt` reproduces CI's effective HAVE_QT=0 (see `docs/private/DEPENDENCY-POLICY.md` §4a).  All 5 local jobs green; linux-i686 = full build+link clean in CI's exact image. | 4 |
| 89 | `16a6bafc18` | **`gfx/` cppcheck warning-level sweep — 2 real DRM/KMS video-driver bugs (normal-path, not OOM).** `gfx/drivers/drm_gfx.c`: (1) `drm_plane_setup` dereferenced `plane_resources->count_planes` after an `if (!plane_resources)` branch that only logged and fell through — added the missing early `return;` (function is void); (2) `init_drm` read `connector` in `if (!connector)` after the connector-search loop, but left it uninitialized when `count_connectors == 0` (loop body never runs) — initialized to `NULL` at declaration, matching the loop's own sentinel. Build-verified (`HAVE_PLAIN_DRM=1`, libdrm 2.4.134); cppcheck clears both sites post-fix. **Audit-tooling improvement:** added `gfx/include/` to `docs/private/audit/scope.txt` — it is entirely vendored third-party graphics headers (GL/Cg/vulkan/ANGLE/d3d/MESA/pixman) plus the Broadcom/RPi VideoCore userland blob (`gfx/include/userland/`, own LICENCE); the unpruned `gfx/` sweep produced 2780 findings of which ~1628 were from that one vendored tree. Verified-FP (logged to `.ants_review_falsepos.jsonl`): `wayland_common.c:292/1066` uninitvar (cppcheck `wl_list_for_each` container-of macro blind spot; empty-list already guarded), `dispmanx_gfx.c:160-162` nullPointerRedundantCheck (`while (!page)` guarantees non-NULL at deref). 📋 **Deferred:** `nullPointerOutOfMemory` (malloc-not-checked, OOM-only deref) cluster across RA-own gfx files — `gl2.c` (12), `fpga_gfx.c` (7), `network_gfx.c`/`vga_gfx.c`/`xshm_gfx.c`/`rsx_gfx.c` (4-5 each), `gfx_widgets.c`, `drm_ctx.c` — real but low-severity, mechanical; a future OOM-hardening bundle. Also noted (not fixed, out of lane): pre-existing `-Wsign-compare` warnings in `drm_gfx.c` loops (454/492/508/797). | 2 |

**Cumulative:** 414 distinct fixes across 113 files. ~105 ✅ closed (full spec-needed cluster S1–S12; cloud-sync OOM via Phase 1 cap; runahead state-size mid-run growth; vulkan overlay destroy-loop NULL guard; cloud-sync atomic-save via Bundle 52; DRIVER-PATTERN `*_driver_find_driver` dedup via Bundle 53; deref-before-check Bundle 5 follow-up sweep on larger files via Bundle 54; S2 cluster fully closed via Bundle 55 (5 missed sites the Bundle 34/36 grep keyed only on inline-form `MENU_LIST_GET_SELECTION(...)->size`); Bundle 57 `gfx/`+`input/`+`command.c` narrow-scope sweep (printf-spec, invalidLifetime, uninitvar, nullPointer); Bundle 58 `audio/`+`cheevos/`+top-level sweep (deref-before-check, useless-assign + 5 FP classes documented at-line); Bundle 59 `network/`+`tasks/` sweep (uselessAssign cluster + dead-clause + signConversion + 2 FP classes documented at-line); Bundle 60 `runahead.c`+`libretro-db/` sweep (mylist_create OOM-NULL cluster + varFuncNullUB + alloca→fixed-array + 2 FP classes documented at-line); Bundle 61 clang-analyzer null-deref + dead-defensive sweep (runahead.c inner-calloc + capacity guard, VIDEO_HAS_FOCUS macro NULL-safety, MENU_LIST_GET_STACK_SIZE macro NULL-safety as S2 sibling, menu_setting/menu_driver deref-after-check, 2 dead-defensive drops, 3 FP NOLINTs); Bundle 62 closes the long-pending materialui.c cppcheck re-run with 3 narrow fixes (2 dead-clause + 1 %d→%u) and roadmaps 4 pre-existing tidy items (style noise, integer-division, misleading-indent FP, unused-includes) for separate sweeps; Bundle 63 wires `HAVE_S3` into the build (qb flag + Makefile gate + 1 latent `credential[256]→[512]` truncation fix) — closes the long-pending "BUILD — s3.c half-landed" item, and 5 prior fork-side bundles (17/26/32/52/56) that had touched s3.c without compile-testing now actually build; Bundle 64 widens the long-pending materialui.c `bugprone-integer-division` Tier-3 tidy entry to a tree-wide sweep across 8 menu+gfx layout files (51 sites total) — closes the line-259 TIDY entry, fixes the ½-px centring drift in float-context layout math throughout the menu UI; Bundle 65 sweeps the other three Linux-buildable menu drivers (ozone/xmb/rgui) for the real-bug + dead-code class Bundle 62 deferred, closing 9 sites total — 1 invalidLifetime fix (xmb.c::xmb_draw_icon, same shape as Bundle 57 gfx_thumbnail.c hoist), 1 dead `draw.color` init, 1 oppositeInnerCondition collapse, 1 duplicateBreak after goto, 3 dead `y +=` / `bool = false` initialisers, 1 shadowArgument rename; Bundle 66 closes the 6 cppcheck FPs Bundle 65 deferred (3 autoVariables in rgui.c::rgui_render_osk — synchronous-call ticker pattern where caller-locals are valid for the entire window the callee reads them; 3 duplicateExpressionTernary in xmb.c — author-marked intent placeholders preserving ps3_layout / icon_thumbnails branch structure for future divergence) via inline `cppcheck-suppress` comments following the Bundle 65 precedent; the 160 pure-style findings (constParameter*, shadowVariable, variableScope) remain open as a separate tree-wide menu-driver sweep per the Bundle 62 entry; Bundle 67 port-forwards Bundle 64+65's xmb.c portions to `local/audit-2026-04` (15 sites — 5 cppcheck real-bug + 10 clang-tidy integer-division) to close the cppcheck-cache findings that surfaced because the audit branch was missing the underlying fix commits the doc fold-ins already reference; Bundle 68 is the sibling port-forward for the remaining two Linux-buildable menu drivers `ozone.c` (12 cppcheck + 26 clang-tidy = 38 sites) and `materialui.c` (5 cppcheck + 5 clang-tidy = 10 sites) — 48 sites total closing every audit-branch warning-level cppcheck finding and every `bugprone-integer-division` clang-tidy warning on the two files, leaving rgui.c (already 0 actionable per Bundle 67 verification) as the only menu driver where no port-forward sweep was required; Bundle 69 closes 29 warning-level cppcheck findings across the 6 non-driver `menu/` top-level UI files that earlier subsystem-narrow bundles had never touched (`menu_displaylist.c`, `menu_setting.c`, `menu_driver.c`, `menu_explore.c`, `menu_contentless_cores.c`, `menu/cbs/menu_cbs_ok.c`) — 15 `%d`→`%u` printf-spec fixes (Bundle 56/57/62 shape), 7 MENU_LIST_GET deref-without-NULL-check sites (audit S2 sibling shape from Bundle 55), 6 deref-before-check / dead-control sites (Bundle 58 shape, including a real null-deref crash in `menu_contentless_cores_get_info` where a missing `return;` after `*info=NULL` let the function's own bad-input branch fall through into the very deref it was meant to reject), and 1 NULL-label deref guard in `action_iterate_type`; Bundle 76 closes the test-audit 2026-05-18 Tier-1 + Tier-2 coverage-gap cluster (7 items — 1 SUT NULL-guard bug fix + 6 test-coverage gaps) and makes the libretro-common libcheck suite build + run green for the first time in this fork via 4 harness repairs (the suite had never been executed here — libcheck was uninstalled — so Bundle 70's `-fsyntax-only` verification missed two link breaks, a GCC-15 `-Werror` break, and two NULL-on-UB segfaults); Bundle 77 closes the test-audit 2026-05-18 Tier-3 refactor/cleanup cluster (3 items — `tcase_add_loop_test` parametrisation of the 7 linked_list remove_* families, `tcase_add_checked_fixture` for the 12 list + 6 queue 3-element-setup tests, and `tmpnam()`→`mkstemp`/`GetTempFileName` migration via a shared `test/test_tmpfile.h`), test-only with exact check-count parity (96/16/4/4/17) and a negative-control mutation proving the loop assertions are load-bearing; …), 3 🚧 in-progress (atomic-save 7 of 8 Linux-buildable sites — Bundle 2 closed 4 disk-save sites and Bundle 52 closed 3 cloud-sync sites; BSV-replay-checkpoint still open as different shape; `config_file.c` is 🔄 vendored. Deref-before-check now 24/30 with 6 remaining — all out-of-Linux-build (Win32 wasapi/d3d10/d3d11/d3d9hlsl + console-only vita2d/gx2/ctr/ps2/gdi/dispmanx/orbis), plaintext-credentials chmod-only), 5 🔄 deferred (libretro-common vendored items + clang-analyzer FPs needing reproducer; core_updater_list.c:928 push_entry ownership-move FP from Bundle 28 triage). Plus the `s3.c:1633-1640` heap-overflow item from indie-review C2 was verified resolved-stale (current code uses safe `%.*s` and writes the terminator to a freshly-malloc'd buffer, not `data->data`). Audit S9 closes as a project-policy suppression — see `docs/private/AUDIT-POLICY.md`. The two `gfx/video_driver.c:3633, 3745` clang-tidy HIGH items closed as verified-FP — `video_ident` is NULL-checked at line 3613 before reaching 3633's `strcmp`, and `geom = &video_st->av_info.geometry` at 3744 is the address of a static-storage struct member (cannot be NULL); clang-analyzer can't trace either path-condition.

The full per-finding history is in the audit / indie-review / clang-tidy sections below — each item carries either 📋 pending, 🚧 partial, ✅ done with commit, or 🔄 deferred with reason.

---

## 🔍 Audit 2026-04-25

Branch: `local/audit-2026-04` off `master @ 6ff3332ea2`.
Tools run: cppcheck (partial — 449/450), semgrep, gitleaks (clean), trivy (clean), ruff, bandit.
Filtered by `audit-triage` subagent: 24 actionable, 12 spec-needed, ~660 noise items dropped.
Tool gaps: clang-tidy + clazy not run (no `compile_commands.json` — install `bear` and rerun); cppcheck did not finish `menu/drivers/materialui.c`; semgrep had 332 parse errors.

### Actionable

#### Critical

- ✅ **CRITICAL — UPnP XML parser trusts attacker-influenced router responses.** `network/natt.c:271-289`. Recursive `natt_parse_desc_node` walks IGD-supplied XML; subsequent `strstr`/`strlcpy` on `service_type->data` (lines 294-300) can be triggered by a malicious LAN gateway. _(Fixed `9953abc994` on `local/fixes-2026-04` — full rewrite of `natt_parse_desc_node` addressing both the NULL-deref and the inverted recursion. Confirmed open question from indie-review: NAT-PMP discovery indeed didn't work against typical IGDs before the fix.)_

#### High

- ✅ **HIGH — OOM null-deref in network/IPC `READ_CORE_MEMORY` handler.** `command.c:1187-1188`. _(Fixed `8aedb937f1` — added NULL-check on the malloc plus an upper bound on `nbytes` before the multiplication can wrap. Same fix applied to `command_read_ram`. Combined with the network-cmd localhost-only bind, the original LAN-RCE reproducer is no longer reachable.)_
- ✅ **HIGH — `strchr(key, '/') + 1` on attacker-supplied cloud-sync manifest.** `tasks/task_cloudsync.c:640`. _(Fixed `18b55ec71a` — capture `slash = strchr(key, '/')` first, treat `path = NULL`/empty/leading-slash/`..`-containing as malformed and short-circuit with `RARCH_WARN` + manifest update + `sync_state->failures = true`. Closes both the `NULL+1` UB and the path-traversal escape on `fill_pathname_join_special` covered by item 143.)_
- ✅ **HIGH — `strcpy` with overlapping buffers (UB).** `cheevos/cheevos_client.c:169`. _(Fixed `8aedb937f1` — `strcpy` → `memmove(start, next+1, strlen(next+1)+1)`.)_
- ✅ **HIGH — OOM null-deref on per-value-hash malloc.** `core_option_manager.c:721-727, 1006-1007`. _(Fixed `18b55ec71a` — `parse_variable` `goto error` on `value_hash == NULL` (function already has matching `error:` label freeing partial state); `parse_option` `return false` on `value_hash == NULL` (matches the function's existing return-false-on-failure pattern; caller `core_option_manager_new` frees the partial `opt` at its own `error:` label).)_
- 🚧 **HIGH — Cluster sweep: ~30+ deref-before-NULL-check sites.** _(Bundle 5 — 8 Linux-build sites fixed in `8eab27096d`; Bundle 18 — 3 follow-up sites in `349cc5644b`; Bundle 54 — 13 sites across the deferred larger files in `6dae6b6377`, all on `local/fixes-2026-04`.)_
  - ✅ `audio/drivers/oss.c::oss_init` error label
  - ✅ `gfx/drivers/gl3.c::gl3_raster_font_render_msg` (`gl->video_width`)
  - ✅ `gfx/drivers/gl2.c::gl2_renderchain_recompute_pass_sizes` (`gl->shader`)
  - ✅ `gfx/drivers/vulkan.c::vulkan_get_message_width` (`font->font_driver->get_glyph`)
  - ✅ `input/input_driver.c::input_config_get_bind_string` (`bind->key`)
  - ✅ `input/common/wayland_common.c::data_device_handle_drop` (`offer_data->dropped`)
  - ✅ `runahead.c::mylist_add_element` (`list->size`)
  - ✅ `tasks/task_pl_thumbnail_download.c` 2 sites (`pl_thumb->type_idx`)
  - ✅ Bundle 54 — `menu/cbs/menu_cbs_ok.c` 4 sites (3 real `settings->bools.*`-before-`!settings` + 1 dead `if (delim)` on stack-array address)
  - ✅ Bundle 54 — `menu/drivers/ozone.c` 5 sites (1 real `(uintptr_t)ozone->pending_message`-before-`!ozone`; 2 dead `if (ozone->theme)` checks where theme is set unconditionally at init; 1 dead `if (node)` post-`if (!node) goto border_iterate`; 1 `s &&` consistency add on the `imageviewer` branch)
  - ✅ Bundle 54 — `menu/drivers/xmb.c` 3 sites (2 real — `xmb->show_playlist_tabs`-before-`!xmb` + `selection_buf->size` outside its existing guard; 1 `s &&` consistency add matching documented NULL-passing callers)
  - ✅ Bundle 54 — `menu/drivers/materialui.c` 3 real sites (`node->entry_width`-before-thumbnail-block-`if (node)` 88 lines later; `settings->uints.*`-before-`!settings`; `strlen(path)`-before-`(path && *path)`)
  - ✅ Bundle 54 — `network/netplay/netplay_frontend.c` 2 sites (redundant `(key && *key) && (value && *value)` collapsed to `*key && *value` since both come from already-NULL-checked allocations)
  - 📋 Win32-only sites (`wasapi.c`, `d3d10/d3d11/d3d9hlsl_gfx.c`) — not in Linux build, deferred.
  - 📋 Console-only sites (`vita2d`, `gx2`, `ctr`, `ps2`, `gdi`, `dispmanx_gfx.c`, `platform_orbis.c`) — out of personal-fork scope.
  - ❌ `gfx/gfx_thumbnail.c` — cppcheck FPs (`thumbnail_path` is a stack array). Won't fix.
- ✅ **HIGH — Wii `pad_type[8]` indexed up to pad 15.** `input/drivers_joypad/gx_joypad.c:435`. _(Fixed `72b2a839f8` — `pad < MAX_USERS` -> `pad < DEFAULT_MAX_PADS` so the check fails before the pad_type read can OOB. Wii / GameCube only; not built in the Linux desktop build.)_

#### Medium

- ✅ **MEDIUM — `oi` may be NULL after empty `wl_list_for_each`.** `gfx/common/wayland_common.c::gfx_ctx_wl_get_video_size_common`. _(Fixed `71fa88edb0` — added `if (!oi) return;` after the fallback `wl_list_for_each` rather than overwriting the caller's width/height with NULL-deref garbage. Reproducer: hot-unplug last monitor before frame size query.)_
- ✅ **MEDIUM — calloc'd struct leaks on `memalign` failure.** _(Fixed `149fdd5c1d` — `audioworklet`: `free(audioworklet)` on memalign(WORKLET_STACK_SIZE) fail; `rwebaudio`: `free(rwebaudio)` on `RWebAudioInit` fail.)_
- ✅ **MEDIUM — Font-init leaks on renderer-create failure across 4 backends.** _(Fixed `149fdd5c1d` — caca/gdi/sixel/vga `font_renderer_create_default` fail path now `free(font)` before `return NULL`. All four sites, same shape.)_
- ✅ **MEDIUM — Cluster: 60 OOM null-deref findings in cloud-sync HTTP request builders.** `network/cloud_sync/google_drive.c` (29), `network/cloud_sync/webdav.c` (31 — roadmap "23" was the 2026-04-25 snapshot pre-`nullPointerArithmeticOutOfMemory` sub-findings against the digest header builder's strlcpy chain). _(Bundle 45 — fixed in `794a7a04bf` on `local/fixes-2026-04`. Closed by adding NULL-checks at the 17 distinct allocation sites the 60 derefs collapse to: 8 in `google_drive.c` (refresh-then-retry, folder-walk, begin-with-token, sync_begin refresh + oauth, read/update/delete public entries) + 9 in `webdav.c` (4 digest-auth helpers with chained NULL propagation, sync_begin, read/update/delete). Pattern mirrors `webdav_ensure_dir` Bundle 12 + `task_overlay_handler` Bundle 11. cppcheck cluster cleared to zero post-fix.)_
- ✅ **MEDIUM — calloc'd `ft_info` not NULL-checked before strdup/snprintf.** `cores/libretro-video-processor/video_processor_v4l2.c:670/683/691`. _(Bundle 48 — fixed in `13689e17da` on `local/fixes-2026-04`. Three early-return guards on the two `calloc(5000, sizeof(char))` sites and the `strdup(ft_info)`. Function is `void` and the cleanup site (`close_devices`) already gates `if (ft_info2)`, so OOM-skipped frames just leave the lazy-init pointer NULL for the next call to retry. Opt-in core (`HAVE_VIDEOPROCESSOR=0` default), so default Linux desktop build unchanged.)_

#### Low

- ✅ **LOW — Unbounded `strcpy`/`strcat` on 1024-byte buffers.** `ai/game_ai.c:153, 182-184`. _(Bundle 47 — fixed in `54fc62b5c5` on `local/fixes-2026-04`. Mechanical migration: strcpy → strlcpy(dst, src, sizeof(dst)) at the `game_ai_load` site; strcpy + 2× strcat → strlcpy + 2× strlcat at `game_ai_think`'s data_path build-up, each bounded against the 1024-byte stack buffer. File already includes `<compat/strl.h>`. Module is opt-in (`HAVE_GAME_AI=0` in default config); standalone compile clean with `-Wall -Wsign-compare`.)_
- 🔄 **LOW — `backend->compressed_file_read` derefs NULL backend.** `libretro-common/file/archive_file.c:555-557`. Vendored — patch upstream and re-vendor.
- 🔄 **LOW — `(json + 1)` arithmetic before NULL check.** `libretro-common/formats/json/rjson.c:971-975`. Vendored.

### Spec-needed

These look intentional but lack a documented contract. Each was a candidate for a reactive spec under `docs/private/specs/` before the cluster closed.

✅ **Cluster closed 2026-04-27** — all twelve spec-needed items have either a code fix (S1, S2, S3, S5, S6, S7, S8, S10, S11, S12), a per-file static-analyser suppression (S4, S12), or a project-policy class suppression (S9 — canonical free-on-fail-label pattern, suppressed in `aggregate.py` and documented in `docs/private/AUDIT-POLICY.md`). Spec at [`docs/private/specs/2026-04-27-audit-spec-needed-cluster.md`](specs/2026-04-27-audit-spec-needed-cluster.md). External research verified Wayland `wl_touch` `id` is uncapped (per `wayland.xml` v10) and XInput `XUSER_MAX_COUNT = 4` is fixed across all XInput versions (per Microsoft Win32 docs).

- ✅ **S5 — `input_key_pressed()` public-API-or-delete decision.** `input/input_driver.c:5433-5476`. _(Fixed `233568bfe0` Bundle 34 — option A: declared public API in `input_driver.h:1097` with Doxygen comment, hardened entry with `if (key < 0 || key >= RARCH_BIND_LIST_END) return false`, simplified inner branch. The unbounded `input_config_binds[0][key]` joypad-fallback read at line 5443 is now unreachable for OOB key.)_
- ✅ **S2 — `MENU_LIST_GET` / `MENU_LIST_GET_SELECTION` macro contract.** `menu/menu_driver.h:72-78` plus 26 unguarded callers (16 caught by the initial sweep + 5 caught by `/debt-sweep` follow-up + 5 caught by Bundle 55 cppcheck re-run) across `menu/cbs/menu_cbs_right.c:223`, `menu_cbs_sublabel.c:1946`, ozone (11 sites), xmb (12 sites), materialui (1 site). _(Fixed `335bb31cf9` (initial sweep) + `382ee9f83f` (debt-sweep follow-up) + `3537f96a38` (Bundle 55 cppcheck re-run) — option A: macro stays NULL-safe (returns NULL when `list` is NULL), all unguarded `->size`/`->list[]` derefs swept to use a NULL guard. Where the site also assigned `selection_buf = MENU_LIST_GET_SELECTION(...)`, lifted that assignment above the size deref and used the local pointer for the guard. Plus a contract comment near the macro definition naming the audit/indie-review evidence (clang-analyzer C1 path-sensitive proof) so future maintainers don't re-introduce the unguarded pattern. The Bundle 55 catches were sites that also extend the contract to `menu_stack` (the other macro the contract covers — `ozone.c:4767` `ozone_change_tab`, `materialui.c:10050` `materialui_preswitch_tabs`) plus three `selection_buf->list[idx].userdata` derefs (`ozone.c:8009/11770`, `xmb.c:1626`) that the Bundle 34/36 grep didn't key on (it keyed on `->size`). Cluster cleared to zero post-fix on cppcheck.)_
- ✅ **S11 — `identicalInnerCondition` policy.** `audio/audio_driver.c:447`, `audio/drivers/dsound.c:452`, `audio/drivers/openal.c:129`, `cheat_manager.c:1820+1873`, `tasks/task_content.c:609`, `menu/menu_driver.c:4647`, `camera/camera_driver.c:155`. _(Fixed `9ddc80cb84` Bundle 34 — option B: copy-paste residue, strip. Driver tables (`audio_drivers[]`, `menu_ctx_drivers[]`, `camera_drivers[]`) are const-qualified static arrays; the inner NULL re-check inside their enumeration loops is dead code. Same for the dsound/openal `if (list->elems)` inside an outer `if (list && list->elems)`. cheat_manager's two CHEAT_MATCH_ACTION_TYPE_DELETE arms had redundant inner `if (bits < 8) / else` where the outer branch already pinned `bits` — kept only the live branch in each arm. task_content.c:609 is a confirmed FP, not a strip — the trailing-slash strip can null `*dir` if dir == "/", so the second `if (*dir)` is load-bearing; marked with inline `cppcheck-suppress` and a comment naming the side effect.)_
- ✅ **S1 — `(1 << 31)` signed-overflow flag enums.** `runloop.h:96-130` (32 enum sites), `input/input_driver.c:1922-2061` (turbo + hold sweep), `menu/cbs/menu_cbs_sublabel.c:1880`, `network/netplay/netplay_frontend.c` (35+ sites). _(Fixed `d6bbeeab55` Bundle 34 — mechanical rewrite to `1u << N`. Plus `input/input_types.h` `turbo_pressed[MAX_USERS]` and `hold_pressed[MAX_USERS]` switched from `int32_t` to `uint32_t` since the sign bit was used as the "turbo active" sentinel — UB once `(1 << 31)` was computed on a signed operand. The `>= 0` test for "bit 31 clear" became `!(... & (1u << 31))`. The OR-into-field is now well-defined.)_
- ✅ **S10 — `(1 << pad)` runtime shifts.** _(Coupled with S1 in commit `d6bbeeab55` Bundle 34. The xdk_joypad / xinput_joypad sites called out by the spec were already addressed by Bundle 31 (the S6+S8 close swept them); Bundle 34 covered the remaining variable-shift sweep across input_driver.c and netplay_frontend.c.)_
- ✅ **S3 — `wayland_common.c` `#ifdef HAVE_LIBDECOR_H` if/else pairing.** `gfx/common/wayland_common.c:1031-1090`. _(Fixed `db21ebd07e` Bundle 34 — option A: hoisted the `if (video_monitor_index <= 0)` (auto) vs. `else` (walk-and-pick) split out of the `#ifdef` so the `else` binding is unambiguous regardless of which `if` survives the preprocessor. Libdecor-only logging stays inside an inner `#ifdef` on the auto arm. Behaviour preserved in both build configs — `output_i` starts at 1, so the original non-libdecor `else` walking when `video_monitor_index <= 0` could never have matched anyway.)_
- ✅ **S4 — Build-matrix question for qnx/vivante/xegl context drivers.** `gfx/drivers_context/qnx_ctx.c`, `vivante_fbdev_ctx.c`, `xegl_ctx.c:299`. _(Closed `8c3d825be6` Bundle 35 — option 2 from the spec: file-pattern review verified all three are EGL-by-design context drivers (xegl by name; qnx_ctx + vivante_fbdev_ctx use EGL for GLES rendering), and `Makefile.common:1598-1602` confirms `xegl_ctx.o` only links when `HAVE_X11=1 && HAVE_EGL=1` (the other two have no purpose without EGL either). cppcheck flags `vid`/`new_width`/`new_height` as uninitialised because it analyses the source with HAVE_EGL undefined — a configuration the build system never compiles. 10 line-anchored entries added to `.cppcheck-suppress.txt` (xegl: 2, qnx: 4, vivante: 4). `--suppressions-list=.cppcheck-suppress.txt` was already wired into `audit-config.json` from the S12 close. No upstream maintainer answer was needed; the file content + Makefile gating made the call clear.)_
- ✅ **S6 — Bind-index arithmetic invariant unasserted.** `menu/cbs/menu_cbs_scan.c:185-188`. _(Fixed `630c7294fd` Bundle 31 — two `_Static_assert`s at file scope above `action_scan_input_desc` pin (1) `MENU_SETTINGS_INPUT_DESC_END − BEGIN == RARCH_ANALOG_BIND_LIST_END * MAX_USERS` (so user_idx stays in [0, MAX_USERS-1]) and (2) `RARCH_ANALOG_BIND_LIST_END <= RARCH_CUSTOM_BIND_LIST_END` (so key stays inside `input_remap_ids`'s inner dim). Spec rationale at `docs/private/specs/2026-04-27-audit-spec-needed-cluster.md`.)_
- ✅ **S7 — Wayland touch-index policy.** `input/drivers/wayland_input.c:117`. _(Fixed `e67532e244` Bundle 33 — tightened the `idx <= MAX_TOUCHES` guard at `input_wl_touch_state` to `idx < MAX_TOUCHES`. The shipping caller at line 316 already bounds idx with the correct `<`, and the upstream slot allocator in `wl_touch_handle_down` (`input/common/wayland_common.c:338`) caps the slot below MAX_TOUCHES — so the OOB on the local guard was unreachable through the production call path. Static analysis flagged it anyway because in isolation `<=` permits `wl->touches[MAX_TOUCHES]`. Added a multi-line invariant comment at the call site naming the upstream `wl_touch_handle_down` slot-allocator as the bound owner so a future caller refactor can't silently reintroduce the off-by-one. Spec rationale at `docs/private/specs/2026-04-27-audit-spec-needed-cluster.md`.)_
- ✅ **S8 — XInput controller-port-count invariant.** `input/drivers_joypad/xinput_joypad.c:289` + `xdk_joypad.c:300`. _(Fixed `630c7294fd` Bundle 31 — introduced `#define MAX_XINPUT_USERS 4` (matches `XUSER_MAX_COUNT` per Microsoft Win32 XInput docs; fixed across XInput 1.3, 1.4, 9.1.0, UWP) plus `_Static_assert(MAX_XINPUT_USERS <= DEFAULT_MAX_PADS, ...)` so a future `DEFAULT_MAX_PADS` shrink below 4 is a build break. Swept the file: array dims (rumble_states, states, active_port), init/destroy loop bounds, the xuser-mapping range check at line 420 — all `MAX_XINPUT_USERS`. xdk_joypad's `pad < MAX_USERS` against `g_xinput_states[DEFAULT_MAX_PADS]` tightened to `pad < DEFAULT_MAX_PADS`. Spec rationale at `docs/private/specs/2026-04-27-audit-spec-needed-cluster.md`.)_
- ✅ **S9 — semgrep `double-free` cluster (29 sites) is a confirmed FP class.** Pattern: success-then-return / fail-label-cleanup. Files: `network/cloud_sync/s3.c`, `tasks/task_database_cue.c`, `gfx/drivers/d3d9cg.c`, `gfx/drivers_font/bitmapfont_*.c`, `tasks/task_save.c`, plus stb / video_shader_parse. _(Closed as project-policy suppression — the canonical pattern is documented in [`docs/private/AUDIT-POLICY.md`](AUDIT-POLICY.md), and `aggregate.py` (KNOWN_FP_RULES) drops the matching findings before triage. The 29 sites are correct as written; refactoring them to a single-cleanup-block idiom would touch well-tested code paths for no behavioural change. The drop rule is permanent — a regression introducing a real double-free in one of the listed files would surface elsewhere (cppcheck / clang-analyzer flow analysis catches the easy cases).)_
- ✅ **S12 — JNI-callback `missingReturn` FPs.** `play_feature_delivery/play_feature_delivery.c:117, 187`. _(Fixed `b27a516cf5` Bundle 34 — anchored `.cppcheck-suppress.txt` at repo root with the two `missingReturn:play_feature_delivery/play_feature_delivery.c:117/187` entries plus a comment naming cppcheck 2.20's mis-classification of `JNIEXPORT void JNICALL` functions. Wire-up via `--suppressions-list=.cppcheck-suppress.txt` — added to `audit-config.json` cppcheck flags.)_

### Pending audit re-runs

- ✅ Install `bear` (via your distro package manager), regenerate `compile_commands.json` via `bear -- make -j$(nproc)`, then run clang-tidy + clazy. _(Bundle 61 — `bear` installed; `compile_commands.json` regenerated; first clang-tidy `clang-analyzer-*` pass against the fixes-branch source surfaced 11 findings across 6 files. 8 real bug sites + 2 dead-defensive cleanups + 3 FP NOLINTs landed in `bd9fc145d8`. clazy still pending (Qt frontend opt-in; can rerun against the existing compile_commands.json on demand). The S1 signed-integer-overflow surface was already closed by Bundle 34 — no new sites surfaced by this pass.)_
- ✅ Re-run cppcheck letting `menu/drivers/materialui.c` finish (expect +10-15 items in the S2 pattern). _(Bundle 62 — `9ed75a3d51`. Re-ran with `--max-configs=1 --check-level=exhaustive` per the audit-config `known_issues` workaround; the file completes in ~10 s instead of timing out at default branch-budget. Predicted +10-15 S2-pattern hits did not materialise: the 4 `nullPointer*` raw findings on the audit-branch snapshot (lines 5916, 9659, 10037, 12045) were stale cite-points whose underlying derefs had already been guarded by Bundles 54 + 55 on the fixes branch. Only 3 unrelated actionable sites on the fixes-branch snapshot — fixed in Bundle 62. Pre-existing style/tidy noise surfaced by this run (style fields, integer-division, misleading-indent, unused-includes) tracked under the new ROADMAP entries below.)_
- 🚧 Re-run semgrep with pinned parser (332 parse errors → expect FP-rate reduction on the double-free cluster).

---

## 🔬 Indie-Review 2026-04-25

8-lane independent multi-agent review of master @ `6ff3332ea2`. Each agent reviewed one subsystem cold (no session context, no test files) against `libretro.h` / inline contract docs / external standards (HTTP/1.1, RFC 7616 digest auth, RFC 8628 device flow, AWS SigV4, RFC 4918 WebDAV).

Lanes: libretro env-callback boundary, configuration triad, driver-pattern meta, task queue, network commands/IPC, cloud sync + cheevos, menu core + 4 drivers, save state + replay + runahead.

Threat model applied to severity calibration: this is a personal fork running on a single-user desktop, but RetroArch ingests untrusted data from many sources — downloaded save states/replays, malicious LAN routers (UPnP), MITM-able HTTPS endpoints (cloud sync, cheevos), and netplay opponents. Items that depend ONLY on the local machine being trustworthy are downgraded; items that depend on remote data being well-formed are kept at raw severity.

**Cross-references** with the audit run above are noted as `(audit Cn/Hn/Sn)`.

### 🔥 Cross-cutting themes (caught by ≥2 independent reviewers)

These are the highest-confidence findings — multiple reviewers coming at the code from different angles all flagged the same root cause.

- 🚧 **🔥 No atomic write on disk-save paths.** Every "save user data to disk" path opens the destination directly with truncating-write mode. Power loss / OOM kill / `kill -9` mid-save = file corrupt and previous save gone. _(Bundle 2 — partially fixed in `403106af54` on `local/fixes-2026-04`.)_
  - ✅ `tasks/task_save.c::task_save_handler` (save state)
  - ✅ `tasks/task_save.c::content_auto_save_state` (auto save at unload)
  - ✅ `tasks/task_save.c::content_ram_state_to_file` (RAM-state-to-file)
  - ✅ `disk_index_file.c::disk_index_file_save` (disc index — multi-disc games revert to disc 1)
  - 📋 `input/bsv/bsvmovie.c:759-797` (BSV replay checkpoint — different shape: writes in-place to active replay stream rather than producing a complete-file destination, so tmp+rename idiom doesn't apply; needs per-event framing/checksumming instead)
  - 🔄 `libretro-common/file/config_file.c:1410-1432` (every cfg save) — vendored upstream, can't patch locally
  - ✅ `network/cloud_sync/{google_drive,webdav,s3}.c` (every downloaded synced file). _(Bundle 52 — fixed `b510d9d654` on `local/fixes-2026-04`. Same tmp+rename idiom as Bundle 2 applied to all three cloud-sync `read_cb` paths: open `<file>.tmp` in WRITE mode, write the body, close, then `filestream_delete(dest); filestream_rename(tmp, dest)`; on partial-write/open/rename failure `filestream_delete(tmp_path); success=false`. Reopen `dest` in READ mode for the caller's MD5-and-close cycle. Behavior change in failure paths: previously a partial write or failed local open silently returned `success=true, file=NULL` (the "remote-doesn't-have-file" contract), so the manifest skipped the file next sync; now those paths report `success=false`, causing a refetch on the next sync. Combined with the Content-Length verification already in place (line 219), this closes the cross-cutting "downloaded synced file gets silently truncated/corrupted" failure mode.)_
  - **The pattern applied:** open at `<path>.tmp`; on success `filestream_delete(dest); filestream_rename(tmp, dest)`; on failure `filestream_delete(tmp)`. POSIX `rename(2)` is atomic over an existing file on the same filesystem.
- ✅ **🔥 Buffer-overflow class in malicious save-state / replay parsing.** _(Bundle 3 — fixed in `1f59d9d31d` on `local/fixes-2026-04`.)_
  - ✅ `tasks/task_save.c::content_load_rastate1` — added bounds check on `block_size` against remaining buffer (rejects truncated headers, oversize blocks, and alignment-arithmetic overflow); switched the byte-shift assembly to `((uint32_t)input[N]) << shift` to remove signed-int promotion UB.
  - ✅ `input/bsv/bsvmovie.c::bsv_movie_read_next_events` — capacity-check `key_event_count` against `ARRAY_SIZE(key_events)` and `input_event_count` against `ARRAY_SIZE(input_events)` before each read loop.
  - ✅ `input/bsv/bsvmovie.c::replay_set_serialized_data` — reject `loaded_len < REPLAY_HEADER_LEN_BYTES` before any `intfstream_seek` / `intfstream_write` picks it up. Negative values were the worst case (cast to huge `size_t`).
  - **Threat is real** — RetroArch users routinely load save states and replays shared online. The corresponding clang-tidy CRITICALs (security.ArrayBound on driver-array iteration, runahead heap OOB) remain on the roadmap.
- ✅ **🔥 `task_http`-class UAF pattern at the caller layer.** _(Bundle 7 — fixed in `de0c6000c8` on `local/fixes-2026-04`.)_
  - ✅ `tasks/task_core_updater.c::cb_http_task_core_updater_get_list` and `::cb_http_task_core_updater_download` — these were the real ASan-detectable UAFs: the outer handlers call `task_get_flags` / `task_get_progress` on the inner `http_task` pointer after the queue has freed it. Fix: NULL the pointer in the cb before flipping the COMPLETE flag the outer handler waits on.
  - ✅ `tasks/task_pl_thumbnail_download.c::cb_http_task_download_pl_thumbnail` — same pattern, no actual deref in shipping code (only NULL-test of stale pointer, strictly UB but practically harmless), plugged for consistency.
- ✅ **🔥 Path traversal via attacker-supplied cloud-sync manifest.** `tasks/task_cloudsync.c:640` `strchr(key, '/') + 1` flagged by audit, tasks-lane reviewer, and cloud-sync reviewer. Compromised or hostile sync server can write `../../../home/user/.bashrc` into the user's machine. _(Bundle 50 — duplicate cross-reference of the audit-section ✅ entry above (line ~93). Fix landed in `18b55ec71a` Bundle 12 — `task_cloudsync.c::task_cloud_sync_fetch_server_file` now (1) captures `slash = strchr(key, '/')` first and treats `path = NULL` (no slash), empty, leading-slash, or `..`-containing as malformed; (2) short-circuits with `RARCH_WARN`, manifest-update via `task_cloud_sync_add_to_updated_manifest`, and `sync_state->failures = true`. Verified in current code at `task_cloudsync.c:644-658`. Closes both the original NULL+1 UB and the path-traversal escape via `fill_pathname_join_special`. The cross-cutting cluster entry was the indie-review listing of the audit-section finding from a different reviewer angle; reconciled here for index hygiene — same shape as the Bundle 49 `cheevos` reconciliation.)_

### 🔒 Tier 1 — ship-this-week security/data-loss blockers

These are exploitable now and have concrete reproducers.

- 🚧 **CRITICAL — TLS certificate verification effectively disabled.** `libretro-common/net/net_socket_ssl_mbed.c:199` uses `MBEDTLS_SSL_VERIFY_OPTIONAL`; the verify result is logged into a buffer and discarded — connection proceeds against any cert including self-signed. **Every** HTTPS request in cloud sync (Google Drive OAuth + Drive API, S3 SigV4 signing, WebDAVS), cheevos (RetroAchievements API), and online updater is un-authenticated. On hostile WiFi, an MITM captures Google OAuth refresh tokens, AWS secret access keys, and RA credentials. Vendored — fix must go upstream first; a temporary RA-side opt-out with a giant warning would be acceptable. (Not in audit; indie-review only.) _(Spec drafted 2026-04-27 → [`docs/private/specs/2026-04-27-tls-verification-opt-in-design.md`](specs/2026-04-27-tls-verification-opt-in-design.md). Three-mode design (REQUIRED default / OPTIONAL bridging / DISABLED with consent dialog), advanced-tier UI, vendored fix paired with upstream PR. ~2-3 days RA-side; phase 1 alone closes the indie-review concern.)_
  Progress (2026-07-01, Bundle 87 / fixes `436928da68`): secure core landed
  on `local/fixes-2026-04`. The indie-review CRITICAL is closed for the
  mbedtls backend this fork builds — `net_socket_ssl_mbed.c` now defaults to
  `MBEDTLS_SSL_VERIFY_REQUIRED` and fails closed (handshake returns
  non-zero, `ssl_socket_connect` returns -1) with a logged reason; OPTIONAL
  soft-fails + warns and proceeds; DISABLED skips verification + warns on
  every connect. New `tls_verify_mode` setting (RA enum
  `network/tls_config.h`; persisted `uints.tls_verify_mode`; Settings →
  Network → Advanced, `SD_FLAG_ADVANCED`), applied live via
  `general_write_handler` and once at startup in `retroarch_main_init`.
  Vendoring-clean log surface: weak `ssl_socket_log_verify_fail/_disabled`
  no-op hooks in the backend, RA-side `RARCH_ERR/WARN` overrides in new
  `network/tls_log.c` (griffin-wired). BearSSL already fails closed (its
  native behaviour) — given a minimal `set_verify_mode` so it links; its
  OPTIONAL/DISABLED opt-out (permissive vtable) is deferred and it keeps
  verifying regardless (fail-safe). 16 files, +212/-3; `make -j retroarch`
  clean, boots 1.22.2, config key + UI strings verified. Deviations from
  spec (documented inline): `volatile unsigned` instead of C11 atomics
  (single aligned word, next-connection semantics; keeps the vendored file
  C89-clean); REQUIRED-failure logging moved to the reachable
  handshake-failure path (the spec's line-220 REQUIRED branch is dead code —
  its own cold-eyes nit #1). Still open, deferred as non-security-core:
  one-time consent dialog on selecting Disabled (D3), on-screen toast +
  upgrade release-note popup (Phase 4), BearSSL permissive-vtable opt-out,
  libcheck suites under `libretro-common/test/net/`, and the upstream
  libretro-common PR (Phase 5). Bullet stays 🚧 until those land; the
  exploitable MITM hole itself is closed on this build.
- ✅ **CRITICAL — Network command socket binds 0.0.0.0 unauthenticated.** `command.c:257-258`. _(Fixed `8aedb937f1` — bind hardcoded to `127.0.0.1`. Cross-host IPC users should prefer SSH local-forward; a setting-driven opt-out is roadmapped but intentionally unwired so the safe default ships universally.)_
- ✅ **CRITICAL — `command_write_ram` heap-write OOB driven from the wire.** `command.c:970-990`. _(Fixed `8aedb937f1` — capped the write loop at 4096 bytes via `COMMAND_WRITE_RAM_MAX_BYTES`. Threading the actual descriptor length through `rcheevos_patch_address` is a deeper API change; this cap is generous for cheat-poke use and prevents arbitrary heap-write past the descriptor.)_
- ✅ **CRITICAL — `natt_parse_desc_node` NULL-deref + inverted recursion on hostile UPnP response.** `network/natt.c:268-318`. _(Fixed `9953abc994` — same fix as the audit C1 entry above.)_
- ✅ **CRITICAL — Use of uninitialized stack memory in `webdav_ensure_dir`.** `webdav.c:700-713`. _(Fixed `18b55ec71a` — `memset(&data, 0, sizeof(data))` before `data.status = 200`. Same commit also NULL-checks the `webdav_mkdir_state_t` malloc and calls `cb(false, ...)` on OOM rather than dereffing NULL on the next line.)_
- ✅ **HIGH — Netplay password salt is `time(NULL)`-seeded LCG; password compare is non-constant-time.** `network/netplay/netplay_frontend.c`. _(Fixed `d973d6cc0c` — `netplay_secure_random_bytes()` helper added next to the existing `simple_rand` block: BCryptGenRandom on Windows (`#pragma comment(lib, "bcrypt")` + `<bcrypt.h>`), `arc4random_buf` on macOS/BSD, glibc-2.25+ `getrandom(2)` (looped on EINTR) on Linux, `/dev/urandom` POSIX fallback. Salt path now reads 4 bytes from CSPRNG; LCG remains as a deeper fallback for platforms with none of the above sources, but its seed is now `cpu_features_get_time_usec() ^ (uintptr_t)connection` instead of bare `time(NULL)`. `netplay_constant_time_eq()` (XOR-accumulate then zero-test, `volatile` read cues) replaces `memcmp` at both verify sites.)_
- ✅ **HIGH — WebDAV digest parser unbounded walks + hardcoded cnonce.** `webdav.c:174-280`. _(Fixed `2386e898d4` — factored the `field=<terminator>` pattern into `webdav_parse_quoted_value` which checks `strchr` first, NULL-checks the malloc, and advances ptr only on success (used by realm/nonce/opaque/algo-with-comma; algo-no-comma branch NULL-checks `strdup`). Fixed the `while (*ptr != ',' && *ptr != ',')` typo to `*ptr != ',' && *ptr != '\0'`; guarded the surrounding `ptr++`-past-NUL sites with `if (*ptr) ptr++;`. Replaced the hardcoded cnonce with `webdav_create_cnonce` mixing `time(NULL) | clock() | (uintptr_t)&local | nc` through MD5 to 32 hex chars (not a CSPRNG — libretro-common doesn't wrap one yet — but stops the wire-only replay attack the digest scheme is meant to defend). Added a `parse_fail:` label that calls `webdav_cleanup_digest()` so any partially-populated state is freed on early failure. cnonce is now malloc'd; `webdav_cleanup_digest` frees it.)_
- 🚧 **HIGH — Plaintext credentials in world-readable `retroarch.cfg`.** `configuration.c:1629, 1634, 1637-1639, 1649, 1722, 1749-1750`: WebDAV password, AWS secret access key, YouTube/Twitch/Facebook stream keys, SMB password, kiosk-mode password, netplay password. Saved as `key = "value"` to a file with default umask (0644). Plus the Google Drive **refresh token** is stored plaintext per `google_drive.c`. _(Bundle 4 — `chmod 0600` mitigation landed in `8aedb937f1`. Secrets-file split + OS-keyring integration still open.)_ Mitigation tier:
  1. `chmod 0600` after `config_file_write` succeeds (one-line, ships immediately)
  2. Move secrets to a separate `retroarch.secrets` file in `~/.local/share/retroarch/secrets/` (longer)
  3. OS-keyring integration via libsecret/Win32 DPAPI/Keychain (longest)
- ✅ **HIGH — `command_read_ram` / `command_read_memory` integer-overflow + missing NULL-check on malloc.** `command.c:929-968` and `:1187-1195`. _(Fixed `8aedb937f1` — capped `nbytes <= COMMAND_READ_NBYTES_MAX` (16 KiB) before the multiplication can wrap, plus NULL-check on each malloc.)_
- ❌ **HIGH — Heap-buffer-overflow class in HTTP failure loggers.** _(Verified resolved-stale 2026-04-25 while triaging Bundle 16. Current `s3_log_http_failure` (s3.c:788-799) uses the safe `%.*s` length-bounded printf form, with an explicit comment documenting why `data->data[data->len]=0` is a one-byte heap overflow. The reviewer's reference to s3.c:1633-1640 is also safe in current code: the multipart-initiate path malloc's `data->len + 1`, memcpy's `data->len` bytes, and writes the terminator on the **newly-allocated** buffer (`response_xml[data->len] = '\0'`), not on `data->data`. A `net_http_data_to_cstring` helper would still be valuable defence-in-depth (the antipattern is easy to re-introduce) but no concrete site to fix today.)_

### 🛡 Tier 2 — hardening sweep (correctness, not exploitability)

- ✅ **HIGH — `find_change` / `find_same` unbounded walk in rewind compressor.** _(Fixed `088289d088` — added `max` parameter to both helpers; SSE2 path checks `a128 + 1 <= a_end` per chunk and falls through to a scalar tail; scalar / `NO_UNALIGNED_MEM` paths gain `a < a_end` guards on every walk loop. Caller passes `num16s` (the remaining unconsumed u16 count). Outer-caller's `if (skip >= num16s) break` still works correctly since the bounded helpers return at most `num16s`.)_
- ✅ **HIGH — `bsv_movie_load_checkpoint` malloc-fail invariant break.** _(Fixed `bcb1ad483c` — reordered to malloc first, commit `cur_save_size` only on success. OOM now aborts the checkpoint cleanly via the existing `exit:` label and sets `BSV_FLAG_MOVIE_END`.)_
- ✅ **HIGH — Configuration zombie settings.** `game_ai_override_p1`, `game_ai_override_p2`, `game_ai_show_debug`. _(Fixed `9bf01aabdb` — added three `SETTING_BOOL` lines under the existing `#ifdef HAVE_GAME_AI` block. Toggling in the menu now persists across restart.)_
- ❌ **HIGH — Configuration save: no lock on settings_t while iterating.** _(Verified non-bug — investigated 2026-04-25 while preparing Bundle 12. The autoconfig worker thread (`input_autoconfigure_connect_handler`) only mutates the per-task `autoconfig_handle`; all settings_t writes (including `input_joypad_index[]`, `input_config_set_device_*`, and the `input_autoconf_binds` table) happen in `cb_input_autoconfigure_connect` — the **callback**, dispatched from `retro_task_internal_gather` on the main thread. `config_save_file` is also main-thread (called from menu/CLI). Both paths are serialized by the main loop; no race is possible. Roadmap entry stayed open from a previous static-analysis pattern match that didn't track callback-vs-handler dispatch.)_
- ✅ **HIGH — `rgui_show_start_screen` triad mismatch.** _(Fixed `9bf01aabdb` — `SETTING_BOOL` line now uses `default_enable=true, default=DEFAULT_MENU_SHOW_START_SCREEN`. The bespoke "force-write in minimal mode" workaround at the saver site was deleted -- both load and save paths now agree without needing the override.)_
- 🚧 **HIGH — Menu drivers gate setting registration by current driver and never refresh on driver swap.** `menu/menu_setting.c:18806, 19016, 19308, 19552, 19833, 20330, 20533, 20804, 20857, 20990` all use `string_is_equal(menu_ident, "xmb")` style gating. After the user changes menu driver mid-session, the setting tree stays from the original driver until restart — XMB-only, ozone-only, materialui-only settings vanish from the UI. Either rebuild the tree on swap or move the gating to a display-time predicate. _(Spec drafted 2026-04-27 → [`docs/private/specs/2026-04-27-menu-driver-swap-design.md`](specs/2026-04-27-menu-driver-swap-design.md). Defers to runloop flag + tree rebuild + snapshot-based fallback on init failure; v1 leaves the 20+ gating sites in place and rebuilds the tree against new `menu_driver` value. ~1-2 days.)_
- ✅ **HIGH — Menu init NULL-deref on driver_data.** _(Fixed `bcb1ad483c` — wrapped the post-init field writes in an `if (driver_data)` guard. The line 4543 NULL-check now actually does its job.)_
- ✅ **HIGH — Audio init early-return paths leak buffers.** _(Fixed `bcb1ad483c` — replaced each of the four `return false` paths with `goto error` so the existing `error:` label calls `audio_driver_deinit()` and reclaims the rewind / out_conv / audio_buf allocations.)_
- ✅ **HIGH — `task_screenshot` task_init NULL-deref.** _(Fixed `9f7b59bcc1` — added NULL-guard that frees the state buffer + state struct before returning false on OOM, matching the existing fail-path cleanup pattern.)_
- ✅ **HIGH — `task_overlay_handler` leaks loader allocations on `data` calloc OOM.** _(Fixed `9f7b59bcc1` — promoted the OOM failure to `RETRO_TASK_FLG_CANCELLED` before returning. `task_overlay_free` then reclaims `overlay_path` / `image_list` / `overlays` via the existing CANCELLED branch.)_
- ✅ **HIGH — libretro env callbacks deref `data` without NULL guard.** `runloop.c`. _(Fixed `46855caa12` — added `if (!data) return false;` to all eight cases that dereferenced data: `SET_SUBSYSTEM_INFO` in both dispatchers (system-info probe + main runloop), `SET_VARIABLES`, `SET_MESSAGE`, `SET_MESSAGE_EXT` (caught while in the file — same class), `SET_INPUT_DESCRIPTORS`, `SET_FRAME_TIME_CALLBACK`, `SET_CONTROLLER_INFO`, `GET_LANGUAGE`. Pure NULL guard; no behavioural change on the well-formed-data path.)_
- ✅ **HIGH — `RETRO_ENVIRONMENT_GET_LANGUAGE` returns true with `*data` unwritten when `HAVE_LANGEXTRA` is undefined.** `runloop.c:1977-1985`. _(Fixed `46855caa12` — `#else *(unsigned*)data = RETRO_LANGUAGE_ENGLISH;` so cores get a defined value rather than reading uninitialised stack. Bundled with the NULL-guard sweep above since same case body.)_
- ✅ **MEDIUM — Cloud sync no Content-Length verification.** `webdav.c::webdav_read_cb`, `google_drive.c::gdrive_read_download_cb`, `s3.c::s3_read_cb`. _(Fixed `c2818d5cfd` — added a per-driver `*_verify_content_length` helper that walks `data->headers`, parses `Content-Length`, and compares against `data->len`; `Transfer-Encoding: chunked` is accepted because net_http.c already requires a complete zero-length chunk to clear `P_BODY_CHUNKLEN`. Anything else is rejected (T_FULL responses are silently truncated by net_http.c on connection close, so a mid-stream drop would otherwise look like a "successful" 200 with a partial body and corrupt the local save when combined with the no-atomic-rename pattern in cross-cutting #1). Failed verification flips `success` to false so the caller sees "remote error" rather than the success=true,file=NULL "remote-doesn't-have-file" 404 contract.)_
- ✅ **MEDIUM — WebDAV 404 response body written to local file.** `webdav.c::webdav_read_cb`. _(Fixed `72b2a839f8` — added `data->status != 404` guard around the `filestream_write` block. 404 still completes the cb with `success=true` per the cloud-sync contract (remote-doesn't-have-this-file means "nothing to sync"), but no longer overwrites the local save with the server's HTML error body.)_
- ✅ **MEDIUM — `s3_url_encode` doesn't encode `?` `&` `=` for path components.** `s3.c:419-441`. _(Fixed `e3d7394951` — removed `?`/`&`/`=` from the unreserved set; only `/` (path-segment delimiter) plus the RFC 3986 unreserved chars stay literal. The function is path-only — query strings go through `s3_url_encode_query_value`. Stops object keys with literal `?` from being mis-parsed as path/query boundary by `s3_build_canonical_uri_from_url`.)_
- ✅ **MEDIUM — `s3_canonicalize_query_string` returns input verbatim.** `s3.c:445-459`. _(Fixed `e3d7394951` — implemented per AWS SigV4 spec: tokenise on `&`, `qsort` by `strcmp` (the '=' / value acts as natural tiebreaker since callers pre-encode names and values), re-join with `&`. The previous TODO worked by accident because every existing caller passed already-alphabetical parameters; the next caller to pass two parameters out of order would have produced silent SigV4 mismatch / HTTP 403.)_
- ✅ **MEDIUM — `s3_update` reads entire file into RAM.** `s3.c:1838`. 5GB save state on a console with 256MB → OOM. Even on desktop: GB-scale uploads block the loop. _(Phase 1 fixed `c3ebdb11e4` Bundle 32 — `cloud_sync_max_upload_mb` setting (default 2048 MiB matches AWS S3 single-PUT ceiling, 0 = unlimited) with cap check at the `cloud_sync_update` dispatch layer (one site, serves s3 + webdav + google_drive). Closes the OOM reachability; an unbounded `malloc(file_size)` is no longer reachable from the cloud_sync entry. Phases 2-4 (streaming SHA-256, pull-callback HTTP body, multipart audit) per [the spec](specs/2026-04-27-cloud-sync-streaming-upload-design.md) remain pending — they're throughput optimisations, not correctness fixes, and are independently shippable.)_
- ✅ **MEDIUM — Netplay LAN-discovery memory exhaustion.** `network/netplay/netplay_frontend.c:386-402`. _(Fixed `9ed554ae3b` — `DISCOVERY_HOSTS_MAX = 256` defined next to the discovery magics; the size-equals-allocated grow branch now `continue`s past the cap rather than realloc'ing further. 256 chosen as well above any realistic LAN-game roster and well below the realloc-failure cliff on memory-constrained targets. Hostile shared-WiFi peer can no longer drive `discovered_hosts.allocated` unbounded with forged RANS responses.)_
- ✅ **MEDIUM — `task_database_cue` allocates from attacker-controlled CUE size.** `tasks/task_database_cue.c:1518-1523`. _(Fixed `9ed554ae3b` — three guards added in the in-memory branch of `intfstream_file_get_serial`: reject `size <= 0` (was producing a `(size_t)`-cast wrap to huge alloc on negative CUE values), reject `offset > file_size` up front (would have silently short-read), and clamp `size = MIN(file_size - offset, 64 MiB)`. The 64 MiB ceiling is well above what `intfstream_get_serial` actually inspects (volume descriptor at 32 KiB plus magic-byte windows in the first MB) and well below the OOM-killer threshold on 256 MB-class consoles.)_
- ✅ **MEDIUM — Heap overflow waiting in `configuration.c` static pools.** _(Fixed `9bf01aabdb` — added `assert(count <= SETTINGS_*_COUNT_MAX)` at the exit of every `populate_settings_*` function (bool/int/uint/float/size/array/path). The 514th `SETTING_BOOL` now produces an immediate assert in debug builds; release builds compile out the check but the invariant is documented in code.)_

### 🏗 Tier 3 — structural / cleanup

- ✅ **MENU — ~30 unguarded `MENU_LIST_GET_SELECTION(...)->size` derefs.** Already in audit roadmap as **S2 (cheap-to-spec)**. Indie-review confirmed and expanded across `menu/cbs/menu_cbs_sublabel.c:1946`, `menu/cbs/menu_cbs_right.c:223`, `menu/menu_driver.c:7561`, ozone.c (5 sites), xmb.c (9 sites), materialui.c (4 listed sites), rgui.c (4 listed sites). _(Closed across Bundle 34 (`335bb31cf9` + `382ee9f83f` debt-sweep follow-up: cbs + ozone + xmb), Bundle 36 (`44e71ead14`: materialui + rgui — 7 unguarded sites; the remaining 2 `materialui.c:3349, 12135` were verified-already-guarded — the deref site already had `if (!list) return` after the assignment), and Bundle 55 (`3537f96a38`: 5 sites cppcheck flagged on the post-Bundle-54 re-run that the original grep missed — `ozone.c:4767/8009/11770`, `xmb.c:1626`, `materialui.c:10050` — the Bundle 34/36 grep keyed on inline `MENU_LIST_GET_SELECTION(...)->size` and on `selection_buf` only; the missed sites either spread the assign-then-deref across more lines or used `menu_stack` (the other macro the contract covers) or `->list[idx].userdata` (the deref shape the grep didn't key on)). The `menu_driver.c:7561` line was also verified-already-guarded — the same `menu_list ?` predicate gates the deref and the surrounding code already checks `selection_buf_size > 0` before any `selection_buf` access. Macro contract comment + path-sensitive proof reference live at `menu/menu_driver.h:72-78`. cppcheck cluster cleared to zero on the four large menu files post-fix.)_
- ✅ **MENU — Dead vtable slots.** `set_thumbnail_content` (declared at `menu_driver.h:411` pre-strip, dispatched nowhere — every call used the local static), `update_thumbnail_path` (dispatched at `menu_cbs_scan.c:164-170`, every driver registered NULL → dispatch was dead), `list_prepend` (no driver filled, no caller dispatched), `navigation_increment` / `navigation_decrement` (dispatched 4× in `menu_driver.c`, every driver NULL). _(Bundle 46 — fixed in `6f59dc0558` on `local/fixes-2026-04`. Stripped 5 slot declarations from `menu_ctx_driver_t` (header) + 25 entries from the 5 driver vtable initialisers (ozone/xmb/rgui/materialui/null) + the 5 dead dispatch sites (4 nav in `menu_driver.c`, 1 update_thumbnail_path block in `menu_cbs_scan.c`). Static functions `ozone_set_thumbnail_content` and `xmb_set_thumbnail_content` retained — they are called directly inside their own files. Trimmed 2 stale function-name lines from a doc-comment in `gfx/gfx_thumbnail.h` since the wrapper functions they named (`menu_driver_set_thumbnail_content`, `menu_driver_update_thumbnail_path`) had been removed long ago. Net -59 lines across 8 files. Build clean.)_
- 📋 **MENU — `materialui.c` is 12 231 lines with author-marked module boundaries.** Themes (~1200 lines), 5 view-types' compute/render triples, navigation, gestures. Author already drew the boundaries with banner comments. Same applies to `xmb.c` and `ozone.c`.
- 📋 **MENU — Per-driver gating in `menu_displaylist.c` is the coupling smell.** ~30 `string_is_equal(menu_ident, "...")` branches across menu_displaylist/menu_setting/menu_cbs_*. A capability-flag set on `menu_ctx_driver_t` (`flags & MENU_DRV_HAS_SIDEBAR_TABS`) would let each driver self-describe.
- ✅ **MENU — `materialui` ident is `"glui"` but file/struct is `materialui_*`.** UI labels say "MaterialUI". Three-way naming inconsistency means a `string_is_equal(menu_ident, "materialui")` check anywhere is a silent bug. (Recurrence from audit S11 — same class.) _(Bundle 49 — closed `0a6c471d46` on `local/fixes-2026-04`. Verified-no-live-mismatch: all 11 `string_is_equal(menu_ident, ...)` callsites in the codebase use `"glui"`, none use `"materialui"` (`menu/cbs/menu_cbs_title.c:199, 232`; `menu/menu_displaylist.c:4160`; `menu/cbs/menu_cbs_sublabel.c:157, 3730, 3759, 5320, 5350`; `menu/menu_setting.c:20356, 20886, 20901`). Renaming the ident is rejected — saved `retroarch.cfg` files reference the driver by `"glui"` and a rename would break existing user setups. Locked in with a 6-line invariant comment block above `menu_ctx_mui`'s definition at `menu/drivers/materialui.c:12196` calling out the intentional inconsistency and naming `"glui"` as the only valid ident-string for any future check on this driver.)_
- ✅ **DRIVER PATTERN — Four near-identical `*_driver_find_driver` implementations.** Audio:429-461, input:5044-5077, menu:4620-4648, video:3012-3034. ~120 lines of duplication that boils down to one `static bool generic_driver_select(label, prefix, &state_field, drivers[], verbosity)`. The shared helper already exists (`driver_find_index` in `retroarch.c:1262`); the wrappers are the duplication. _(Bundle 53 — closed `f980df5d63` on `local/fixes-2026-04`. Survey at fix-time found nine sites with the same canonical "Couldn't find any X / available are: ... / default to first" RARCH_ERR/RARCH_LOG_OUTPUT/RARCH_WARN block, not four — the indie-review note had only listed the four largest. Hoisted the failure-path log block into `driver_log_unknown(label, prefix, drv, verbosity_enabled)` in `retroarch.c` (declaration in `driver.h`) and reused the existing `find_driver_nonempty()` registry to enumerate idents — no need for each call site to know the concrete `driver_t` type. Updated all nine callers: audio, microphone, input, video, menu, location, camera, bluetooth, wifi, record (the `*_driver_find_driver` pattern minus cloud_sync, which keeps its own variant since its fallback target is `"null"` rather than first-driver, plus an extra "only log if user named one" guard). Format normalized at the same time: `menu_driver_find_driver`'s "couldn't find" went from RARCH_WARN to RARCH_ERR (intentional severity bump to match the other 9 subsystems); `menu` and `microphone` lost a stray trailing period; `microphone` lost a redundant inner null-check inside a loop already guarded by the same expression. No control-flow change at any call site — assignment, fallback, and return-value behavior preserved verbatim. Net diff +70/-124 across 10 files (audio_driver, bluetooth_driver, camera_driver, driver.h, video_driver, input_driver, menu_driver, wifi_driver, record_driver, retroarch). Build clean, binary smoke-tests OK. Single source of truth for the format if it needs to change again.)_
- 📋 **DRIVER PATTERN — Three different "null driver" philosophies in one repo.** `audio_null` is all-NULL (relies on every callsite NULL-checking); `input_null`/`video_null`/`menu_ctx_null` stub everything (removes that whole bug class). Pick one — preferably stub-everything — and apply it. Removes a class of "did the caller remember to NULL-check?" bugs by construction.
- 📋 **CONFIG — Boilerplate sprawl: 26 030 lines `menu_setting.c` + 7 725 lines `configuration.c` for a 4-touchpoint contract.** A table-driven approach (already partially used at `menu_setting.c:11608-11671` for bool entries) would let `populate_settings_*`, the menu list, and the load/save paths share one declarative table per type. ~3000 lines of duplication and the entire H1/H4 zombie/drift class go away by construction.
- ✅ **TASKS — `task_queue.c` lock-naming inconsistency.** `title`/`error`/`progress`/`flags` use `property_lock`; `task_data` uses `running_lock`. Asymmetric and undocumented. Pick one. _(Bundle 80 — closed `5729e8a83c` on `local/fixes-2026-04`. Documentation, not rename. Verified the asymmetry is by design: `task_data` is guarded by `running_lock` because its validity is tied to the task's membership in the running set — the worker publishes it via `task_set_data`, the gather/callback path at `task_queue.c:199` consumes it directly — whereas `title`/`error`/`progress`/`flags` are display properties touched by the UI/render thread and so use `property_lock`. Added a lock-contract block at the lock declarations (`task_queue.c:75`) naming what each of the four locks guards plus the `running_lock → queue_lock` / `running_lock → property_lock` nesting order (verified against `retro_task_threaded_push_running:410` and `retro_task_threaded_gather:440`). Renaming `running_lock`→`property_lock` at the two `task_data` sites was rejected — it would alter lock-ordering with zero test coverage to catch a resulting deadlock. Comment-only; `task_queue.o` rebuilds clean.)_
- ✅ **TASKS — `task_queue_push_progress` invokes `msg_push` callback while holding `property_lock`.** `task_queue.c:106-141`. Re-entrancy hazard if the callback ever calls `task_get_title` etc. — no current implementation does, but the API contract isn't documented. _(Bundle 80 — closed `5729e8a83c` on `local/fixes-2026-04`. Documented the contract inline at the `slock_lock(property_lock)` site: `property_lock` is held across BOTH the `msg_push` callback and the `task->progress_cb` call; slocks are non-recursive, so neither callback may invoke any `task_get_`/`task_set_` accessor (they re-acquire `property_lock` → self-deadlock). Confirmed no current callback does. Comment-only; no behaviour change.)_
- 📋 **TASKS — `task_http_iterate_transfer` busy-spins with `retro_sleep(1)`.** `tasks/task_http.c:113-114`. Acknowledged FIXME. Switch to event-driven `select`/`poll` on the underlying socket.
- 📋 **BUILD — `make <path>.o` (without OBJDIR prefix) misses `-DHAVE_CONFIG_H`, causing per-file builds to fail on HAVE_*-gated struct members.** Discovered during Bundle 66 verification.  Invoking `make menu/drivers/xmb.o` from the repo root falls through to GNU make's implicit `.c → .o` pattern rule because the explicit Makefile rule at line 254 keys on `$(OBJDIR)/%.o: %.c config.h config.mk` — only the OBJDIR-prefixed path matches.  The implicit rule doesn't include `$(DEFINES)`, so `-DHAVE_CONFIG_H` is missing, the `#ifdef HAVE_CONFIG_H #include "../../config.h"` block in each source file is dead, every `HAVE_*` symbol evaluates undefined, and references to gated struct fields like `settings->ints.menu_xmb_title_margin` (declared in `configuration.h:154` inside `#ifdef HAVE_XMB`) and `menu_st->input_dialog_kb_label` surface as `'struct <anonymous>' has no member named '...'` compile errors that look like real source bugs.  Workaround: always use the OBJDIR-prefixed path, e.g. `make obj-unix/release/menu/drivers/xmb.o`.  **Fix options:** (a) add a thin shim rule `%.o: $(OBJDIR)/%.o ; @true` so the non-prefixed path delegates to the explicit rule; (b) globally export `CPPFLAGS += -DHAVE_CONFIG_H` so even the implicit rule picks it up; (c) drop the `#ifdef HAVE_CONFIG_H` guard from sources and unconditionally include `config.h` (configure always generates it).  Recommend (a) — smallest blast radius, makes the common developer mistake just work without changing the macro-gating contract anywhere else.  Surfaced when a Bundle-66 sanity build using the wrong path emitted confusing pre-existing-looking errors that took ~20 minutes to diagnose as a build-invocation problem rather than a source bug — the failure mode is hostile to new contributors.  Cluster too narrow for AUDIT-POLICY (one Makefile rule).
- ✅ **BUILD — `network/cloud_sync/s3.c` is half-landed: source + struct fields + UI present, but never wired into the build.** _(Bundle 63 — wired in `ff483a6362` on `local/audit-2026-04`: `--enable-s3` qb flag in `config.params.sh` + `check_enabled NETWORKING S3` in `config.libs.sh` + `ifeq ($(HAVE_S3),1)` block in `Makefile.common` HAVE_CLOUDSYNC section.  `s3.c` compiles + links + binary boots; one undersized `credential[256]` buffer bumped to `[512]` during first real build.  Option (a) "SigV4 round-trip smoke test against a localhost minio container" remains a follow-up.)_ Discovered while verifying Bundle 56 — `make obj-unix/release/network/cloud_sync/s3.o` fails with `'struct <anonymous>' has no member named 'access_key_id'` at `s3.c:1834-1836`. Root cause: the source code references `settings->arrays.{s3_url,access_key_id,secret_access_key}` (declared in `configuration.h:554-558` only when `HAVE_S3` is defined) but **no build path defines `HAVE_S3`** — `qb/` has no `--enable-s3` / `check_lib HAVE_S3` invocation, `Makefile.common:2390-2401` never appends `network/cloud_sync/s3.o` to `OBJ` under any condition, and `config.h`/`config.mk` never set `HAVE_S3=1`. The plumbing on the consumer side IS correctly gated — `configuration.c:1642-1646` load/save block sits inside `#ifdef HAVE_S3`, `menu/menu_setting.c:12395-...` UI block sits inside `#ifdef HAVE_S3`, the struct fields sit inside nested `HAVE_NETWORKING / HAVE_CLOUDSYNC / HAVE_S3` blocks. So the feature compiles cleanly with HAVE_S3 undefined (default) because every consumer of those fields is gated. The only thing missing is the last mile: declare an `--enable-s3` qb option, add `ifeq ($(HAVE_S3), 1) OBJ += network/cloud_sync/s3.o` inside the existing `HAVE_CLOUDSYNC` block at `Makefile.common:2390`, set `HAVE_S3` to `auto`-detect a libcrypto/openssl dependency (or just gate behind `HAVE_SSL` since SigV4 needs SHA-256). Upstream provenance: `c7bf80fb72 S3 Implementation` introduced the file; subsequent upstream commits refined parsing/multipart/sigV4 but never wired in the build. Fork-side this file has been touched by Bundle 17 (`e3d7394951` SigV4 contract), Bundle 26 (`c2818d5cfd` Content-Length), Bundle 32 (`c3ebdb11e4` upload cap), Bundle 52 (`b510d9d654` atomic write), Bundle 56 (`78729e2ca6` dead-clause) — none of those fixes have actually been compile-tested against a real build. Two options for resolution: **(a) wire it in** — three lines in `Makefile.common`, one stanza in `qb/qb.libs.sh`, write a short SigV4-roundtrip smoke test against a localhost minio container; **(b) move it out** — relocate `s3.c` under `pkg/` or delete it and pull from `git log` when an integrator wants it. Recommend (a) — the file has accreted 5 bundles of fork-side fixes; the work to compile-verify them is smaller than the work to keep applying analyzer findings to source nobody links. Cluster too narrow for AUDIT-POLICY (one file, build-system change).
- ✅ **TIDY — `menu/drivers/materialui.c` style noise (67 sites, surfaced by Bundle 62 cppcheck re-run).** Cluster of pre-existing style-level findings on the 12 231-LoC menu driver that came in alongside the 3 actionable warnings Bundle 62 closed. None are bugs; all would be one mechanical sweep through the file. **Breakdown:** 31× `constVariablePointer` (pointer locals that never reassign — should be `const`); 13× `shadowVariable` (inner-scope decl shadows an outer one — rename inner); 13× `constParameterCallback` (callback fn params that don't write the pointee — should be `const T*`); 9× `constParameterPointer` (non-callback fn params, same shape); 3× `variableScope` (`label_x` at :5102, `sys_bar_clock_width` at :6496, `color_white` at :8158 — declared at outer function scope, only read inside an inner `if`-block); 1× `unreadVariable` (`sys_bar_clock_width` at :6496 — its `= 0` initialiser is dead since both readers sit inside the same if-guarded block that unconditionally re-assigns it on entry). Why deferred from Bundle 62: each fix touches code the bundle didn't otherwise need to read, and the same shapes are likely strewn across `ozone.c` / `xmb.c` / `rgui.c` too — a per-file tidy sweep is the wrong granularity. Right approach: cppcheck full-tree run with `--enable=style` only, aggregate the four `constParameter*` IDs and the `shadowVariable` ID across all menu drivers, fix as one cluster. Cluster too narrow at present (one file, style-only) for an AUDIT-POLICY entry. _(Partial — Bundle 81, `8f7145637e` on `local/fixes-2026-04`: cleared the **48 caller-safe findings on `materialui.c`** — 31× constVariablePointer, 13× shadowVariable (12 were redundant inner re-fetches of a singleton already held by an outer var → inner decl removed; the 13th, loop counter `i` in `materialui_list_insert`, renamed `i`→`user_idx`), 3× variableScope (`label_x` / `color_white` / `sys_bar_clock_width` moved into the inner block that uses them — `color_white`'s host `if` gains braces), 1× unreadVariable (`sys_bar_clock_width`'s dead `= 0` init drops out with the scope move). Verified: clean `obj-unix/release/menu/drivers/materialui.o` build + style cppcheck re-run = 0 of the 48 cleared findings remain, only the deferred classes left. **Still open on `materialui.c`:** 10× constParameterCallback (deferred by design — these params feed menu-vtable function-pointer tables; const-qualifying the param changes the function type and breaks the table assignment unless the typedef also gains const — high blast radius, low value) + 9× constParameterPointer. **Still open tree-wide:** the same cluster across `ozone.c` (~59), `xmb.c` (~48), `rgui.c` (~47) — the "aggregate across all menu drivers" arm. Note: cppcheck 2.20 now reports 10× constParameterCallback here, not the 13 logged from the Bundle 62 run.)_
  Resolved (Bundle 82, `6f318c19e3` on `local/fixes-2026-04`): materialui.c portion complete. 20 const additions (12× constParameterPointer + 8× constVariablePointer surfaced by the const-propagation cascade, run to convergence over 4 rounds — each round recompiled clean, exit 0) + 14× constParameterCallback confirmed FPs suppressed in `.cppcheck-suppress.txt` (S20 — shared-fnptr/vtable signature constraint; const-ifying one impl would diverge it from siblings on the same function pointer). `cppcheck --enable=style --suppressions-list=.cppcheck-suppress.txt` now reports zero constParameter*/constVariablePointer/shadowVariable/variableScope/unreadVariable findings on the file. NOTE: the body's cross-driver suggestion (ozone/xmb/rgui constParameter* arm) was a forward-looking note, not part of this item's materialui.c-scoped 67 sites — run a tree-wide `--enable=style` pass to scope it as a separate bundle if wanted.
  Progress (2026-06-30, Bundle 84 / fixes 55f0df054e): the caller-safe
  tree-wide arm advanced to `ozone.c` — 45 sites cleared (27
  constVariablePointer, 12 shadowVariable, 6 variableScope), same shape
  as Bundle 81's materialui.c pass. shadowVariable detail: 3× _len
  word-wrap accumulators reuse the function-scope _len (dead after last
  read); the IS_PLAYLISTS_TAB random-selection block's menu_st/selection/
  selection_total/new_selection reuse the identical function-scope vars
  (its inner selection_total already assumed entries.list non-null ⇒
  selection list present, matching the outer); IS_PLAYLIST/EXPLORE
  new_selection reuses outer scratch; MENU_ACTION_DOWN/UP sidebar
  selection (a category index, not the entry selection) renamed
  sidebar_selection; messagebox per-line width renamed line_width; OSK
  menu_st redundant re-fetch removed. variableScope: left_thumbnail_
  alignment/_y_position moved into if (show_left_thumbnail); ozone_
  selection_changed's selection into the FILE_LIST branch; ozone_context_
  reset's two static texture tables + i into if (ozone). Verified: clean
  ozone.o build (no -Wdiscarded-qualifiers), style cppcheck re-run = 0
  constVariablePointer/shadowVariable/variableScope on ozone.c, full
  make -j retroarch LD clean (20.8 MiB), ./retroarch --version boots
  (1.22.2). Still open on `ozone.c`: 13× constParameterPointer + 3×
  constParameterCallback (ozone_list_push vtable) — the signature-
  touching arm deferred as on materialui per Bundle 81/82 — plus the
  pre-existing Bundle-65 deferred set (knownConditionTrueFalse,
  unusedStructMember, duplicateCondition). Still open tree-wide: `xmb.c`
  (~48), `rgui.c` (~47).
  Progress (2026-07-01, Bundle 85 / fixes f710e8c6be): the caller-safe
  tree-wide arm advanced to `xmb.c` — 37 sites cleared (16
  constVariablePointer, 18 shadowVariable, 3 variableScope), same shape
  as Bundle 81/84. shadowVariable detail: the `xmb_render` touch/drag
  path re-caches the function-scope pointers — menu_st (process
  singleton), menu_list / selection_buf (provably equal to the outer at
  the decl point, no list swap before them in their branch) — so those
  inner decls were removed; current_menu_icon in both thumbnail blocks
  and the pending-load config reads (playlist / upscale-threshold /
  network-on-demand) likewise reuse the outer. Renamed where the value
  genuinely differs: the drag handlers write `menu_st->selection_ptr`
  mid-frame (vertical-drag path), so xmb_render's inner `selection`
  re-fetches are necessary — renamed pointer_selection (pointer block)
  and pending_selection (pending-thumbnail block); the xmb_frame battery
  powerstate buffer renamed battery_msg. Two more pure removals:
  xmb_parse_menu_entry_action's MENU_ACTION_SCAN menu_st re-fetch, and
  xmb_frame's show_icon_thumbnail selection (selection_ptr unwritten
  between the outer assign and the inner use). variableScope: messagebox
  per-line width into the `if (*start)` block; xmb_init_scale_mod's
  counter i into its inner block; xmb_init_ribbon's col into the inner
  for-loop body. Deferred (documented): the 4 thumbnail-float
  variableScope findings (scaled_thumb_height / thumb_x in both
  left/right thumbnail blocks) — an interdependent float group whose
  partial scope-split would have to drag in the unflagged sibling
  thumb_y and would read less idiomatically than its mirror block.
  Verified: clean xmb.o build (no -Wdiscarded-qualifiers), style
  cppcheck re-run = 0 constVariablePointer/shadowVariable on xmb.c (only
  the 4 deferred thumbnail variableScope remain), full make -j retroarch
  LD clean (20.8 MiB), ./retroarch --version boots (1.22.2). Still open
  on `xmb.c`: 9× constParameterPointer (signature-touching arm, deferred
  as on materialui/ozone per Bundle 81/82) + the pre-existing deferred
  set (knownConditionTrueFalse, duplicateExpressionTernary,
  duplicateCondition). Still open tree-wide: `rgui.c` (~47).
  Progress (2026-07-01, Bundle 86 / fixes dc2e9d2dfc): the caller-safe
  tree-wide arm reached its last file, `rgui.c` — 32 sites cleared, 12
  deferred, closing the "aggregate across all menu drivers" arm
  (materialui/ozone/xmb/rgui all done). constVariablePointer (15) +
  constVariable (2): pointer/array locals never written-through marked
  const — src, background_buf, rgui (×3), p_disp (×2), menu_st,
  custom_vp, runloop_st, and the two rain `weights[]` lookup tables. The
  scanline-fill thickness block flagged only src_d; consted its src_a/
  src_b/src_c siblings too for group consistency (all four are read-only
  memcpy sources) — noted in the commit. Every const compiler-verified
  (zero -Wdiscarded-qualifiers), no false positives. shadowVariable (2):
  removed the redundant inner `menu_st` re-fetch in rgui_render's
  fs-thumbnail title block and rgui_pointer_up's select block — both
  reuse the outer menu_state_get_ptr() singleton (pointer never
  reassigned; the select-block write to `menu_st->selection_ptr` now
  binds to the outer). variableScope (13 of 25): a4 into `if (a3 > 0)`;
  fs- and mini-thumbnail `src`/`dst` declared inside their memcpy loops
  (one move clears both the scope and const findings); blit-line-regular
  and -shadow glyph counters `i`/`j` into the `symbol != ' '` block; osk
  `title_x`/`title_width` into the inner label `if`; upscale `x_src`/
  `y_src` into their nearest for-body. Deferred (12): the particle-effect
  render cluster (snow/rain/vortex/starfield, ~2073-2239) — loop
  temporaries the file deliberately hoists to case-block top, and
  `on_screen` already carries a prior-pass rationale comment for keeping
  block scope; left as a coherent group, mirroring xmb.c's
  interdependent-group deferral. Verified: clean rgui.o build (no
  warnings), style cppcheck re-run = 0 constVariablePointer/constVariable/
  shadowVariable on rgui.c (only the 12 deferred particle-cluster
  variableScope remain), full make -j retroarch LD clean (20 MiB),
  ./retroarch --version boots (1.22.2). Still open on `rgui.c`: 10×
  constParameterPointer + 1× constParameterCallback (signature-touching
  arm, deferred as on materialui/ozone/xmb) + the pre-existing deferred
  set (knownConditionTrueFalse, autoVariables). Tree-wide caller-safe
  arm now complete across all four menu drivers.
- ✅ **TIDY — clang-tidy `bugprone-integer-division` in `menu/drivers/materialui.c` (5 sites, surfaced by Bundle 62).** _(Bundle 64 — widened tree-wide to 51 sites across 8 files (materialui 5, ozone 26, xmb 10, gfx_widgets 2, gfx_display 4, gfx/widgets/leaderboard 1, gfx/widgets/screenshot 2, gfx/widgets/volume 1) and closed in `2137199165` on `local/fixes-2026-04`. Fix shape: integer-literal divisor → float literal (`/ 2` → `/ 2.0f`, also `/ 4`, `/ 6`, `/ 8`, `/ 12`) — division happens in float space, ½-px centring drift gone wherever result feeds a float coord. Behaviour-preserving in int-returning contexts. clang-tidy on the 8 files: 0 warnings post-Bundle. `make -j4 retroarch` clean, binary runs.)_
- 📋 **TIDY — clang-tidy `readability-misleading-indentation` at `menu/drivers/materialui.c:12086`.** The `break;` at the end of a switch-case body whose preceding statement is an unbraced `else materialui_set_node_playlist_icon(...);` (line 12082-12083). clang-tidy reads the `break;`'s indent against the unbraced `else` body and flags as misleading. **Likely FP per project style** — `CODING-GUIDELINES` (referenced from CLAUDE.md) explicitly mandates "No braces for single-statement blocks", and the `break;` is correctly indented relative to its enclosing `case`, not its preceding `else`. Right resolution is either an inline `// NOLINTNEXTLINE(readability-misleading-indentation)` with a one-line comment naming the no-brace style, OR a project-level `.clang-tidy` disabling the check (since the style guide makes it unactionable codebase-wide). Defer until clang-tidy `readability-*` is brought into the audit checks list — currently the audit-config only enables `bugprone-*` + `clang-analyzer-*`, so this never lights up under the audit pipeline (only under interactive clangd).
  Note (2026-06-30, Bundle 83): site line drifted — the unbraced-else-then-break pattern is now at materialui.c:12071-12075, not :12086 (:12086 is now an unrelated `size_t _len` decl in materialui_list_clear). Resolution constraint clarified: the suggested inline NOLINTNEXTLINE is NOT usable here — it is a //-style line comment, which this codebase's C89 rule (no //-only comments) forbids, and clang-tidy honours only //-style NOLINT. The sole clean fix is a repo-root .clang-tidy disabling readability-misleading-indentation tree-wide — a broad change for a check not in the audit pipeline (only bugprone-* + clang-analyzer-* run). Kept deferred per the bullet's own precondition (until readability-* enters the audit checks list).
- 📋 **TIDY — clangd `unused-includes` sweep (initial seed: 4 sites in `menu/drivers/materialui.c`).** Surfaced opportunistically by Bundle 62 — `config.h`, `frontend_driver.h`, `core_info.h`, `input_osk.h` all `#include`d at the top of `materialui.c` (lines 37/40/50/54) but no symbol from any of them is referenced in the file. Risk class: every `#include` is a build-time tax (header tree walk, dep-rebuild fanout) plus a hidden coupling (renaming a header forces a touch on every file that includes it). Same pattern is almost certainly present across the menu/ subtree and the wider codebase — RetroArch's headers tend to be transitively pulled in by `runloop.h` and similar so individual `.c` files accumulate over-broad includes. Right approach: run clangd's `unused-includes` check tree-wide (the new `compile_commands.json` from Bundle 61 makes this trivially scriptable), aggregate per-directory, fix as one chore commit per subsystem. Defer until an interested party has the appetite; not a correctness item, but a build-time + readability win. Out of audit-pipeline scope today (the `/audit` skill doesn't surface clangd diagnostics).
  Progress (2026-06-30, Bundle 83 / fixes 25bdb0c4ca): the 4-site materialui.c seed is resolved. 3 unused includes removed — frontend_driver.h, core_info.h, input_osk.h (zero symbol refs each, verified by grep + confirmed by clean recompile). The 4th flagged include, config.h, is KEPT as a verified clangd false positive: HAVE_CONFIG_H-guarded, it supplies the HAVE_* macros gating #ifdef HAVE_AUDIOMIXER / HAVE_CHEEVOS / ... in this file; clangd include-cleaner does not track preprocessor-macro usage, so it misflags config.h (removing it would silently compile out features). Tree-wide unused-includes sweep across menu/ + wider codebase remains deferred — that is the bulk of this item.
- ✅ **STATE — `runahead_save_state_size` cached at create time, never re-queried.** `runahead.c:1216` (runahead_save_state) + `runahead.c:1774` (preempt_run). _(Fixed `e1d376da4c` Bundle 30 — re-query each save call, tear down via existing `runahead_err` / preempt `error:` label if the core's required size has grown beyond the cached buffer. Both paths previously called `retro_serialize(buf, cached_size)` where the core could expect a buffer larger than `cached_size` after mid-run growth (memory-map changes, dynamic cheats, RAM expansion). Reallocation rejected as too expensive in the runahead/preempt hot path; teardown forces clean re-init through the existing `runahead_create`/`preempt_init` paths which is the canonical "size changed" flow.)_
- ✅ **STATE — `runahead` temp-DLL filename is predictable LCG seeded from `time(NULL)`.** `runahead.c::write_file_with_random_name` + `::copy_core_to_temp_file`. _(Fixed `92d8e97cd5` — added `runahead_secure_random_bytes` (BCryptGenRandom / arc4random_buf / glibc-2.25+ getrandom / POSIX /dev/urandom) and `runahead_atomic_write_excl` (O_WRONLY|O_CREAT|O_EXCL|O_NOFOLLOW on POSIX; _O_EXCL|_O_BINARY on Windows). Random-name loop now draws fresh CSPRNG entropy per iteration (the original LCG never advanced its state -- the loop generated the SAME filename 30 times) and hex-encodes 8 bytes -> 16 chars. `copy_core_to_temp_file`'s primary deterministic-name path also switched to the atomic helper, so a hostile pre-plant at `<tmp>/<core_name>` rejects with EEXIST/ELOOP rather than silently following a symlink. The TOCTOU between atomic write and dylib_load is left as a separate item -- closing it requires fd-based dylib loading.)_
- ✅ **CHEEVOS — `rcheevos_filter_url_param` overlapping `strcpy`.** `cheevos_client.c:169`. Already in audit (#4). Confirmed by cloud-sync reviewer. _(Bundle 49 — duplicate of audit High `cheevos/cheevos_client.c:169` already-✅ at the same line above; fix landed in `8aedb937f1` on `local/fixes-2026-04` (Bundle 4 — same commit that hardened the localhost-attacker trust boundary): `strcpy(start, next + 1)` → `memmove(start, next + 1, strlen(next + 1) + 1)`. Indie-review tier-3 entry was a stale cross-reference re-listing the audit-section finding; reconciled here.)_

### 📝 Open questions (where intent is genuinely ambiguous)

These are things the agents flagged where they couldn't tell from the code alone whether the behavior is intentional. Worth asking in libretro discussions or filing as upstream issues:

- Is the network command socket *intended* to bind 0.0.0.0, or is "localhost-only" the assumed default and the AI_PASSIVE bind is an oversight? The setting name `network_cmd_enable` doesn't suggest LAN exposure.
- Is the netplay password feature considered real authentication or a casual gate? If real, the salt + memcmp issues are blockers; if casual, document loudly.
- Is the cloud-sync server expected to be authenticated and trusted, so path traversal is "by design"? README doesn't say.
- Why does `command_write_ram` not take a length bound when `command_write_memory` does?
- The `natt_parse_desc_node` recursion structure is broken — does NAT-PMP discovery actually work today against typical IGDs, or is this code path effectively dead?
- `RETRO_ENVIRONMENT_SET_PROC_ADDRESS_CALLBACK` (cmd 33) is unimplemented — silently returns false via the default case. Is this deliberate (RetroArch chooses not to expose extension hooks)? If so, an explicit case + comment would close the search.
- v0/v1 BSV replay format support — load-and-warn vs reject? Currently silently produces garbage emulation on size mismatch.

### 🧮 Tally

- **8 Critical** (TLS off, network-cmd RCE, write_ram OOB, natt NULL, webdav uninit, plus 3 cross-cutting clusters)
- **23 High**
- **18 Medium**
- **15 Tier-3 structural**
- **7 Open questions** — candidates for upstream issues

---

## 🔬 clang-tidy 2026-04-25

Re-run with `compile_commands.json` (generated via `bear -- make -j$(nproc)` after `./configure`). Checks: `bugprone-*`, `clang-analyzer-core.*`, `clang-analyzer-security.*`, `clang-analyzer-unix.*`. 200 files in audit scope. **125 clang-analyzer findings + ~30 high-signal bugprone**, vs cppcheck's 549 (after filtering). The path-sensitive analyzer caught classes cppcheck missed — particularly array-bound violations and UAF traces.

### 🔥 NEW Critical (not in prior audit/indie-review)

- ✅ **CRITICAL — `tasks/task_patch.c:207, 216, 238, 247` four sites: array access on NULL `target_data`.** IPS/UPS/BPS patch parser. Trust boundary: `.ips`/`.ups`/`.bps` patch files (downloaded from ROM-hack sites). _(Fixed `17cea42c5b` — root cause was a malicious `.bps` declaring `modify_target_size == 0` would skip the realloc branch and leave `bps.target_data` NULL; reject 0-size up front and force fresh malloc when caller passed NULL.)_
- ✅ **CRITICAL — `audio/audio_driver.c:1507` Out-of-bound write past `audio_driver_st.mixer_streams`.** _(Fixed `6423760b01` — bounded `params->slot_selection_idx` to `< AUDIO_MIXER_MAX_SYSTEM_STREAMS` in the manual-slot path. Caller-supplied index from libretro mixer-add env callback was previously unchecked.)_
- ✅ **CRITICAL — `core_option_manager.c:1164` Out-of-bound heap access.** _(Fixed `6423760b01` — the categories array was sized by `_len` (options count) but indexed by `cats_size` (categories count). Allocate by `cats_size` instead. Cores declaring more categories than options previously heap-wrote past the allocation.)_
- 🔄 **CRITICAL — `runahead.c:640, 662` Out-of-bound access preceding heap area.** Two sites in the input-state-list walk. clang-analyzer's preceding-heap warnings need a concrete reproducer to confirm vs FP — the loop iterates `i = 0` upward against `input_state_list->size` and the deref `data[i]` is bounded. Possible FP from path-sensitive analysis on platform-conditional branches. **Deferred** pending reproducer or deeper inter-procedural look.
- 🔄 **CRITICAL — `retroarch.c:435, 1186, 2270` three sites Out-of-bound past `location_drivers[]`.** Likely **clang-analyzer FPs** — the array is NULL-terminated at definition (`{ ..., &location_null, NULL }`), the iteration `for (d = 0; location_drivers[d]; d++)` is correctly NULL-guarded, and `find_driver_nonempty` only returns indices 0..N-1. **Deferred** unless a reproducer surfaces.
- 🔄 **CRITICAL — `tasks/task_translation.c:177` Out-of-bound past `translation_drivers[]`.** Same FP class as the location-drivers item above.
- ✅ **CRITICAL — `menu/menu_setting.c:2784` Out-of-bound access preceding `input_config_bind_order[]`.** _(Fixed `6423760b01` — the walk-and-shift loop in `setting_action_left_retropad_bind` and its right-hand twin did `[i ± step]` reads when the user was on the first/last entry, OOB-reading before/after the 24-entry array. Guard with `if (i ± step in range)`; the existing wraparound block below the loop handles the wrap case correctly.)_
- ✅ **CRITICAL — `menu/drivers/rgui.c:1695, 1784` Out-of-bound access preceding `scanline_even`.** _(Fixed `6423760b01` — `scanline_even`/`scanline_odd` are stack arrays sized `[RGUI_MAX_FB_WIDTH]`, but loop bounds were derived from runtime `fb_width`. A `fb_width > RGUI_MAX_FB_WIDTH` configuration walked off the stack array. Capped working width to `MIN(fb_width, RGUI_MAX_FB_WIDTH)` up front.)_

### 🔥 NEW High (UAFs, leaks, NULL-derefs not in prior reports)

- ✅ **HIGH — `gfx/common/wayland_common.c:380, 385` UAF / double-free.** _(Defensive fix `a9f5dcf7f4` on `local/fixes-2026-04` — restructured `all_outputs` cleanup loop to unlink before deref and added NULL-guards on `oi` and `oi->output`. clang-analyzer's path-sensitive trace persists despite the fix; appears to be macro-expansion noise from `wl_container_of` since the loop is correct after the restructure. Will revisit if a real reproducer surfaces.)_
- ✅ **HIGH — `input/bsv/uint32s_index.c::uint32s_bucket_free` + `uint32s_index_pop` UAF/double-free.** _(Fixed `3d10605ee4` — `uint32s_bucket_free` now NULLs the freed `vec.idxs` and resets `len=0` so a subsequent `RHMAP_PTR` (which has add-on-miss semantics) on the cleared slot can't double-free via stale `len > 3`. `uint32s_index_pop` now guards `RBUF_LEN(...)-1` against UINT32_MAX wrap and gates `RHMAP_PTR` behind `RHMAP_HAS` so corrupted `hashes[]` entries can't auto-create a stale-content bucket. Closes the BSV-replay-file trust-boundary UAF class.)_
- ✅ **HIGH — `input/common/wayland_common.c::wl_update_scale` UAF + `::wl_data_device_handle_drop` memory leak.** _(Fixed `71fa88edb0` — `wl_update_scale` now `continue`s on NULL `os->output` (output_info_t can be asymmetrically NULL'd by hot-unplug). DnD handler now `free(buffer)` on the fmemopen-fails early-return, and `free(line)` after the getline read loop regardless of how it ended.)_
- ✅ **HIGH — `gfx/drivers/vulkan.c:7976` Dereference of NULL pointer.** Vulkan render path. _(Fixed `e35479c146` Bundle 37 — the destroy-loop at lines 7979-7983 reads `vk->overlay.images[i]` *before* the post-loop `if (vk->overlay.images)` free-guard. If `count > 0` but `images == NULL` (resource init partially completed and then bailed), the loop crashes before reaching the existing NULL-check. Moved the NULL check to gate both the loop and the free; `memset` still runs unconditionally so `count`/`images` end consistent regardless. `vk->context->device` left unchecked — every other deref of `vk->context` in this file (1028, 1248, 1386, …) trusts the struct invariant that `context` is non-NULL once `vk` is initialised, and adding a check only here would be inconsistent.)_
- ❌ **HIGH — `gfx/video_driver.c:3633, 3745` NULL passed to nonnull-attributed param + NULL deref.** _(Verified-FP 2026-04-27 — both are clang-analyzer path-sensitivity false positives. At line 3633, `strcmp(video_ident, "glcore")` is unreachable for `video_ident == NULL` because line 3613 has `if (!video_ident) return GFX_CTX_NONE`. At line 3744, `video_driver_set_viewport_square_pixel(geom, rotation)` passes `geom = &video_st->av_info.geometry` where `video_st = &video_driver_st` (static-storage); the analyzer doesn't trace static-storage addresses as definitely-non-NULL. No code action.)_
- ✅ **HIGH — `gfx/video_shader_parse.c::video_shader_apply_shader` `current_video->set_shader` deref-without-NULL-check.** _(Fixed `349cc5644b` — added `video_st->current_video &&` guard before the `set_shader` field check on the synchronous fallback path. The deferred path eight lines above already used the explicit current_video NULL-check, but the sync path had drifted — same author-aware pattern as Bundle 5.)_
- ✅ **HIGH — `command.c::command_event_video_apply` `current_video->set_nonblock_state` deref-without-NULL-check.** _(Fixed `349cc5644b` — added `&& video_st->current_video` to the existing `&&`-chain on the menu-alive branch. The neighbouring `video_st->poke && video_st->poke->show_mouse` block was already correct.)_
- ❌ **HIGH — `runloop.c:5701` `current_video->focus` NULL deref.** _(Verified resolved-stale 2026-04-25 — current code at the corresponding site uses the ternary `video_st->current_video ? video_st->current_video->alive(video_st->data) : true;` which is correctly check-before-deref. clang-tidy's reported line drifted; no equivalent unguarded `current_video->focus` deref found by grep.)_
- ❌ **HIGH — `input/input_driver.c:4501` `bind->key` NULL deref.** _(Verified resolved-stale 2026-04-25 — current code at `input_config_get_bind_string` derefs `bind->key` only inside `if (bind)` at the outer guard, and the prior `bind->joykey` / `bind->joyaxis` derefs all use `bind && ...` short-circuit. No unguarded `bind->key` site reachable.)_
- ✅ **HIGH — `slang_process.cpp:967, 970, 974, 977` four sites: NULL C++ object pointer call.** _(Fixed `89b8adf690` — combined NULL-guard with the existing `!empty()` check on `vs/ps_resources.{uniform,push_constant}_buffers` at all four `set_decoration` sites. The function happened to be safe today because `dst_type==RARCH_SHADER_HLSL`-without-`HAVE_HLSL` leaves `vs/ps_resources` default-constructed (empty) and the `!empty()` short-circuits the call -- but that's a brittle coincidence; made the guard explicit so a future ShaderResources default-ctor change can't reintroduce the NULL-method-call.)_
- ✅ **HIGH — `ui/drivers/ui_qt_widgets.cpp` ShaderParamsDialog: five sites where `*_shader->flags` was written outside its `if (*_shader)` guard.** _(Fixed `e9681a0a4d` — wrapped each of the trailing `*_shader->flags |= SHDR_FLAG_MODIFIED;` lines (onShaderPassFilterChanged, onShaderPassScaleChanged, onShaderPassMoveDownClicked, onShaderPassMoveUpClicked, onShaderParamCheckBoxClicked) in a one-line NULL guard. The earlier `pass[]` writes were already guarded -- only the trailing flag write had landed outside the brace.)_
- ✅ **HIGH — Realloc-leak class (`bugprone-suspicious-realloc-usage`).** `runahead.c::mylist_resize` and `input/bsv/uint32s_index.c::uint32s_bucket_expand`. _(Fixed `5c99961ccd` — `tmp = realloc(p, …); if (!tmp) return; p = tmp;` pattern at both sites; both functions are `void` so resize/expand fail silently rather than corrupt heap state. The `uint32s_bucket_expand` realloc fix had a sibling bug: `bucket->contents.vec.cap` was being mutated *before* the realloc attempt, leaving the cap doubled even on OOM — moved the mutation into the success branch. Also closed the adjacent calloc-fail at the `bucket->len == 3` transition (memcpy(NULL, …) crash on OOM).)_

### 🔥 NEW Medium (divides-by-zero, leaks, uninit, format-mismatch)

- ✅ **MEDIUM — Division-by-zero in 4 cited sites.** `menu/drivers/materialui.c:4727, 5566`, `menu/drivers/rgui.c:2473`, `menu/menu_setting.c:5773`. _(Bundle 43 — fixed in `dbb3f030c3` on `local/fixes-2026-04`. Triaged per-site rather than uniformly: materialui sites root-caused to `glyph_width` potentially rounding to 0 in `materialui_init_font` → added a `< 1` clamp immediately after the lroundf assignment, killing the class for 8+ similar `width / glyph_width` ticker divides in the same file (only 2 flagged, the rest are unflagged twins). rgui:2473 root-caused to `rgui_downscale_thumbnail` not guarding `max_width`/`max_height` (caller checks `image_src` dims but not the thumbnail size box) → top-of-function early-return matches the caller's existing failure path. menu_setting.c:5773 verified FP — `libretro_device_get_size` always seeds 2 entries (NONE + JOYPAD) before any conditional, so the mod-by-types at line 5785 cannot hit zero; clang-analyzer doesn't trace into the helper to learn the invariant.)_
- 🚧 **MEDIUM — Memory-leak class (8 sites).** _(Bundle 28 — 7 of 8 fixed in `856758e6b8` on `local/fixes-2026-04`. Independent fix shapes per site, only theme is "the existing cleanup discipline missed one entry." Sites:)_
  - ✅ `core_backup.c:383` `backup_filename` — empty-string `strdup("")` returned non-NULL pointer; the early-out treated it as failure-without-cleanup.
  - ✅ `core_info.c:825/855` `core_info_cache_list` — `core_info_cache_list_free` only frees inner contents; outer struct needs separate `free()` (caller's responsibility, see canonical pairing in `core_info_cache_list_new`'s failure path). Both branches now match.
  - 🔄 `core_updater_list.c:928` `entry.local_info_path` — clang-tidy false positive. Walked the full ownership chain (set_paths/set_core_info allocate fields, push_entry transfers all six pointers on success, error label frees on failure); no leak path. Path-sensitive analyzer loses the field-by-field move in push_entry. Left for the next reproducer-driven pass.
  - ✅ `gfx/gfx_animation.c:783` — `gfx_animation_push_delayed` malloced a `gfx_delayed_animation_t` and handed it off as timer-callback userdata; `gfx_animation_push` silently rejected the tween as born-dead when `duration == 0`, leaking the userdata. Top-of-function guard now forwards `delay == 0` to `gfx_animation_push` directly (no malloc, no timer round-trip).
  - ✅ `menu/drivers/ozone.c:5170` `node` — `ozone_context_reset_horizontal_list` read `userdata` and allocated a fresh node when NULL. Only the `.lpl` branch later stored the node anywhere (RHMAP_SET_STR); the no-path early-continue and the `.lvw` branch leaked the freshly-allocated node. Hand ownership to the list immediately after `ozone_alloc_node` returns -- matches the canonical pattern at `ozone.c:13050-13064`.
  - ✅ `menu/menu_setting.c:2257-2258 + 2436-2437` — `config_uint_alt` and `config_string_alt` called `setting_*_setting` with `dont_use_enum_idx=true`; the constructor strdups internally on that path. Callers were ALSO strdup'ing each string before passing it in, so every alt-flavour setting allocated and immediately leaked one strdup of name and one of short_description (~16 calls each at startup, leak proportional to MAX_USERS). Stack-local snprintf buffers remain valid through the macro expansion; removing the caller-side strdup is safe.
  - ✅ `network/discord.c:851-859` `discord_json_next_strdup` — did `*out = strdup(s)` without freeing previous `*out`. A Discord IPC payload that repeats a key in the same object (well-formed JSON allows it) silently leaked the previous strdup. Helper now `free(*out)` before assigning. Callers pass `&local_var` initialised to NULL so the typical single-key path costs nothing (`free(NULL)` is a no-op).
- ✅ **MEDIUM — Branch-on-uninitialized-value (3 sites).** _(Fixed `ab7f4d0f6e` on `local/fixes-2026-04`. All three sites the same shape: stack `char` buffer declared without initialiser, written only on some incoming paths, then read by the if-condition that decides what to do next.)_
  - ✅ `tasks/task_screenshot.c:444` `new_screenshot_dir` — written only inside `if (screenshot_dir && *screenshot_dir)`. Empty/NULL screenshot_dir + auto_screenshot_filename branch fell through to `!*new_screenshot_dir` reading garbage. Worst case: silent `path_mkdir` against arbitrary stack-leaked filename.
  - ✅ `verbosity.c:608` `log_directory` — written in two paths (override-active-with-slash branch + log_dir-non-empty branch). The override-active-WITHOUT-slash path leaves it unwritten but sets `log_file_path`, gating into the `if (log_directory[0])` test which then `path_mkdir`s an arbitrary stack content.
  - ✅ `menu/menu_displaylist.c:6848` `rom_buff` — filled by a for-loop bounded by `content_get_subsystem_rom_id()`. When zero ROMs are loaded the loop body never runs and the `if (*rom_buff)` test branches on garbage, occasionally appending a gibberish "[ROMs]" suffix to the subsystem-load menu entry.
- ✅ **MEDIUM — Float used as loop counter, 13 sites (12 cited + 1 missed):** `menu/menu_displaylist.c:16166/16193/16243/16269/16317/16344` (DROPDOWN_LIST), `menu/menu_displaylist.c:16509/16536/16585/16611/16658/16685` (DROPDOWN_LIST_SPECIAL), `ui/drivers/ui_qt_widgets.cpp:518` (UIntComboBox::populate — missed by audit), `ui/drivers/ui_qt_widgets.cpp:713` (UIntRadioButtons). _(Bundle 41 — fixed in `9498278d7e` on `local/fixes-2026-04`. All sites convert `for (float i = min; i <= max; i += step)` → `for (int j = 0; j <= n_steps; j++)` with `n_steps = (int)((max - min) / step)` computed up front, then `i = min + (float)j * step` derived inside the loop body. Same iteration count as before in non-pathological cases, but loop control no longer depends on float-add drift. ST_FLOAT variants use `(max + half_step - min) / step` to preserve the original termination intent. Step==0 guard collapses to a single iteration. Cluster too narrow for AUDIT-POLICY.)_
- ❌ **MEDIUM — `bugprone-not-null-terminated-result` (5 sites) — verified FP.** `tasks/task_save.c:435`, `network/netplay/netplay_frontend.c:5097, 7773`, `gfx/drivers_shader/slang_cache.cpp:152, 154`. Triaged Bundle 40 (2026-04-27): all five clang-tidy false positives. `task_save.c` writes are binary headers (`"RASTATE"` magic, 4-byte block tags) into a serialization buffer that is never C-string-walked. `netplay_frontend.c` `memcpy(payload.nick, src, sizeof(payload.nick))` always copies a `strlcpy`-NUL-terminated source into a fixed-size NUL-padded wire field. `slang_cache.cpp:152, 154` build a binary hash input fed to `sha256_hash` — NUL termination would corrupt the hash. No code change.
- ✅ **MEDIUM — `bugprone-suspicious-string-compare` (10 cited sites).** _(Bundle 44 — fixed in `5e7692e5ce` on `local/fixes-2026-04`. Re-ran clang-tidy on the cited files post-compaction; authoritative current-snapshot lines are `core_info.c:1550`, `menu/menu_displaylist.c:2587/2695/2697`, `menu/menu_explore.c:534/564`, `network/netplay/netplay_frontend.c:3258/3299/6953`, `retroarch.c:799` — roadmap's earlier "3157, 3198, 6852" netplay cites and "1539" core_info cite were stale snapshots; the in-source `memcmp`/`strcmp`/`strcasecmp` calls without explicit zero comparison live at the listed lines today. All 10 verified correct-intent ("if differ from constant"): `_libretro` suffix detection, `ozone` driver name compare, `FILE_PATH_DETECT` placeholder, `.lpl` extension filter, db_name vs filename override, two netplay resim-needed signals, MITM session-id non-zero check, midi driver fallback writeback. No inverted-logic bugs found in this batch — the `if (memcmp(...))` shorthand is house style throughout the codebase, but the cited sites are the ones the linter calls out specifically. Mechanical close-out: append `!= 0` to each call. Re-ran clang-tidy: cluster cleared to zero. Build verified clean.)_
- ✅ **MEDIUM — `gfx/drivers/vulkan.c:5899` `bugprone-suspicious-memory-comparison` on `math_matrix_4x4`.** _(Bundle 39 — fixed in `9edf7ffa57` on `local/fixes-2026-04`. Switched compare from `memcmp(*quad->mvp, ...)` to `memcmp(quad->mvp->data, ..., sizeof(quad->mvp->data))` — narrows the compare to the bare `float[16]` and documents intent: bit-exact change-detection for descriptor-refresh skip, never a rendering bug if a false-positive "changed" verdict triggers an extra UBO upload.)_
- ✅ **MEDIUM — Sizeof-pointer-not-array, 3 sites in `gfx/gfx_widgets.c:380, 980, 1934`.** `bugprone-sizeof-expression`. _(Bundle 40 — fixed in `2760d936c8` on `local/fixes-2026-04`. Triaged as FP class: the FIFO is initialised at line 2126 as `MSG_QUEUE_PENDING_MAX * sizeof(disp_widget_msg_t*)` — entries are pointer-typed by design, so `sizeof(pointer)` is correct, not a buffer-size mistake. Three call-sites had drifted from the init-site style to the shorter `sizeof(msg_widget)` form, hiding intent from the reader and the linter. Switched all three to `sizeof(disp_widget_msg_t*)` so intent is explicit at the call-site. No behavioural change — pointer size identical either way; only textual form differs. Cluster too narrow (one file, two functions) for an AUDIT-POLICY entry per the 3-unrelated-files threshold.)_
- ✅ **MEDIUM — `bugprone-incorrect-roundings` (4 sites + 3 obvious neighbours).** _(Bundle 38 — fixed in `e1e0fd8e1d` on `local/fixes-2026-04`. Switched the cited sites from `(int)(x + 0.5f)` to `(int)lroundf(x)`. The idiom is wrong for negatives (truncates-toward-zero before adding 0.5) — current callers feed non-negative font sizes / scale factors so no behaviour-visible bug today, but the idiom is the wrong tool. Sites swept: `gfx/gfx_widgets.c:882, 886, 894` (cited 882; 886 + 894 are the same idiom three lines apart in the same function), `menu/drivers/materialui.c:8872`, `menu/drivers/ozone.c:9575-9576` (cited as 9566 in pre-drift ROADMAP), `menu/drivers/ozone.c:9750, 9752, 9753` (cited as 9742-9743). Other rounding-idiom sites in `materialui.c` (~12 occurrences) and `ozone.c` not flagged in the cited cluster — left for a separate sweep if clang-tidy re-flags them.)_
- ✅ **MEDIUM — `bugprone-signed-char-misuse` (5 sites).** `menu/cbs/menu_cbs_ok.c:4151`, `libretro-db/rmsgpack.c:371`, `menu/drivers/xmb.c:1921`, `menu/menu_explore.c:303-305`. _(Bundle 42 — fixed in `e629aaaac2` on `local/fixes-2026-04`. Each addressed at the right level: `menu_cbs_ok.c` declared `ret` as `int8_t` to match `config_save_overrides`'s return type instead of widening; `rmsgpack.c` made the intentional msgpack sign-extension explicit via `(int64_t)(int8_t)tmp.u8` plus a one-line comment so intent reads as deliberate; `xmb.c` changed the field type `char entry_index_offset` → `unsigned char` (the field stores 0/1/2 only, set at xmb.c:3502-3511); `menu_explore.c` cast through `(unsigned char)` to match the existing pattern at line 287-288 in the same file. Build verified clean. Cluster spans 4 unrelated subsystems (menu cbs, msgpack reader, xmb, menu explore) — too narrow for an AUDIT-POLICY rule entry, and the diagnostic itself already exists upstream.)_

### ✅ Confirmations of prior items

clang-analyzer's path-sensitive trace confirmed these prior findings — not new, but elevated confidence (3rd independent signal in some cases):

- `gfx/common/wayland_common.c:296` `oi->width` NULL deref → matches **audit M8** (already roadmapped).
- `network/natt.c:314` `child->next` NULL deref → matches **audit C1** + **indie-review C-3** (cross-confirmed third time).
- `tasks/task_cloudsync.c:847, 849, 881, 1286` NULL derefs → matches **audit H3** + **indie-review C-3** path-traversal cluster.
- `tasks/task_overlay.c:647` `desc->next_index` NULL deref → matches **indie-review H3** (loader allocations leak class).
- `tasks/task_core_updater.c:633, 639` `download_handle` NULL deref → matches **indie-review H-2** (same UAF cluster as task_http).
- `menu/cbs/menu_cbs_right.c:223`, `menu_cbs_sublabel.c:1946`, `menu_driver.c:7349, 1330`, ozone.c (5 sites), xmb.c (6 sites), materialui.c (1 site) → all match the **indie-review C1 / audit S2** `MENU_LIST_GET_SELECTION(...)->size` cluster. clang-analyzer traced path-sensitive proof of NULL.
- `menu/menu_setting.c:9273, 9858, 10163, 10229, 3261` NULL derefs in setting handlers → adjacent to the audit H1 zombie-settings class.
- `menu/menu_displaylist.c:5176, 6789, 6870, 15317` NULL derefs → consistent with the menu coupling-and-NULL-handling pattern flagged in indie-review.

### 🛠 Tool gaps now closed / remaining

- ✅ **`compile_commands.json` generated** (via `bear -- make`). clang-tidy + clazy now runnable.
- ⏳ **clazy not yet run.** RetroArch's Qt UI is small (`ui_qt_widgets.cpp` only); clazy yield expected to be modest. Defer until needed.
- ⏳ **cppcheck `materialui.c` macro-config exhaustion** still not finished — clang-analyzer caught the materialui.c findings the cppcheck pass would have hit.
- ⏳ **Platform-conditional builds** (`HAVE_EGL=0`, console targets) still not verified — would resolve audit S3/S4/S7/S8.

### 🧮 Updated tally (cumulative across audit + indie-review + clang-tidy)

- **15+ Critical** (TLS off, network-cmd RCE, write_ram OOB, natt, webdav uninit, no-atomic-save cluster, malicious-savestate-overflow cluster, IPS/UPS patch NULL, audio mixer OOB, runahead OOB, location_drivers OOB ×3, translation_drivers OOB, input_config_bind_order OOB, scanline_even OOB ×2, core_option_manager OOB, plus the cross-cutting cluster items)
- **40+ High** (NULL derefs, UAFs, OOM null-checks, plaintext credentials, netplay weak-RNG and timing leaks, WebDAV digest parser issues, deref-before-check sweep ~30 sites, env-callback NULL hardening, 5+ Qt UI shader NULL derefs)
- **30+ Medium** (divides-by-zero, leaks, uninit branches, float-loop-counters, string-compare misuse, etc.)
- **15+ Tier-3 structural** (driver-pattern dedup, materialui split, dead vtable slots, configuration table-drive, etc.)

---

## 🧪 Test Audit 2026-05-18

Branch: `local/audit-2026-04` (doc fold-in) / `local/fixes-2026-04` @ `3a6278b94f` (Bundle 70 source).
Scope: 5 libcheck unit-test files (`libretro-common/test/{hash,lists,queues,string,utils}/test_*.c`, 2274 lines total).
Tools run: 2 parallel `test-audit-chunk` subagents (all 18 dimensions); pre-pass grep; orchestrator syntax-pass via libcheck shim.
Filtered by orchestrator: 1 build-break + 6 missing-`END_TEST` + ~35 weak-assertion + 4 misc actioned in Bundle 70; ~12 coverage gaps + 2 refactor candidates + 1 SUT-side gap deferred below; 2 chunk-A false positives logged.

Status markers per top-of-file legend (📋 / 🚧 / ✅ / 🔄 / ❌).

### 🧪 Tier-1 — coverage gaps in libretro-common SUT (real defensive gaps)

- ✅ **HIGH — `lists/linked_list.c::linked_list_remove_*_matching` (3 functions) lack the NULL-callback guard that `get_*_matching` has.** `linked_list.c:159, 175` guards `if (!matches) return NULL;` in `linked_list_get_first_matching` / `linked_list_get_last_matching`; the three remove variants at lines 297, 322, 347 have no such guard, so `remove_first_matching(non_empty_list, NULL, NULL)` derefs NULL inside the iteration callback. No test currently exercises this path because the existing `*_function_null` tests only cover the empty-list case. **Fix shape:** add `if (!matches) return NULL;` early-return at the top of each of the three functions, mirroring the get variants. **Test sibling:** add three `test_linked_list_remove_*_matching_function_null` cases that build a non-empty list and call with NULL callback (asserting the function returns NULL and the list state is unchanged). Both fixes land on `local/fixes-2026-04`. _(Bundle 76 — `00e6d85d4e`: added the `!matches` guard to all three remove variants + non-empty-list `remove_{first,last,all}_matching_function_null` regression tests; verified load-bearing by reverting the guard → ASan SEGV. `linked_list.c` is vendored — guard is a candidate for upstreaming.)_

### 🧪 Tier-2 — test-coverage gaps (no SUT change needed)

- ✅ **MEDIUM — `linked_list.c` NULL-arg coverage gaps.** Missing tests for: `linked_list_add(NULL, value)` (early-return path), `linked_list_iterator_next(NULL)` (returns NULL per header), `linked_list_iterator_value(NULL)` (returns NULL per header). The codebase pattern is to have one `*_null` test per mutating function — these three are the gaps in the otherwise-complete coverage. Cheap to add (~6 lines each). _(Bundle 76 — `00e6d85d4e`: added `test_linked_list_add_null`, `_iterator_next_null`, `_iterator_value_null`.)_
- ✅ **MEDIUM — `linked_list.c::linked_list_foreach` callback args untested.** `_foreach_fn` in `test_linked_list.c:1153` ignores both the `index` and `value` arguments and only increments a counter. The contract that index runs 0..N-1 and value matches the inserted pointer is unverified. **Fix:** extend the callback to push (index, value) into a fixture array, assert the array contents after the call. _(Bundle 76 — `00e6d85d4e`: added `test_linked_list_foreach_args` with a dedicated capture callback asserting index 0..N-1 + value identity, leaving the existing `_foreach_fn` tests untouched.)_
- ✅ **MEDIUM — `lrc_hash.c::SHA1Digest` (in-memory SHA1) untested.** `test_hash.c::test_sha1` only exercises `sha1_calculate()` (the file-hashing path). The in-memory `SHA1Digest()` API diverges on Apple (uses `CC_SHA1`) vs other platforms (own implementation) — a broken non-Apple `SHA1Digest` would be invisible. **Fix:** add `test_sha1_digest` that calls `SHA1Digest((uint8_t*)"abc", 3, ...)` and verifies the canonical `A9993E36...` digest bytes. _(Bundle 76 — `00e6d85d4e`: added `test_sha1_digest` asserting the 20 raw digest bytes via `ck_assert_mem_eq`.)_
- ✅ **MEDIUM — `test_utils.c::file_crc32` boundary cases.** The `i < CRC32_MAX_MB` loop and the `nread < 0` error branch are never exercised — the 8-byte test fixture exits at the first `filestream_eof`. **Fix:** for the loop boundary, a `CRC32_BUFFER_SIZE`-sized (1 MiB) committed fixture under `test/data/` exercises one full iteration; for `nread < 0`, document as platform-dependent and leave with a comment naming the uncovered path. _(Bundle 76 — `00e6d85d4e`: added `test_crc32_file_large` covering the loop boundary via a runtime-generated >1 MiB temp file (no committed binary fixture, cross-checked against a one-shot `encoding_crc32`); the `nread<0` branch is left documented/uncovered as planned.)_
- ✅ **MEDIUM — `test_generic_queue.c` NULL-arg gaps.** `generic_queue_length(NULL)` is documented to return 0 (SUT line 197) but never tested with NULL; `generic_queue_remove(queue, missing_value)` "value not in queue → returns NULL" contract is unverified; `generic_queue_iterator(empty_queue, true)` "returns NULL" path also not tested. Cheap additions to `test_generic_queue_empty` (just append the three asserts). _(Bundle 76 — `00e6d85d4e`: appended `length(NULL)==0`, `remove(missing)==NULL`, `iterator(empty)==NULL` to `test_generic_queue_empty`.)_
- ✅ **LOW — `test_stdstring.c::test_string_char_classify` does not exercise `ISUALPHA` / `ISUALNUM` / `IS_XDIGIT` / `TOUPPER` / `TOLOWER`.** These macros are exported from `stdstring.h` but only `ISSPACE` / `ISALPHA` / `ISALNUM` get coverage. Spot-checks (`ck_assert(IS_XDIGIT('a')); ck_assert(!IS_XDIGIT('g'));`) extend the test from ~14 lines to ~25. _(Bundle 76 — `00e6d85d4e`: extended `test_string_char_classify` to cover ISUALPHA/ISUALNUM (incl. `_`), IS_XDIGIT (incl. non-hex `g`/`G`), and TOUPPER/TOLOWER.)_

### 🧪 Tier-3 — refactor / cleanup

- ✅ **MEDIUM — `tcase_add_checked_fixture` for the 3-element-list/queue setup.** `test_linked_list.c` has ~40 test bodies inline-constructing `linked_list_new()` + three `linked_list_add(list, _value_*)` calls; `test_generic_queue.c` has ~6 doing the equivalent for `generic_queue_*`. Extract to a per-tcase `setup_three_element_list` / `teardown_list` fixture pair registered via `tcase_add_checked_fixture`; individual tests needing different initial state still set up locally. Tests get shorter, intent gets clearer, run cost unchanged. _(Bundle 77 — `31e000b6eb`: added a `setup/teardown_three_element_{list,queue}` checked fixture in a dedicated "ThreeElement" tcase for the tests that all start from a fresh `[v1,v2,v3]` collection — 12 in `test_linked_list.c` (read-only reads, in-place set, iterator removal, foreach), 6 in `test_generic_queue.c`. The remove-family construction is handled instead by the `_build_list_arr` helper introduced for the loop-test sibling item below — those families need heterogeneous per-row inputs that a single fixed fixture cannot serve.)_
- ✅ **MEDIUM — `tcase_add_loop_test` parametrisation for `linked_list.c::remove_{at,first,last,all}*` families.** Seven families (remove_at, remove_first, remove_last, remove_all, remove_first_matching, remove_last_matching, remove_all_matching) each repeat the "build 3-element list / call remove(index_or_value) / verify remaining" pattern ~5 times for first/middle/last/only/not-found. ~35 near-identical bodies across the file. `tcase_add_loop_test` with a `{remove_arg, expected_size, expected_values[]}` table per family collapses each to ~10 lines. Big readability win; same coverage. _(Bundle 77 — `31e000b6eb`: all 7 families now drive a `tcase_add_loop_test` over a per-family `static const` table (3 row structs: index-keyed, value-keyed, predicate-keyed). The table rows carry the full input arrangement per row — so the matching families keep their original position-shuffled inputs — plus, for `remove_at`, the previously-unchecked return value (strictly stronger). Each row's list is built by `_build_list_arr` and checked by `_verify_list_arr` (the varargs `_verify_list` now delegates to the array form). `_value_{1,2,3}` switched `char*`→`char[]` so their addresses are compile-time constants usable in the static tables; pointer-identity semantics unchanged. Exact 96-check parity; a negative-control row mutation confirmed the loop assertions fire with the iteration index reported. Net ~600 fewer lines in the file.)_
- ✅ **LOW — `tmpnam()` → `mkstemp()` (POSIX) / `GetTempFileName()` (Windows) migration in `test_utils.c::test_crc32_file` + `test_hash.c::test_sha1`.** Bundle 70 added `remove()` cleanup but left `tmpnam` itself in place (CWE-377 TOCTOU race + glibc `-Wdeprecated-declarations` warning under `-Werror`). The full migration needs an `#ifdef _WIN32` shim because `mkstemp` is POSIX-only and the test suite has to build on both. Defer until the warning materialises as a build break, or fold into a "C89 portability shim" if one accumulates. _(Bundle 77 — `31e000b6eb`: extracted a shared `test/test_tmpfile.h` helper (`test_tmpfile_open` → `mkstemp`+`fdopen` on POSIX, `GetTempPathA`+`GetTempFileNameA`+`fopen` under `#ifdef _WIN32`) and routed all **three** call sites through it — `test_crc32_file`, `test_crc32_file_large` (added in Bundle 76), and `test_sha1`. Verified the glibc "the use of tmpnam is dangerous" link-time warning is gone. New shared header is fork-local test scaffolding; `mkstemp` compiled clean under the default GCC dialect with no feature-test macro needed.)_

### 📝 Test-audit follow-ups (process/tooling)

- ✅ **MEDIUM — TEST-AUDIT — orphan-test detection.** The `string_replace_substring` build break (test called 3-arg SUT signature 17 months after the SUT moved to 6 args) only surfaced via the test-audit shim syntax-pass. The libcheck suite has no CI hook in this fork's build matrix — Makefile.test is run by hand. **Fix shape:** wire `make -f libretro-common/Makefile.test` (or at minimum `-fsyntax-only`) into a `make audit-tests` target so a future SUT signature change that orphans a test is caught at the next audit pass instead of accreting silently. Could be one ROADMAP-level audit lane (`/audit --tests`) or a `Makefile.audit` recipe. _(Bundle 76 strongly corroborates this: the suite had never run to completion in this fork — libcheck was uninstalled, so Bundle 70's `-fsyntax-only` pass missed two undefined-reference link breaks (`strldup`, `path_is_directory`/`path_mkdir`), a GCC-15 `-Werror=discarded-qualifiers` break, and two latent NULL-on-UB segfaults. Bundle 76 installed `check-devel`+`lcov` and got the suite green end-to-end, but did NOT wire a `make audit-tests` CI hook — this item stays open for that.)_ _(Bundle 78 — `a55c5986e5` on `local/fixes-2026-04`: added a `build` target to `libretro-common/Makefile.test` that compiles + links all five suites via the existing `$(TEST_*_SRC)` vars but does **not** run them or lcov-cover — the cheap orphan-detector. Run `make -f libretro-common/Makefile.test build` each audit pass. Verified green on the current tree (all 5 link) and EXIT 2 with "too few arguments to function `linked_list_add`" when a stale-signature call is injected — i.e. it catches exactly the Bundle-70 compile-drift class and the Bundle-76 undefined-reference link class. Documented as the test-suite audit step in [`audit/README.md`](audit/README.md) "Quick reference — auditing the test suite". A standing CI hook in a hosted runner is still out of scope (this fork runs the audit by hand), but the by-hand orphan gate now exists and is documented.)_
- ✅ **LOW — TEST-AUDIT — chunk-agent missed 3 of 6 missing-`END_TEST` sites.** Chunk B reviewed `test_generic_queue.c` and did not flag the 3 missing `END_TEST` macros after the `remove_first` / `remove_middle` / `remove_last` test bodies. The orchestrator's shim-based syntax pass caught them. This suggests the chunk-review checklist should explicitly cite "verify START_TEST → END_TEST pairing via grep + line-walk" rather than rely on reading-flow detection — the missing macros don't break compilation in real libcheck (because libcheck's `START_TEST` macro emits a `static void name(...) {` opener and `END_TEST` emits `}`; without END_TEST, the next START_TEST's `static void` is nested into the current scope, which IS a UB compile error). Update the test-audit-chunk subagent prompt: add an explicit "structural-pair audit" item to the dimension 4 (Naming/AAA) section. _(Bundle 78 — global Claude config, no repo commit (user-approved self-modification 2026-06-10): added a grep-based **structural-pair audit** bullet to `~/.claude/skills/test-audit/references/dimensions.md` §4 (Naming & AAA) — count `START_TEST` vs `END_TEST` per file and line-walk any mismatch, plus flag defined-but-unregistered tests — and a matching explicit Procedure step (#5) in `~/.claude/agents/test-audit-chunk.md` telling the chunk agent to do the count with grep rather than reading-flow. Framework-aware (names the libcheck macros + pytest collection as the two instances). Closes the "reading-flow missed 3 of 6" gap by making the count a mechanical step. Lives in global config because the test-audit subagent is shared across all projects, not RetroArch-local.)_

---

### 🧭 Strategic follow-ups (2026-07-04)

Forward-looking workstreams surfaced while reviewing the 88-bundle audit history — the backlog of *known* bugs is largely worked down, so these are the higher-leverage structural moves. Recorded as suggestions; none block current work.

- 📋 **HIGH — Regression coverage for the shipped security-critical fixes.** The CRITICAL/HIGH security fixes in RA's own source shipped with **no regression test**, so an upstream re-sync or a refactor near the site can silently revert one and nothing catches it. Highest-value protection, in rough priority: `network/natt.c::natt_parse_desc_node` (hostile UPnP XML → NULL-deref / inverted recursion, `9953abc994`); `command.c` wire-driven OOB caps (`command_write_ram` 4096-byte cap; `command_read_ram`/`command_read_memory` `nbytes` overflow guard; `8aedb937f1`); `tasks/task_cloudsync.c` path-traversal reject (`..` / leading-slash manifest keys, `18b55ec71a`); save-state / replay parsers (`tasks/task_save.c::content_load_rastate1` block-size bounds + `input/bsv/bsvmovie.c` capacity checks, `1f59d9d31d`). **Shape:** one test per fix that feeds the malicious input and asserts the reject/cap path (would fail on pre-fix code). libretro-common-side fixes (rjson/archive) → a libcheck case under `libretro-common/test/`; RA-side parser fixes (save/BSV) → a `.ratst` replay or a small RA-side harness. Distinct from the TLS spec's own `test/net/` wiring (Phase 5) and the libcheck-utility suite (Bundles 70/76/77) — neither covers these fixes. Use `/feature-test`; land on `local/fixes-2026-04`.

- 📋 **MEDIUM — Upstreaming backlog for the clear-cut fork fixes (branch-drift reduction).** Many shipped fixes are plain `libretro/RetroArch` bugs, not fork-specific, yet they live only on `local/fixes-2026-04` — so they (a) help no other libretro user and (b) accrue merge debt as the fixes branch drifts from upstream `master` (already ~76/24 commits diverged at Bundle 67). The vendored-blocked items (`libretro-common/file/config_file.c` atomic write, `formats/json/rjson.c` `(json+1)`, `file/archive_file.c` NULL backend) are *only* fixable by upstreaming — they sit 🔄 today for exactly this reason. **Shape:** keep a tracked list of upstream-PR candidates (seed: the security fixes above + the `linked_list.c` NULL-guard flagged in Bundles 76/77 + the three vendored-common items) and PR them against upstream on a cadence; each merged PR lets the next re-vendor drop a local edit instead of clobbering it. Pairs with a periodic `local/fixes-2026-04` rebase-onto-`master` to bound drift. (Upstreaming is stated as policy in the header + per-item asides today, but there is no single tracked backlog — this is that tracker.)

- ℹ️ **Already roadmapped (no new entry needed):** the go/no-go on the two big Tier-1 items — TLS remaining phases (consent dialog / BearSSL opt-out / upstream PR) and plaintext-credentials tiers 2–3 (secrets-file split / OS-keyring) — is tracked under Tier-1 above; both are 🚧 awaiting a decision to proceed or explicitly close. The libcheck CI-hook suggestion is ✅ closed (Bundle 78 orphan-gate).

---

### 📝 Cold-eyes 2026-05-18

Docs reviewed: 17 (across 8 lanes — contracts, fork-ops, audit-infra, 4 spec docs, platform READMEs). Loops to clean: 1 (single-pass; substantial rewrites surfaced as banners + deferred items rather than re-looped — user RAM pressure at 19% avail). Findings verified: ~100. Findings fixed in-place this session: 31. Findings deferred to ROADMAP follow-ups (below): 27 — substantive spec rewrites and design-decision items.

Fixes already landed this session (not roadmap items, listed for the audit trail):

- ✅ `CLAUDE.md`: replaced bogus `frontend/frontend.c:main_entry` cite with `frontend/frontend.h:39 (decl) → retroarch.c:6117 (def)`; renamed `Makefile.common:2750` flag from `test_input` to `HAVE_TEST_DRIVERS`; corrected version-lockstep list (snapcraft.yaml doesn't exist; metainfo.xml `<release>` block added); appended a new `## Fork workflow (private)` section documenting the `local/audit-2026-04` / `local/fixes-2026-04` two-branch model and pointing at `docs/private/ROADMAP.md` / `AUDIT-POLICY.md` / `audit/aggregate.py` / `.cppcheck-suppress.txt`.
- ✅ `CONTRIBUTING.md:101`: `AUTHORS` → `AUTHORS.h` (the actual filename).
- ✅ `README.md`: dropped dead `travis-ci.org` badge, dropped dead `posts.google.com` link, dropped dead `bountysource.com` link, fixed `$XDG\_CONFIG\_HOME` → `` `$XDG_CONFIG_HOME` `` markdown escape.
- ✅ `docs/private/audit/README.md`: added `--suppressions-list=.cppcheck-suppress.txt` to the cppcheck quick-ref; corrected `/indie-review --quick` 3rd lane name from "cloud sync" to "network commands / IPC" (per `indie-review-partition.md:262` — lanes 1+3+5, not 1+3+6); corrected "Adding a new tool" step 2 (parser registers in `PARSERS` dict; `--from-<tool>` arg is derived); replaced stale "Through Bundle 49: 187 fixes" snapshot with a "see top of ROADMAP" pointer to stop the 21-bundle-drift recurrence.
- ✅ `docs/private/audit/indie-review-partition.md`: refreshed big-file LoC table (17 rows × LoC drift +9 to +43 lines, against current source); fixed `cores/libretro-common/libretro.h` → `libretro-common/include/libretro.h` (the actual path; Lane 1 dispatch would hard-fail before); fixed Lane 6 `cheevos/{cheevos_client,rcheevos}.c` brace expansion (`rcheevos.c` doesn't exist — listed `cheevos.c,cheevos_client.c,cheevos_menu.c,cheevos_rvz.c` explicitly, noted vendored rcheevos under `deps/`); refreshed Lane 3 `*_driver_find_driver` line cites with re-grep instructions.
- ✅ `docs/private/audit/suppressions.md`: S12 self-contradiction (gfx_thumbnail FP entry said ".cppcheck-suppress.txt not yet introduced"; S12 has landed — clarified that `.cppcheck-suppress.txt` is for project-wide per-rule, inline-suppress stays for per-line FPs); enriched the `task_content.c:609` rationale to match ROADMAP's S11 narrative.
- ✅ `docs/private/ROADMAP.md`: added `❌ won't-fix / verified-FP / resolved-stale` to the legend (7 in-body uses were undefined); deleted the orphaned `💸 cheap-to-spec` tag paragraph (spec-cluster cluster closed Bundle 35); pointed the Test Audit duplicate-legend at the top-of-file legend.
- ✅ `docs/private/specs/2026-04-27-audit-spec-needed-cluster.md`: added cold-eyes status banner (5 corrections: active-voice reads, `retro_static_assert` shim does not exist (use `_Static_assert`), `menu_driver.h:255` → `:260`, S4 recommendation contradicts shipped option (2), follow-on spec `<date>` placeholders).
- ✅ `docs/private/specs/2026-04-27-menu-driver-swap-design.md`: added cold-eyes status banner (8 corrections: gating-site line numbers stale ~26 lines, API names `menu_settings_list_{new,free}` do not exist (real: `menu_setting_{new,free}`), D2 is moot (function already exists at `menu_driver.c:2283`), gating count is 25 not "20+", D1 precedent appeal unverified, glui-vs-materialui aliasing, failure-fallback ordering, missing edge cases).
- ✅ `docs/private/specs/2026-04-27-tls-verification-opt-in-design.md`: title renamed "Opt-In" → "Default-On with Documented Opt-Out" (was at odds with Goal 1's "mandatory by default"); added cold-eyes status banner (8 corrections: `RARCH_*` in vendored file creates merge debt, BearSSL backend silently uncovered, vendored mbedtls path is `deps/mbedtls/mbedtls/ssl.h` not `libretro-common/include/`, `network/net_compat.h` doesn't exist at root, incomplete failure-mode enumeration, static `ssl_authmode` thread-safety, missing numeric SLOs + automated test wiring, plus low/info nits).
- ✅ `docs/private/specs/2026-04-27-cloud-sync-streaming-upload-design.md`: added cold-eyes status banner (9 corrections: Phase 1 shipped Bundle 32 `c3ebdb11e4` at dispatch layer (per-backend in spec is SUPERSEDED), s3.c line cites drifted ~130 lines, mbedtls SHA-256 API is unsuffixed legacy not `_ret`, multipart carries full file (Phase 4 is definite refactor not "audit"), missing `## Failure modes` section, missing numeric SLOs, TLS-spec dependency unmentioned, third D3 option (`UNSIGNED-PAYLOAD` + `Content-MD5`), cross-spec settings-UI placement disagreement).
- ✅ `README-exynos.md`: added historical-reference banner (driver wired but targets EOL hardware); dropped invalid `README-ODROID` cross-reference; noted upstream libdrm has Exynos API today; fixed backslash-underscore escape artefacts.
- ✅ `README-mali_fbdev_r4p0.md`: added historical-reference banner with genre note (`mali_fbdev` is a *context* driver feeding gl/glcore, not a top-level video driver; users should set `video_driver = gl`, not `mali_fbdev`); flagged the r4p0 blob source and ARM developer URLs as dead.
- ✅ `README-OMAP.md`: added historical-reference banner (omapfb-era sysfs; omapdrm is the modern path); fixed escape artefacts.

Follow-up items (the items the cold-eyes pass surfaced that need decisions, not edits):

- ✅ **MEDIUM — SPEC — TLS spec needs BearSSL backend coverage or scope-narrowing.** `qb/config.libs.sh:485-498` shows `HAVE_BUILTINBEARSSL` as a peer of `HAVE_BUILTINMBEDTLS`; the spec at `docs/private/specs/2026-04-27-tls-verification-opt-in-design.md` is mbedtls-only. A user on a BearSSL build gets zero behavioural change. Either extend the spec with a parallel `## BearSSL backend` section keyed to `br_x509_minimal_set_*` knobs, or narrow the spec title/goals to "mbedtls-backed builds" and roadmap a sibling BearSSL spec.
- ✅ **MEDIUM — SPEC — TLS spec's RARCH_*-from-vendored-file shape needs a vendoring-clean replacement.** `libretro-common/net/net_socket_ssl_mbed.c` has zero `RARCH_*` references today and no `#include "verbosity.h"`; the spec proposes adding both, which creates exactly the merge debt §4 says it's avoiding. Replace with a weak-hook pattern (RA-side override) or have `ssl_socket_connect` return a distinct error code and let the RA caller log.
- ✅ **MEDIUM — SPEC — TLS spec failure-mode enumeration is incomplete.** Add a `## Failure modes` section covering revocation-unavailable (CRL/OCSP), time-skew (consoles without RTC), captive-portal redirect, enterprise MITM proxy refusing on REQUIRED. Each row should name the expected log line + user-visible behaviour per mode.
- ✅ **LOW — SPEC — TLS spec static `ssl_authmode` thread-safety race.** Settings-change writes vs. connect-time reads from the task-queue thread. Either `_Atomic unsigned` (C11, under HAVE_THREADS) or snapshot-at-entry with "mid-session toggle takes effect next connection" documented.
- ✅ **LOW — SPEC — TLS spec performance budget + automated test wiring.** Numeric handshake SLO (e.g. 2 s p99 desktop / 5 s p99 console-class) + a libcheck test under `libretro-common/test/net/` exercising `ssl_socket_set_verify_mode` against stub mbedtls; commit to exact log-line regex contracts so the conformance tests can match them.
- ✅ **MEDIUM — SPEC — Cloud-sync spec needs Phase 1-shipped rewrite.** _(Bundle 73.)_ Bundle 32 (`c3ebdb11e4`) landed the cap at the `network/cloud_sync_driver.c:129-164` dispatch layer (better than the per-backend layout in the spec). Rewrite Phase 1 / Data-model sections to "shipped" + "Phases 2-4 only" framing; re-anchor all `s3.c` line cites to function names (`s3_update`, `s3_sha256_hash`) — they drift ~130 lines per audit cycle; correct the mbedtls SHA-256 API to the unsuffixed legacy names (`mbedtls_sha256_starts/_update/_finish` — no `_ret` in the vendored copy at `deps/mbedtls/mbedtls/sha256.h:83,92,101`); extend the existing `s3.c:386 s3_sha256_hash` helper to offer an incremental variant rather than introducing a parallel helper.
- ✅ **MEDIUM — SPEC — Cloud-sync spec multipart sizing bump.** _(Bundle 73.)_ `mp_st->file_data` (s3.c near `:2014`) carries the entire file buffer for the duration of every part. Phase 4 was estimated at 0.5 day as "audit + maybe fix"; promote to a definite refactor with known shape (mp state needs `RFILE *` + offset, seek per part). Bump to ~1 day.
- ✅ **LOW — SPEC — Cloud-sync spec missing `## Failure modes`.** _(Bundle 73.)_ Network drop mid-upload; 5xx retry budget + backoff bound; OAuth-token expiry mid-upload (v1.1); HTTP 507 quota exhaustion; conflict resolution when remote is ahead of local. Numeric SLO: max heap inflation per concurrent upload (256 KB desktop / 128 KB console-class).
- ✅ **LOW — SPEC — Cloud-sync ↔ TLS spec cross-reference.** _(Bundle 73.)_ Cloud-sync upload streams credentials over a TLS pipe — a `## Cross-spec dependency` note in the cloud-sync spec should state "TLS verification (sibling spec) MUST be `required` or `optional` before this work hardens privacy claims; streaming changes data-at-risk shape (incremental vs one-shot) but not the auth surface."
- ✅ **LOW — SPEC — Cloud-sync UI placement reconciliation with TLS spec.** _(Bundle 73.)_ This spec puts the upload cap under *Settings → Cloud Sync → Advanced*; sibling TLS spec uses *Settings → Network → Advanced*. Existing menu UI in `menu/menu_setting.c` uses a flat *Network* page with section dividers — neither spec verified. Pick one convention and align both specs before implementation.
- ✅ **MEDIUM — SPEC — Menu-driver-swap spec needs API rename + gating-site refresh + D2 deletion.** _(Bundle 74.)_ Body cites `menu_settings_list_{new,free}` which do not exist; real names are `menu_setting_{new,free}` (decl `menu/menu_setting.h:102,104`; def `menu/menu_setting.c:25955,25743`). Decision D2 ("lift `menu_settings_list_free` from shutdown") is moot — `menu_setting_free` is already callable and already invoked from `menu_entries_settings_deinit` at `menu/menu_driver.c:2283`. Drop Phase 1 (the 0.5-day "lift"), reduce total to ~1.5 days. Refresh the §"Current behaviour" gating-site table from a current grep (all 13 line numbers drifted ~26 lines).
- ✅ **MEDIUM — SPEC — Menu-driver-swap D1 precedent appeal needs verification.** _(Bundle 74.)_ Spec claims `RUNLOOP_FLAG_*_DRIVER_RECONFIGURE` exists for audio/video driver swaps; `grep -rn RECONFIGURE runloop.h runloop.c retroarch.c` returns zero hits. The actually-existing precedent is synchronous `video_driver_reinit` (`gfx/video_driver.c:4827`) called from `command.c`. Re-evaluate D1(A) vs (B) against the real pattern, not the imagined deferred-flag pattern.
- ✅ **LOW — SPEC — Menu-driver-swap failure-fallback ordering.** _(Bundle 74.)_ Architecture box deinit-frees-old before init-new; if init-new fails the "fall back to old" requires re-running `menu_driver_init(old)` which can itself fail (unbounded recursion). Tighten to build-new-first / tear-down-old-only-on-success, or commit to a recursive-fallback termination (e.g. final fallback to `rgui`).
- ✅ **LOW — SPEC — Menu-driver-swap missing edge cases.** _(Bundle 74.)_ Swap during context-menu popup; swap with content loaded vs unloaded; swap during a task progress overlay. Lane brief explicitly flagged these as dim-10 gaps; none covered today.
- ✅ **LOW — SPEC — Menu-driver-swap `glui` vs `materialui` aliasing.** _(Bundle 74.)_ Spec uses `glui` exclusively. Add one sentence at first mention noting `glui` is the legacy ident string for the `materialui` driver (set at `menu/drivers/materialui.c:12232` — the ROADMAP S11-followup invariant comment is the canonical reference).
- ✅ **LOW — SPEC — Audit-spec-needed cluster: per-S Chosen/Bundle/Commit footers.** _(Bundle 75 — done as a top-level `## Closure summary` table.)_ Cluster is closed but the body voice reads "open recommendation." Add a `Chosen: (option), Bundle NN, commit <hash>` footer per S-section so a future maintainer can tell which option shipped without cross-walking ROADMAP. (Alternative: a top-level closure table mirroring the implementation-order table.)
- ✅ **LOW — SPEC — Audit-spec-needed cluster: dispose `<date>` placeholders.** _(Bundle 75.)_ §"Follow-on spec docs (proposed)" cites three `<date>-*.md` paths; all three exist as `2026-04-27-*.md` today. Replace `<date>` → `2026-04-27`, drop the "(proposed)" framing.
- ✅ **MEDIUM — DOC — Master `README.md` has no references to the three platform sub-READMEs.** `grep -n "exynos\|omap\|mali" README.md` returns zero. Either add a "Platform-specific build notes" section linking to `README-exynos.md` / `README-mali_fbdev_r4p0.md` / `README-OMAP.md` (with the "historical reference" caveat the banners now carry), or explicitly state in `README.md` that legacy platform docs sit at repo root and are discoverable by filename. Five-line edit. Defer to a session deciding whether to split the README's "Platforms" list into "Actively built" vs "Historically ported" sub-lists at the same time.
- ✅ **LOW — DOC — Master `README.md` "Platforms" list mixes maintained + abandonware targets.** `Blackberry`, `Windows NT 3.5`, `Windows 95-Millennium`, `ReactOS`, `RetroFW`, `SerenityOS` etc. sit next to Android/iOS/macOS/Linux/Windows-current with no distinguisher. A reader can't tell which platforms have working CI. Split into "Actively built" + "Historically ported" subsections, or drop the long tail. Pair with the platform-sub-README discoverability fix above.
- ✅ **LOW — DOC — Master `README.md` line 128 "OSX requires latest Xcode" is vague.** No numeric Xcode version pin. Either pin (best — current minimum is Xcode 12-ish to match `-mmacosx-version-min`) or move the build-step prose entirely to the Documentation Center link two lines down.
- ✅ **LOW — DOC — `CONTRIBUTING.md` coding-style § terser than CLAUDE.md.** `CLAUDE.md` "Coding rules (non-obvious)" enumerates C89 / no-VLA / Allman / `-Wsign-compare` / `MISSING_DECLS=1` / sort-struct-by-alignment / 128KB-stack rules; `CONTRIBUTING.md:91-95` mentions only "C89 + ISO C++ + `-Wall`." A contributor reading only `CONTRIBUTING.md` misses the stricter sub-rules. Either cross-link `CONTRIBUTING.md` → `CLAUDE.md`-non-obvious section or fold the sub-rules into `CONTRIBUTING.md`. (Note that `CLAUDE.md` is fork-only so upstream `CONTRIBUTING.md` is the natural canonical home for the sub-rules; cross-link is the lighter touch.)
- ✅ **LOW — DOC — `README-exynos.md` body still contains dead `tobiasjakobi/*` URLs.** Two GitHub fork URLs (`linux-odroid` and `libdrm`) are 404 as of 2026; the new banner now signals "historical reference," but a body-cleanup pass would replace those URLs with archive.org pointers or just-link-text. Same shape for `README-mali_fbdev_r4p0.md` (`forum.odroid.com` thread, `builder.mdrjr.net/tools/r4p0-mp400-fbdev.tar`, `malideveloper.arm.com`).
- ✅ **LOW — DOC — `README-mali_fbdev_r4p0.md` is plain text with `.md` extension.** Setext H1 at top but no further markdown structure — no fenced code blocks, no link syntax. Convert to real markdown (or rename to `.txt`). Lower priority than the dead-URL cleanup above.
- ✅ **LOW — DOC — `README-OMAP.md` content boundary is unclear.** File ends mid-section at line 39 with only `video_monitor_index` + `video_vsync` documented — `gfx/drivers/omap_gfx.c` likely reads more settings than that. Cross-check the source and document the full set or add an explicit "only these are honored" boundary statement.
- ✅ **LOW — DOC — Audit/README.md "Adding a new big-file boundary" should also note the `indie-review-partition.md` LoC-drift expectation.** Big-file LoCs cited in `indie-review-partition.md` drifted +9 to +43 lines since the last refresh; the doc already says "verify against current banner comments before each run" but the practical mechanism is `wc -l`. Add one line to the README walk-through naming the `wc -l` step.
- ✅ **LOW — DOC — ROADMAP.md bundle-table monotonicity.** Bundle 70 appears mid-table (rows are not chronologically ordered: 55→54, 60→59, 64→63→62, 69→68→67→66→**70**→65). A reader scanning for "newest bundle" hits Bundle 70 at row position 13 instead of last. Resort ascending by bundle number when the next bundle lands, or add a chronological column.
- 📋 **LOW — DOC — ROADMAP.md table cell widths (lines 65, 75-90) too wide.** Cells run 2.7-6.4 KB of inline prose; editing risks pipe-corruption. Restructure long-term to `Bundle | Commit | Theme (1-line)` with multi-paragraph narrative in per-bundle `### Bundle N — theme` subsections. Larger edit; deferred. Pair with the "Cumulative line is one 11k-character sentence" finding — split the cumulative into bullet form when the table restructure happens.



