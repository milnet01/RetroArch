# Review loop log — repo-root `CLAUDE.md`

Review history for this repo's `CLAUDE.md`. It is kept outside the file
because every session loads that file in full. `review-contract` writes one
row per loop.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-25 | 3 | 4 | 0 | 2 | 0 | First gate on this file (RETR-S0147). The run is armed by `5a551008ed` and this session's edits (`4ba330bc59`, `aeb43c313d`, `3e12649c24`, `c07532ed27`). 3 lanes, every lane held every question. The lanes' harness-loaded copy of the file was stale, so the scrubbing steps were inert as expected; each lane disclosed it and reviewed the disk copy. 6 verified, 0 dismissed, 6 fixed. [Q1] `dynamic.c` no longer exists, and core loading is in `runloop.c` — found while building the packet, fixed in `4b2ac005de`. [Q1] "Linux/Windows builds pick a new source file up automatically" was false: `Makefile.common` lists every object by hand (A). [Q1] "every new setting needs three edits" was incomplete: an existing setting spans seven or more files. It is now an executable search for an existing setting's lower- and upper-case names (A). [Q1] the version lockstep list omitted the `pkg/` platform manifests. It is now defined by a search for the version string (A). [Q3] the fixes worktree had no path and did not exist. It is named `/mnt/Games/Scripts/Linux/ra-fixes` with its create command, and the stale `/tmp/ra-fixes` registration was pruned (A, B, C). [Q3] whether `write-spec` still writes specs under the location override was unstated. It does, with its output redirected (C). Collateral: `coding-standard.md` and `security-standard.md` dropped `/tmp/ra-fixes`, and `coding-standard.md` no longer guarantees that `local/*` pushes start no CI. `file-naming-standard.md` repeats three of these claims and is left to its own gate (RETR-S0146). Loop 2 dispatched. |
| 2 | 2026-09-25 | 3 | 0 | 1 | 3 | 0 | Loop 2 of this run. 3 lanes, every lane held every question. 4 verified, 0 dismissed, 4 fixed. [Q2] loop 1's source-file fix made griffin registration unconditional, against § Driver pattern's "if it should build on consoles" (B). Both are now conditional. [Q3] "mirror every hit" swept in code that uses one setting (`runloop.c`) (A, B). Now scoped to registration hits. [Q3] whether a NEW commit body or loop-log row may cite line numbers was unstated (A). Dated records may, being written once and never revised; the orchestrator took this call on the user's live-docs-only decision. [Q3] `write-spec` does not read the location override, hard-codes `docs/specs/<ID>-<topic>.md`, and writes its loop log to a top-level `docs/reviews/`, which is the upstream collision the override exists to prevent (B open question, C). The file now says to hand `write-spec` the fork paths, loop log under `docs/private/reviews/`. Three of the four landed on text loop 1 wrote. Loop 3 (the cap) dispatched. |
| 3 | 2026-09-25 | 3 | 2 | 1 | 1 | 0 | Loop 3 of this run — the cap for a standard; shipped. 3 lanes, every lane held every question. 4 verified, 0 dismissed, 4 fixed. [Q3] the `write-spec` instruction could not be followed as written: the skill took no path, gives a plan its own `-plan-loop-log.md`, and the date-form filename leaves the roadmap id nowhere (A, B, C). A peer session fixed the skill during this loop (`~/.claude` `d24fce9`) so it reads a DECLARED location override for the spec, the plan and both loop logs. The section is now a declaration naming all four paths, with the id in the title line, and `docs/reviews/` is added to the collision reason. [Q1] `dynamic.h` does not declare `libretro_get_system_info`; `runloop.h` does (B). [Q1] the setting search cannot find the `DEFAULT_*` macro in `config.def.h` (B, C). The text now says to take its name from the `configuration.c` hit. [Q2] "every other new text uses names, not line numbers" dropped counts and sizes, which the parent rule still forbids (B). **Violent cap**: all 4 findings anchor on text this run wrote (loop 1 or 2, or the packet-time fix). The `write-spec` thread closed at its source rather than by another loop, so no further gate on this text as it stands. Share inside the armed span (`5a551008ed` + `4ba330bc59`, `aeb43c313d`, `3e12649c24`, `c07532ed27`): about 5 of the run's 14 verified findings; the rest were pre-existing. No deferred tail. |
