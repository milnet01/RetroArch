# Review loop log — `docs/private/standards/file-naming-standard.md`

Review history for the fork's file-naming standard, kept outside the
standard itself. `review-contract` writes one row per loop.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-25 | 3 | 2 | 1 | 0 | 0 | First gate on this file (RETR-S0146). The run is armed by `84d5339a93`, and by `71062ebbbc`, which wrote back the repo-root `CLAUDE.md` gate's findings. 3 lanes, every lane held every question. 3 verified, 0 dismissed, 3 fixed. [Q1] driver instances were given as `const <subsystem>_driver_t <name>_<subsystem>`, in §1 and again in §6. The tree uses `<subsystem>_<name>` (`audio_alsa`, `video_gl2`, `menu_ctx_ozone`), only record drivers are `const`, and joypads are `<name>_joypad` (A, B, C). [Q1] "every subsystem follows one shape" named directories, headers, types and arrays that video (`gfx/`), joypad (`input/drivers_joypad/`, `input_device_driver_t`) and menu (`menu_ctx_driver_t`, `menu_ctx_drivers[]`) do not follow (A, C). §1 now says names vary and gives the real ones. [Q2] "every file carrying the version string" was wider than the `CLAUDE.md` search it defers to (B, C). Collateral: the repo-root `CLAUDE.md` § Driver pattern step 2 carried the same false template, corrected in the same commit. Loop 2 dispatched. |
| 2 | 2026-09-25 | 3 | 2 | 0 | 2 | 0 | Loop 2 of this run. 3 lanes, every lane held every question. 5 verified, 0 dismissed, 4 fixed, 1 filed. [Q1] "write `<name>.c`" contradicted the sibling rule: joypads are `<name>_joypad.c`, record drivers `record_<name>.c` (B, C). The filename now follows siblings, with examples. [Q1] microphone drivers have no directory of their own; each lives in its audio backend's file, and `microphone_drivers[]` is in `audio/audio_driver.c` (A). Now stated. [Q3] design documents and ADRs had no location here, though `CLAUDE.md` puts them with specs (A, B). Added. [Q3] loop logs for a standard, a policy or `CLAUDE.md` had no naming rule (B; C as an open question). Now `<document-name>-loop-log.md`, the form the existing logs use. [Q3] §6's "no new global state" leaves file-scope `static` undefined (B). Pre-existing and outside the armed span, so filed as RETR-0002 rather than looped on. Collateral: `coding-standard.md` §6 still said Linux/Windows builds pick a new file up via `Makefile.common` (A open question); corrected. Loop 3 (the cap) dispatched. |
| 3 | 2026-09-25 | 3 | 2 | 0 | 1 | 0 | Loop 3 of this run — the cap for a standard; shipped. 3 lanes, every lane held every question. 3 verified, 0 dismissed, 3 fixed. [Q1] joypads were named as the only instance-name exception; HID drivers are `<name>_hid` in `<name>_hid.c` (`libusb_hid`) (A). Added to §1, the filename list and §6. [Q1] "each" microphone driver lives in its audio backend's file was false: `coreaudio_mic_macos.m` and `coreaudio_mic_ios.m` are dedicated files (C). Now "most", with the exception named. [Q3] the loop-log name rule produced neither of its own examples (A, B, C). It is now mechanical: filename lower-cased with `.md` dropped, with the repo-root `CLAUDE.md` as the one named exception. **Violent cap on the log-name rule**: that finding anchors on loop 2's text. The other two anchor on loop 1's §1 rewrite, which widened the section's claims. So 3 of 3 of the final loop's findings landed on this run's text, though each is a real omission exposed rather than a repair undone. Share inside the armed span (`71062ebbbc`): 2 of the run's 11 verified findings. No deferred tail beyond RETR-0002. |
