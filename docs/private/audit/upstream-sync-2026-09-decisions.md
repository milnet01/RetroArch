# Upstream sync decisions — 2026-09

One line per fork commit that needed a decision when `local/fixes-2026-04` was replayed onto upstream as `local/fixes-2026-09` (RETR-0003). Format: fork sha | subject | outcome | files | reason. Commits that applied cleanly are not listed.

# ra-sync replay decisions

Format: `<fork-sha7> | <subject> | decision | files | reason`

1f59d9d | save state + replay: bounds-check the malicious-file format trust boundary | PARTIAL | tasks/task_save.c, input/bsv/bsvmovie.c | Upstream already bounds-checks rastate blocks and bsv key/input event counts; the negative/short loaded_len check in replay_set_serialized_data was missing upstream and was ported.
8aedb93 | local IPC + cheevos: harden the localhost-attacker trust boundary | PARTIAL | command.c, cheevos/cheevos_client.c, configuration.c | Upstream already NULL-checks the READ_CORE_RAM/MEMORY mallocs (kept upstream replies); loopback bind, nbytes/write caps, memmove and cfg chmod ported, with COMMAND_READ_NBYTES_MAX moved outside HAVE_CHEEVOS so non-cheevos builds compile.
8eab270 | deref-before-NULL-check sweep across 8 Linux-build sites | PARTIAL | audio/drivers/oss.c, gfx/drivers/gl2.c, gfx/drivers/gl3.c, gfx/drivers/vulkan.c, input/common/wayland_common.c, input/input_driver.c, runahead.c, tasks/task_pl_thumbnail_download.c | runahead.c mylist_add_element already guarded upstream; the other seven sites still deref before the check and were ported (gl3 onto upstream video_dims, vulkan onto the new atlas-dirty block); stray .claude/scheduled_tasks.lock dropped.
6423760 | 4 array-bound bugs flagged by clang-analyzer security.ArrayBound | PARTIAL | audio/audio_driver.c, core_option_manager.c, menu/drivers/rgui.c, menu/menu_setting.c | Upstream already bounds slot_selection_idx in audio_driver_mixer_add_stream (with lock release); the categories calloc sizing, rgui scanline cap and retropad-bind index guards were still missing and were ported.
de0c600 | tasks: NULL http_task in completion callback | PARTIAL | task_core_updater.c (upstream kept), task_pl_thumbnail_download.c (auto-merged) | upstream redesigned core-updater threading (70b606ea85, 2f9703049c): the fork NULLing a plain pointer would race the worker. Kept upstream; possible UAF (worker calls task_get_flags(http_task) before checking completion) filed for review against the new model.
9bf01aa | config triad cleanup: zombie settings, bound guard, default mismatch | PARTIAL | configuration.c | Upstream deliberately registers rgui_show_start_screen without a default and keeps the minimal-mode force-write (558b24a685), so that hunk was dropped; the populate_settings_* count asserts and the missing game_ai_override_p1/p2/show_debug persistence rows were ported (quick_menu_show_game_ai is now generated upstream).
088289d | state_manager: bound find_change/find_same walks against buffer size | UPSTREAM-FIXED | state_manager.c | Upstream reworked the scanners (AVX2 path) around a documented invariant: sentinels at block_size+6 plus STATE_MANAGER_SCAN_PAD tail padding guarantee termination inside the allocation, so the bounded walk is superseded.
bcb1ad4 | audio + menu + bsv: three small targeted fixes | PARTIAL | audio/audio_driver.c, menu/menu_driver.c, input/bsv/bsvmovie.c | Upstream moved the audio buffers into arenas that stay reachable and are freed by the next deinit, so the early-return "leak" is gone and goto error would change the return value (upstream kept); the menu driver_data NULL guard and bsv checkpoint alloc-before-commit were ported.
18b55ec | cloud sync + core options: NULL-deref / UB / uninit-stack cluster | PARTIAL | network/cloud_sync/webdav.c, tasks/task_cloudsync.c, core_option_manager.c | Upstream already NULL-checks the webdav mkdir state (and now builds the URL via webdav_url_for_path); the data memset, the manifest-key NULL/traversal guard and the value_hash malloc checks were still missing and were ported.
149fdd5 | audio + gfx font init: free struct on init-failure paths | PARTIAL | audio/drivers/audioworklet.c, gfx/drivers/caca_gfx.c, gfx/drivers/sixel_gfx.c, gfx/drivers/vga_gfx.c (gdi_gfx.c, rwebaudio.c upstream) | Upstream already frees on failure in gdi_gfx.c and rwebaudio.c; the other four sites still leaked and were ported onto upstream's new FONT_ATLAS_FORMAT_A8 call. NOT COMPILED: none of these drivers builds on this Linux host.
72b2a83 | defensive small fixes: gx_joypad pad bound + webdav 404 body filter | PARTIAL | input/drivers_joypad/gx_joypad.c, network/cloud_sync/webdav.c | Upstream webdav_read_cb now writes only when found (2xx), so the 404 filter is superseded; the gx_joypad DEFAULT_MAX_PADS bound was still missing and was ported. NOT COMPILED: gx_joypad.c is Wii/GameCube-only.
5c99961 | realloc-leak class: tmp-pattern at runahead + bsv uint32s sites | PARTIAL | runahead.c, input/bsv/uint32s_index.c | Upstream mylist_resize already reallocs into a temporary; the uint32s_bucket_expand calloc NULL check and commit-cap-after-realloc were still missing and were ported.
3d10605 | bsv uint32s_index: harden against double-free + RHMAP_PTR auto-create | PORTED | input/bsv/uint32s_index.c | Neither guard existed upstream; ported bucket_free reset plus the empty-index and RHMAP_HAS checks onto upstream's restructured pop, with the missing-hash case still shrinking the buffers so uint32s_index_remove_after cannot spin forever.
71fa88e | wayland: NULL-deref + DnD leak cluster (audit M + clang-tidy HIGH) | PORTED | gfx/common/wayland_common.c, input/common/wayland_common.c | All three defects were still present upstream; the empty-all_outputs guard was re-applied onto upstream's packed dims interface, and the dangling-output skip and DnD buffer/line frees merged unchanged.
89b8adf | slang_process: explicit NULL-guard on the four set_decoration sites | UPSTREAM-FIXED | gfx/drivers_shader/slang_process.cpp | Upstream rewrote the file as slang_process.c on the spirv-cross C API; the no-HLSL case now errors out and both compilers are always created before set_decoration, so the guarded path no longer exists.
92d8e97 | runahead: CSPRNG temp-DLL filename + O_EXCL/O_NOFOLLOW | UPSTREAM-KEPT | runahead.c | upstream replaced the copy with an async VFS copy task; the fork hardening would need changes in vendored libretro-common (forbidden locally). Predictable temp name filed as RETR security item for upstream.
e9681a0 | ui_qt: NULL-guard the five SHDR_FLAG_MODIFIED writes | UPSTREAM-FIXED | ui/drivers/ui_qt_widgets.cpp | Upstream now sets SHDR_FLAG_MODIFIED inside the if (video_shader)/if (menu_shader) blocks at all five sites.
c2818d5 | cloud_sync: verify Content-Length on read responses | PORTED | network/cloud_sync/webdav.c, google_drive.c, s3.c | Upstream net_http.c states it cannot detect a truncated close-delimited body and the read callbacks still write data->data unchecked; the per-driver Content-Length/chunked verifier was ported onto upstream's restructured callbacks. s3.c is not in Makefile.common and fails to compile on upstream-only lines (settings->arrays.s3_url etc.); webdav.c/google_drive.c compile with only a pre-existing upstream warning at webdav.c:162.
9ed554a | network + tasks: cap attacker-controlled allocations on discovery + CD serial | UPSTREAM-FIXED | network/netplay/netplay_frontend.c, tasks/task_database_cue.c | Upstream caps LAN discovery at NETPLAY_MAX_DISCOVERED_HOSTS in netplay_discovery_reserve_host and caps the CUE serial buffer at SERIAL_PROBE_SPAN; negative or out-of-range spans fail at malloc/read.
856758e | core/menu/gfx/network: clang-tidy MEDIUM memory-leak class (7 sites) | PARTIAL | core_backup.c, gfx/gfx_animation.c, menu/drivers/ozone.c, network/discord.c (core_info.c, menu/menu_setting.c upstream) | core_info.c is fixed upstream; menu_setting.c's strdup removal was REJECTED because upstream's setting_*_setting no longer strdups and takes ownership of the caller's copy (removal would free stack buffers); the other four leaks were still live and were ported.
ab7f4d0 | verbosity + tasks + menu: branch-on-uninitialized stack buffer (3 sites) | PARTIAL | tasks/task_screenshot.c (verbosity.c, menu/menu_displaylist.c upstream) | Upstream already initialises log_directory/log_file_path and rom_buff (the merged duplicates would also have broken C89 declaration order); new_screenshot_dir was still uninitialised and was ported.
e1d376d | runahead: re-query savestate size to catch mid-run growth (2 sites) | PORTED | runahead.c | Neither site re-checks the size upstream; the runahead check was re-applied against upstream's single runahead_savestate_info (info->size) and the preempt check merged unchanged.
c3ebdb1 | cloud_sync: configurable max-upload cap | UPSTREAM-KEPT (to revert: its auto-staged half landed as 16f04d233b; revert at end) | configuration.c, menu_setting.c, msg_hash.h, msg_hash_us.h | the cap is a new user setting; upstream moved settings to settings_def_*.h, so porting is feature work. Root cause (whole upload file in memory) is covered by docs/private/specs/2026-04-27-cloud-sync-streaming-upload-design.md; left to that design.
9ddc80c | audit S11: strip identicalInnerCondition double-checks (6 sites, 1 FP) | PARTIAL | audio/audio_driver.c, audio/drivers/dsound.c, camera/camera_driver.c, cheat_manager.c, menu/menu_driver.c, tasks/task_content.c (openal.c upstream) | Cosmetic dead-code strip; re-verified each inner check is still redundant under upstream (cheat_manager outer bits<8 branches unchanged); openal.c kept as upstream since its device-selection block was reworked and the strip is behaviour-neutral. dsound.c not compiled (Windows-only).
d6bbeea | audit S1+S10: (1 << 31) signed-overflow UB sweep | PARTIAL | input/input_driver.c, input/input_types.h, menu/cbs/menu_cbs_sublabel.c, network/netplay/netplay_frontend.c (runloop.h upstream) | Upstream moved RUNLOOP_FLAG_IS_INITED out of bit 31, so runloop.h keeps upstream's int enum (1u<<31 in an enum is not C89 anyway); the turbo/hold unsigned conversion and variable-shift sweeps were still needed and merged consistently (every sign-bit test converted).
335bb31 | audit S2: MENU_LIST_GET_SELECTION callers - NULL-guard the ->size deref | PARTIAL | menu/drivers/ozone.c, menu/drivers/xmb.c, menu/cbs/menu_cbs_right.c, menu/cbs/menu_cbs_sublabel.c, menu/menu_driver.h | Upstream had moved or already guarded several sites (xmb entry-index block relocated; cbs and several xmb/ozone sites already guarded); the remaining unguarded ozone/xmb sites were ported, and ozone's list_size declaration (dropped by the merge) was restored in guarded form.
44e71ea | audit S2 ext: materialui + rgui MENU_LIST_GET_SELECTION sweep | PORTED | menu/drivers/materialui.c, menu/drivers/rgui.c | Upstream guards these sites on menu_list only, not the returned selection_buf[0] slot; the slot guard was ported at all listed sites (conflicted materialui site uses upstream's existing menu_st). Upstream-new unguarded sites at rgui.c/materialui.c list_size/entries_end were outside the fork commit's scope and left as upstream.
e35479c | clang-tidy HIGH: vulkan_overlay_free - gate loop on images non-NULL | PORTED | gfx/drivers/vulkan.c | Upstream's loop (now deferred-free, skipped when borrowed) still reads images[i] before the NULL check; the gate was re-applied around upstream's loop, keeping its borrowed/defer semantics and dropping the merge-reintroduced synchronous destroy loop.
e1e0fd8 | clang-tidy MEDIUM: bugprone-incorrect-roundings - switch to lroundf | PARTIAL | gfx/gfx_widgets.c, menu/drivers/materialui.c, menu/drivers/ozone.c | Upstream already rounds the ozone spacers with its new C89 VIDEO_PX helper; the remaining (x + 0.5f) sites were ported using VIDEO_PX instead of the fork's C99 lroundf.
9edf7ff | clang-tidy MEDIUM: vulkan memcmp on math_matrix_4x4 - compare .data | PORTED | gfx/drivers/vulkan.c | Upstream still memcmp'd the whole struct; the .data comparison merged cleanly and the conflicting comment was folded into upstream's rewritten one.
2760d93 | clang-tidy MEDIUM: bugprone-sizeof-expression in gfx_widgets msg_queue | UPSTREAM-FIXED | gfx/gfx_widgets.c | Upstream replaced the msg_queue FIFO with locked gfx_widgets_pending_push/pop, so the three sizeof(msg_widget) sites no longer exist.
9498278 | clang-tidy MEDIUM: float-loop counters - switch to integer index | PARTIAL | menu/menu_displaylist.c, ui/drivers/ui_qt_widgets.cpp | Upstream already moved the INT/UINT dropdown loops and Qt UIntRadioButtons to integer counters; the two ST_FLOAT dropdown loops and UIntComboBox::populate still used float counters and were ported by hand (auto-merge had left half-converted blocks), with the Qt span clamped to INT_MAX before the int conversion.
dbb3f03 | clang-tidy MEDIUM: divide-by-zero - clamp glyph_width at init, guard rgui downscale dims | PORTED | menu/drivers/materialui.c, menu/drivers/rgui.c | Neither guard exists upstream; the glyph_width >= 1 clamp was applied after upstream's VIDEO_PX rounding (dropping the fork's stale font-path block, which upstream moved), and the zero max_width/max_height guard was placed after upstream's packed max_dims unpacking.
794a7a0 | cloud-sync: NULL-check OOM allocation paths in google_drive.c + webdav.c | PARTIAL | network/cloud_sync/google_drive.c, network/cloud_sync/webdav.c | Upstream already checks the gdrive refresh/folder-walk contexts and the webdav read state (with its own cb_state_finish helper); the digest-auth chain (ha1/ha2/response/header), gdrive_begin_with_token and webdav_delete allocations were still unchecked and were ported.
6f59dc0 | menu: strip 5 dead vtable slots from menu_ctx_driver_t | PORTED | menu/menu_driver.h, menu/menu_driver.c, menu/drivers/{materialui,ozone,rgui,xmb}.c, menu/cbs/menu_cbs_scan.c, gfx/gfx_thumbnail.h | All five slots are still dead upstream (three dispatched, but every driver registers NULL); removed from upstream's updated struct (keeping its dims-based osk_* slots), and every vtable initialiser was mechanically checked to drop exactly the same five positions.
54fc62b | ai/game_ai: migrate strcpy/strcat to strlcpy/strlcat | UPSTREAM-FIXED | ai/game_ai.c | Upstream already bounds g_game_name with strlcpy and builds data_path with length-tracked strlcpy plus a too-long abort.
220b4e0 | video-processor-v4l2: strcpy -> strlcpy for splitresolution copy (2 sites) | UPSTREAM-FIXED | cores/libretro-video-processor/video_processor_v4l2.c | Upstream replaced both strcpy+strtok parses with a validated sscanf (and removed the first block), so no unbounded copy remains.
b510d9d | cloud-sync: atomic write via tmp + rename in download paths | PORTED | network/cloud_sync/google_drive.c, webdav.c, s3.c | All three read callbacks still truncate-and-write the destination upstream; ported using upstream's libretro-common filestream_write_file_atomic helper (which also checks the close) instead of the fork's hand-rolled tmp+rename, then reopening read-only for the caller's hash. s3.c not in the build (pre-existing upstream errors elsewhere in the file).
6dae6b6 | menu/netplay: deref-before-NULL-check follow-up sweep on larger files | PARTIAL | menu/cbs/menu_cbs_ok.c, menu/drivers/materialui.c, menu/drivers/ozone.c, menu/drivers/xmb.c (netplay_frontend.c upstream) | The real deref-before-check bugs (menu_cbs_ok settings x3, ozone_frame pending_message, xmb list_cache, materialui savestate node/remember_selection/strlen, thumbnail s NULL) were still live and ported onto upstream's code (VIDEO_SCALE_H, video_info theme); the fork's cosmetic removals of redundant checks (ozone theme/node, menu_cbs_ok delim, netplay key/value) were dropped because upstream kept and extended those checks.
3537f96 | menu: S2 follow-up sweep + Bundle 54 dead-clause cleanup | PARTIAL | menu/drivers/ozone.c, menu/drivers/xmb.c, menu/drivers/materialui.c (menu_cbs_ok.c upstream) | ozone_change_tab is already safe upstream (NULL stack gives size 0 and hits its stack_size<1 return); the set_thumbnail_content and selection_changed selection_buf guards (combined with upstream's new bounds check) and materialui_preswitch_tabs guard were still needed and ported; the cosmetic sys_info dead-clause removals in menu_cbs_ok.c were dropped.
c4b1b42 | cppcheck Bundle 57: %d/%u + invalidLifetime + uninitvar + nullPointer sweep | PARTIAL | gfx/display_servers/dispserv_kms.c, command.c, gfx/drivers_context/xegl_ctx.c, gfx/gfx_thumbnail.c, gfx/video_shader_parse.c, input/input_driver.c (video_crt_switch.c upstream) | Upstream moved the CRT super_width handling into crt_apply_menu_preset with no printf; the kms %u formats were re-applied to upstream's VIDEO_SCALE_W/H lines and the other hunks (shader-parse %u, del_idx cast, xegl vid init, thumbnail shadow_color lifetime, bind NULL guard) were still needed and merged. Pre-existing upstream -Wstringop-overread at video_shader_parse.c:1905 is untouched.
6a2b703 | cppcheck Bundle 58: deref-before-check + useless-assign + FP suppressions | PARTIAL | audio/drivers/wasapi.c, .cppcheck-suppress.txt (playlist.c, core_info.c upstream) | playlist_push's core_name deref is fixed upstream (the auto-merged line would have clobbered the out-parameter, so playlist.c is upstream verbatim); wasapi_free still read w->write_event and w->client before any NULL check and got an early return; the core_info dead-store removal was dropped as cosmetic; suppression entries kept (line anchors are pre-sync). wasapi.c not compiled (Windows-only).
f03558b | cppcheck Bundle 59: tasks/+network/ uselessAssign + dead-clause sweep | PORTED | tasks/task_translation.c, task_autodetect.c, task_content.c, task_core_backup.c, task_core_updater.c, task_database.c, task_menu_explore.c, task_pl_thumbnail_download.c, task_playlist_manager.c, .cppcheck-suppress.txt | Cosmetic dead-store/redundant-clause removals applied cleanly to unchanged upstream code (task_content archive_delim premise re-verified); the task_translation negative-stride signedness fix was re-applied onto upstream's VIDEO_SCALE_W(dims).
3545d70 | cppcheck Bundle 60: runahead OOM + libretrodb varFuncNullUB + alloca | PARTIAL | libretro-db/libretrodb.c, libretro-db/rmsgpack_dom.c, runahead.c, .cppcheck-suppress.txt | Upstream mylist_create already NULL-checks and runahead_create now uses runahead_savestate_info_init (list hunk obsolete, orphan comment dropped); the input_state_list caller guard, the (const char *)NULL variadic terminators (re-applied to upstream's RDF_-typed calls) and the alloca-to-fixed-array change were still needed and ported.
bd9fc14 | clang-tidy Bundle 61: clang-analyzer null-deref + dead-defensive sweep | PARTIAL | menu/menu_setting.c, menu/menu_driver.c, menu/menu_driver.h, retroarch.c, runahead.c (command.c, gfx/video_driver.h upstream) | Upstream's VIDEO_HAS_FOCUS is already NULL-safe, mylist_create already handles the inner calloc failure (capacity 0), and command_event_reinit was rewritten (cosmetic dead-clause drop not ported); the password-field guard, general_write_handler/menu_running_finished settings guards, generic_menu_iterate selection_buf gate, NULL-safe MENU_LIST_GET_STACK_SIZE, populate-helper guards (one moved below its declarations for C89), mylist_resize guard and NOLINT annotations were ported.
9ed75a3 | cppcheck Bundle 62: materialui.c post-Bundle 55 cleanup (3 sites) | PORTED | menu/drivers/materialui.c | All three sites unchanged upstream (random-selection loop premise re-verified); the %u format was re-applied onto upstream's strlcpy_lit line.
2137199 | clang-tidy Bundle 64: bugprone-integer-division tree-wide sweep (51 sites) | PARTIAL | gfx/gfx_display.c, gfx/gfx_widgets.c, gfx/widgets/*.c, menu/drivers/materialui.c, menu/drivers/ozone.c, menu/drivers/xmb.c | Cosmetic half-pixel centring sweep; hunks that merged onto unchanged float destinations were kept (destinations re-checked as float params/locals). Conflicted blocks sit on draw calls upstream reworked to packed integer video_dims, so upstream was kept there; one conflict side would also have reverted upstream's (int)((float)y + scroll_y) UB fix.
de24958 | cppcheck Bundle 65: post-Bundle 62 sweep on the other menu drivers | PARTIAL | menu/drivers/xmb.c (ozone.c, rgui.c upstream) | The xmb_draw_icon shadow_color lifetime bug is still live upstream and the hoist was ported (with the shadowing rename in xmb_pointer_up); the ozone/rgui/xmb dead-write and dead-initialiser removals were dropped because upstream reworked those blocks and their unconditional-assignment premises could not be re-verified.
3a6278b | test-audit Bundle 70: libretro-common test-suite repairs | UPSTREAM-KEPT | libretro-common/test/{hash,lists,queues,string,utils}/*.c | Vendored code: upstream already fixed the build break and dropped the NULL calls, so upstream's tests were kept; still missing upstream are the 3+3 END_TEST macros in test_linked_list.c and test_generic_queue.c (START/END counts 89/86 and 16/13) and the ck_assert_str_eq/ptr assertion-quality conversions.
00e6d85 | test-audit Bundle 76: make libcheck suite runnable + coverage-gap fold-in | UPSTREAM-KEPT | libretro-common/Makefile.test, lists/linked_list.c, test/*.c | Vendored code kept as upstream (upstream already dropped the NULL-arg test calls); still missing upstream: the !matches NULL guard in linked_list_remove_{first,last,all}_matching (a real NULL call), the Makefile.test link fixes (compat_strldup.c, file_path_io.c) and -Wno-error=discarded-qualifiers, and the added regression/coverage tests.
31e000b | test-audit Bundle 77: libretro-common test refactor (loop-test tables, checked fixtures, mkstemp) | UPSTREAM-KEPT | libretro-common/test/{hash,lists,queues,utils}/*.c, test/test_tmpfile.h | Vendored test-only refactor, no behaviour change; upstream tests kept. Still missing upstream: tmpnam() to mkstemp temp files, and the loop-test/checked-fixture restructuring.
14bf6e5 | cppcheck Bundle 79: port Bundle 69 menu/ sweep onto fixes branch | PARTIAL | menu/cbs/menu_cbs_ok.c, menu/menu_contentless_cores.c, menu/menu_displaylist.c, menu/menu_driver.c, menu/menu_explore.c, menu/menu_setting.c | Upstream already NULL-guards the menu_driver list/cbs site and keeps the delim check (cosmetic removal dropped); the %u formats (re-applied to upstream's VIDEO_SCALE_W/H and strlcpy_lit lines, -Wformat-signedness clean on changed lines), action_iterate_type !label guard (kept after upstream's new confirm check), menu_explore MENU_LIST_GET guards, displaylist S2 guard and contentless_cores missing return were ported.
5729e8a | TASKS: document task_queue.c lock contract + push_progress re-entrancy | UPSTREAM-KEPT | libretro-common/queues/task_queue.c | Vendored, comment-only; upstream kept. Possibly missing upstream: the written lock contract (running_lock vs property_lock, nesting order) and the note that push_progress callbacks must not call task_get_/task_set_ accessors.
8f71456 | cppcheck Bundle 81: materialui.c caller-safe style sweep (48 sites) | STYLE-DROPPED | menu/drivers/materialui.c | Behaviour-neutral const/shadow/scope sweep (no defect) conflicting 7 ways with upstream's reworked materialui.c; its shadow removals depend on outer variables that moved, so it was not re-applied. Re-run cppcheck style on upstream's file if still wanted.
6f318c1 | cppcheck Bundle 82: materialui.c const-correctness completion | STYLE-DROPPED | menu/drivers/materialui.c, .cppcheck-suppress.txt | Behaviour-neutral const-correctness continuation of the dropped Bundle 81 sweep on upstream's reworked materialui.c; its S20 suppressions are anchored to lines of the dropped version, so both were left out.
25bdb0c | menu/materialui: drop 3 unused includes (Bundle 83) | PORTED | menu/drivers/materialui.c | frontend_driver.h, core_info.h and input_osk.h are still unreferenced in upstream's materialui.c (re-checked symbol by symbol); removed, keeping upstream's new gfx_surface.h include.
55f0df0 | cppcheck Bundle 84: ozone.c caller-safe style sweep (45 sites) | STYLE-DROPPED | menu/drivers/ozone.c | Behaviour-neutral const/shadow/scope sweep conflicting 9 ways with upstream's reworked ozone.c; shadow reuse depends on function-scope variables upstream moved, so it was not re-applied.
f710e8c | cppcheck Bundle 85: xmb.c caller-safe style sweep (37 sites) | STYLE-DROPPED | menu/drivers/xmb.c | Behaviour-neutral const/shadow sweep conflicting 16 ways with upstream's reworked xmb.c; its 'provably equal to the outer' shadow removals depend on the fork's code shape and were not re-verified, so it was not re-applied.
dc2e9d2 | cppcheck Bundle 86: rgui.c caller-safe style sweep (32 sites) | STYLE-DROPPED | menu/drivers/rgui.c | Behaviour-neutral const/shadow/scope sweep conflicting 5 ways with upstream's reworked rgui.c; not re-applied, consistent with Bundles 81/84/85.
436928d | net/tls: enforce certificate verification by default (fixes CRITICAL MITM) | PORTED | libretro-common/net/net_socket_ssl_{mbed,bear}.c, libretro-common/include/net/net_socket_ssl.h, network/tls_config.h, network/tls_log.c, settings/settings_def_netplay_sync.h, menu/menu_setting.c, retroarch.c, config.def.h, configuration.h, msg_hash.h, intl/msg_hash_us.h, msg_hash_lbl_str.h, Makefile.common, griffin/griffin.c | Upstream still forced MBEDTLS_SSL_VERIFY_OPTIONAL; the mbedtls authmode switch (REQUIRED default, fail-closed), weak no-op log hooks and bear stub were re-applied onto upstream's last_err/min_version code, and tls_verify_mode became an S_UINT_EX row (SD_FLAG_ADVANCED, HAVE_NETWORKING+HAVE_SSL) appended to settings_def_netplay_sync.h so it lands where the fork had it (after Analog Input Sharing) - the hand-written configuration.c/menu_setting.c/msg_hash_lbl entries were dropped; option-value strings stay hand-written like cloud_sync_sync_mode's; default verified 0 -> MBEDTLS_SSL_VERIFY_REQUIRED via retroarch_main_init.
1a238db | C89: fix decl-after-statement + enum-range regressions breaking linux-c89 CI | PARTIAL | runloop.c, menu/menu_setting.c (runloop.h, verbosity.c upstream) | Upstream moved RUNLOOP_FLAG_IS_INITED out of bit 31, so runloop.h keeps upstream's enum; verbosity.c's init relocation targets inits the dropped ab7f4d0 hunk never added here (upstream initialises log_directory itself), so upstream kept; the runloop.c SET_MESSAGE p_dispwidget hoist fixes a live decl-after-statement from the earlier guard port, and the menu_setting.c comment merged.
16a6baf | gfx/drm: fix NULL-deref + uninitialized-read in DRM/KMS video driver | PORTED | gfx/drivers/drm_gfx.c | Both defects were still live upstream; the missing return after the NULL plane_resources log merged unchanged and connector = NULL was re-applied beside upstream's new modeset_buf declaration (compiles; the sign-compare warnings in the file are pre-existing upstream).
89ae09c | tasks/cloudsync: extract path-traversal guard into testable predicate + regression test | PORTED | tasks/task_cloudsync.c, tasks/task_cloudsync_path.{c,h}, samples/tasks/cloudsync/{Makefile,cloudsync_path_safety_test.c}, Makefile.common, griffin/griffin.c, local-CI.sh, .github/workflows/Linux-samples-tasks.yml | Upstream kept the inline guard with no regression test; the predicate extraction was re-applied onto upstream's sync_state->dirlist (dropping the fork's now-unused settings local), and upstream's new samples/tasks/cloudsync/Makefile was extended to build and check both its savefile_owner test and the path-safety test (both pass).
d1f64ca | tests: lock the rastate save-state block-bounds fix against silent revert | UPSTREAM-FIXED | tasks/task_save.c, tasks/task_save_rastate.{c,h}, samples/tasks/save_rastate/*, Makefile.common, griffin/griffin.c, local-CI.sh, .github/workflows/Linux-samples-tasks.yml | Upstream rewrote content_load_rastate1's bounds (input+8<=stop loop, uint32 decode, END-before-extent) and deliberately clamps the last block's padding, which the fork predicate rejects (aligned > remaining), so wiring the predicate in would regress valid flush-ended states; whole commit dropped. Still missing upstream: a regression test for the rastate block bounds.
31d115c | tests: lock the UPnP IGD description-parser fix against silent revert | PORTED | network/natt.c, network/natt_desc.{c,h}, samples/tasks/natt_desc/*, Makefile.common, griffin/griffin.c, local-CI.sh, .github/workflows/Linux-samples-tasks.yml | Upstream's natt_parse_desc_node/natt_build_control_url already match the fork fix but had no test; the walk was moved into natt_desc.c with upstream's strlcpy_lit, upstream's new natt_device_is_busy kept in natt.c, and the rastate CI/local-CI steps that rode along were dropped with d1f64ca (natt test passes 13/13).
d8d03d7 | metainfo.xml: backfill <release> blocks for 1.20.0 -> 1.22.2 | PORTED | com.libretro.RetroArch.metainfo.xml | Upstream metainfo (version.all 1.22.2) had no release blocks newer than 1.9.11; the 1.20.0-1.22.2 blocks applied cleanly with no duplicate versions and the file validates with xmllint. (audit-branch-only commit)
ff483a6 | build Bundle 63: wire HAVE_S3 into build (--enable-s3 qb flag + Makefile gate) | PORTED | Makefile.common, qb/config.libs.sh, qb/config.params.sh, network/cloud_sync/s3.c | Upstream guards S3 code/settings on HAVE_S3 but still has no qb flag or Makefile gate on this build system; the default-off --enable-s3 wiring (kept beside upstream new HAVE_LIBSMB param) and the credential[512] truncation fix were applied; s3.c syntax-checks clean with -DHAVE_S3, and ./configure was re-run (HAVE_S3 = 0). (audit-branch-only commit)
c8996e2 | cppcheck Bundle 66: suppress 6 cppcheck false-positives Bundle 65 deferred | PORTED | menu/drivers/rgui.c, menu/drivers/xmb.c | Comment-only inline suppressions; applied cleanly and each lands directly above the same unchanged upstream statement (ticker assignments, 37:37/58:58 ternaries, height_offset continuation); rgui.o/xmb.o compile. (audit-branch-only commit)
b68aa9b | cppcheck/clang-tidy Bundle 67: port-forward Bundle 64+65 xmb.c portions to audit branch | UPSTREAM-KEPT | menu/drivers/xmb.c | Audit-branch port-forward of Bundles 64/65, whose fixes-branch originals (2137199, de24958) were already replayed as PARTIAL; its six conflicts are the same packed-video_dims / reworked-block hunks those decisions kept upstream, so the whole commit was skipped. (audit-branch-only commit)
2804c81 | cppcheck/clang-tidy Bundle 68: port-forward Bundle 64+65 ozone+materialui to audit branch | UPSTREAM-KEPT | menu/drivers/materialui.c, menu/drivers/ozone.c | Audit-branch port-forward of Bundles 64/65 whose fixes-branch originals (2137199, de24958) were already replayed as PARTIAL; its conflicts sit on the same reworked draw calls and dead-write blocks those decisions kept upstream, so the whole commit was skipped. (audit-branch-only commit)
150ace8 | cppcheck Bundle 69: menu/ top-level sweep - printf-spec, S2-sibling, deref-before-check | UPSTREAM-KEPT | menu/cbs/menu_cbs_ok.c, menu/menu_displaylist.c, menu/menu_driver.c, menu/menu_setting.c (menu_contentless_cores.c, menu_explore.c no-op) | Audit-branch original of the sweep already replayed from its fixes-branch port 14bf6e5 (Bundle 79, PARTIAL): its explore/contentless hunks merged as no-ops and the four conflicts are the sites that decision already resolved (upstream guards kept, the rest ported), so the whole commit was skipped. (audit-branch-only commit)
16f04d2 | (auto-staged half of c3ebdb1) cloud_sync: configurable max-upload cap | REVERTED | network/cloud_sync_driver.c, config.def.h, configuration.h | git revert --no-edit 16f04d233b as planned under c3ebdb1: the cap setting was not ported (settings moved to settings_def_*.h), so its half-applied dispatch-layer check and default were removed; no cloud_sync_max_upload references remain and cloud_sync_driver.o compiles.
